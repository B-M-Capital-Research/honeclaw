# Decisions

Last updated: 2026-07-19

## D-2026-03-07-01 Maintain LLM Collaboration Context In-Repo

- Status: Accepted
- Decision: Keep the stable rules, repo map, current plan, decision log, and task handoffs as repository documents instead of relying on session history
- Impact: A new session should read `AGENTS.md`, `docs/repo-map.md`, and `docs/current-plan.md` first
- Details: See `docs/adr/0001-repo-context-contract.md`

## D-2026-03-07-02 Separate Long-Lived Rules From Task State

- Status: Accepted
- Decision: `AGENTS.md`, `repo-map`, `invariants`, and `decisions` only store long-lived information; `current-plan` and `handoffs` only store the current task and single handoff state
- Impact: Temporary state must not be piled into `AGENTS.md`, and long-term rules must not be written into handoffs

## D-2026-03-07-03 Make Documentation Updates Part of Done

- Status: Accepted
- Decision: Documentation maintenance is not optional; changes that affect behavior, structure, or workflow must update the matching context docs
- Impact: Delivery requires checking the context assets in addition to code and tests

## D-2026-03-07-04 Normalize User-Owned Data by `ActorIdentity`

- Status: Accepted
- Decision: All long-lived on-disk data that belongs to a user must use `ActorIdentity(channel, user_id, channel_scope)` as the ownership key instead of raw `user_id`
- Impact: Sessions, scheduled tasks, portfolios, generated image directories, and any future stores of the same kind should use the actor as the file key or query key
- Note: In direct chat, `channel_scope` is empty; in group or shared-context scenarios, the concrete channel fills in `channel_scope`

## D-2026-03-07-05 Switch Dynamic Plans to an Index Page Plus Single-Task Files

- Status: Accepted
- Decision: `docs/current-plan.md` is only an active-task index page; each parallel task uses its own `docs/current-plans/*.md`
- Impact: Parallel tasks no longer fight over one detailed plan file; starting or switching tasks now requires updating both the index page and the single-task plan page
- Note: The index page records the task name, status, ownership scope, and file links, while the detailed todo and progress live in the matching task file

## D-2026-03-07-06 Use a Minimal Handoff Policy

- Status: Accepted
- Decision: Handoffs are only kept for tasks that need transfer, pause-and-resume support, or medium-or-larger tasks with follow-up risk. When possible, prefer updating the original file instead of creating fragmented new ones.
- Impact: Small pure-execution tasks do not get a new handoff by default; handoffs should keep only the goal, result, verification, risk, and unfinished items, not a full activity log
- Note: A handoff is a relay document, not a complete operation log

## D-2026-03-08-01 Use Local SQLite for the Minimal LLM Audit Layer

- Status: Accepted
- Decision: Land the minimal viable LLM audit layer in the repo first, organizing call records by `ActorIdentity + session_id + created_at`
- Storage: Use local SQLite instead of one JSON file per call; enable WAL to balance write cost and later lookup by actor / session / time
- Coverage: function-calling agent, Gemini / Codex CLI agents, and session compression
- Retention: Keep a rolling 30-day window by default; clean once on startup and then incrementally every 100 writes
- Impact: New audit-chain changes should reuse the audit types in `hone-core` and `memory/src/llm_audit.rs` instead of reimplementing them in channel entrypoints

## D-2026-03-13-01 Unify `AgentSession` and the Channel Session Flow

- Status: Accepted
- Decision: Add `agent_session` in `hone-channels` to unify session lifecycle, system prompt construction, event listeners, and logging. Channel code should run through `AgentSession` while keeping placeholder / streaming adapters.
- Impact: Channel entrypoints should no longer build `restore_context`, `build_system_prompt`, or `ensure_session_exists` on their own; new channels or new flows must reuse `AgentSession` and `AgentSessionListener`

## D-2026-03-15-01 Disable iMessage by Default

- Status: Accepted
- Decision: The default project config keeps iMessage off, and the scheduler skips deliveries to disabled iMessage targets. The UI should no longer treat iMessage as a default channel.
- Impact: Unless runtime config is changed explicitly, iMessage will not start or receive scheduler deliveries; historical iMessage tasks are for viewing only and should not be added to any new active workstream.

## D-2026-03-17-01 Unify Agent Runtime Around Runner + Prompt Layering + Session V2

- Status: Accepted
- Decision: `AgentSession` now funnels through the `run()` entrypoint; executor selection comes from `agent.runner`; prompts are split into three layers - static system, session-fixed context, and dynamic session context; session storage is upgraded to versioned JSON v2 and explicitly stores `summary` and `runtime.prompt.frozen_time_beijing`
- Impact:
  - Channels and the Web UI no longer split into `run_blocking` / `run_gemini_streaming`
  - The Web chat SSE protocol is now `run_started / assistant_delta / tool_call / run_error / run_finished`
  - Session summaries are no longer encoded as fake `system` messages
  - The breaking config key moved from `agent.provider` to `agent.runner`
- Note: `opencode_acp` connects through `opencode acp` over stdio / JSON-RPC. Later runner hardening moved ACP continuity back to Hone's local transcript restore: `opencode_acp` and `codex_acp` both seed fresh ACP sessions from local context instead of relying on remote `session/load` replay.
- Details: See `docs/adr/0002-agent-runtime-acp-refactor.md`

## D-2026-03-18-01 Make Dynamic Plans Opt-In

- Status: Accepted
- Decision: `docs/current-plan.md` and `docs/current-plans/*.md` only track tasks that need ongoing follow-up; only medium-or-larger items, cross-turn / cross-module changes, behavior / structure / workflow changes, or tasks that need parallel collaboration, handoff, or blocker management should enter the dynamic plan docs
- Impact: Small tasks such as a single commit / sync / rebase, light script or config fixes, no-behavior-change patches, and pure copy or formatting changes are no longer mechanically written into the dynamic plan docs; the simple task todo can stay in the delivery context
- Note: The dynamic plan docs are meant to support handoff and parallel work, not to log every action

## D-2026-04-08-01 Reuse One Execution-Preparation Path for Session and Scheduler Runs

- Status: Accepted
- Decision: Keep `AgentSession` as the public session entrypoint, but move prompt-audit write, tool registry creation, runner creation, and actor-sandbox-backed request assembly into a shared `execution` layer inside `hone-channels`
- Impact:
  - Scheduler heartbeat / transient task flows must not instantiate concrete runners directly
  - `AgentSession` stays focused on session semantics such as quota, slash skill handling, and transcript persistence
  - Session compaction and prompt audit are now explicit support services rather than ad hoc logic inside the main orchestrator

## D-2026-04-09-01 Normalize Active Plans, Handoffs, and Archive Index

- Status: Accepted
- Decision: Keep `docs/current-plan.md` as an active-only index, require a concrete `docs/current-plans/*.md` file for every active tracked task, move completed plan pages into `docs/archive/plans/*.md`, and use `docs/archive/index.md` as the stable entry point for historical work. Standardize new plan / handoff / decision documents around the templates in `docs/templates/*.md`.
- Impact: Agents can no longer leave active-task links dangling without backing files, and historical work no longer depends on `docs/current-plan.md` retaining a growing "recently completed" section. Future task closure should update the archive index and, when applicable, move the plan page into `docs/archive/plans/*.md`.
- Note: Existing older documents may keep legacy formatting, but any touched or newly created task-tracking document should carry the minimal metadata and structure defined in `AGENTS.md`.

## D-2026-04-11-01 Centralize Runtime Overrides and Channel Bootstrap

- Status: Accepted
- Decision: Keep runtime data-root / skills-dir override logic in `hone-core::HoneConfig`, and keep channel startup scaffolding in `hone-channels::bootstrap_channel_runtime`. Entry binaries should only add channel-specific protocol wiring after these shared layers complete.
- Impact:
  - `hone-web-api` and channel binaries should not rewrite storage, session, or runtime directory paths ad hoc
  - New channel entrypoints should reuse shared logging, enabled-check, process-lock, and heartbeat bootstrap instead of cloning startup logic
  - Channel-local scheduler or outbound helpers should live beside the channel module instead of expanding the binary entry file

## D-2026-04-12-01 Let OpenCode Own Its User Config By Default

- Status: Accepted
- Decision: `opencode_acp` should inherit the user's local OpenCode provider/auth/model config by default. Hone may still inject a narrow custom `OPENCODE_CONFIG` for permissions and explicit overrides, but it must not replace the user's global OpenCode config root or force an OpenRouter-centric default route when `agent.opencode.*` is empty.
- Impact:
  - First-install CLI onboarding should tell users to finish provider setup in `opencode` itself
  - `config.example.yaml` should leave `agent.opencode.model / variant / api_base_url / api_key` empty by default
  - The runner should not override `XDG_CONFIG_HOME` just to apply Hone's ACP permission policy
- Note: Explicit Hone-side overrides through `agent.opencode.*` or `hone-cli models set ...` remain supported for users who want Hone to pin a different route than their local OpenCode default.

## D-2026-04-17-01 Use Inline `file://` Markers As The Canonical Local Image Contract

- Status: Accepted
- Decision: Hone assistant-visible local images use inline `file:///abs/path/to/image.png` markers in the final assistant text. Web keeps that marker in message content and renders it through the local file proxy, while outbound chat channels must parse the same text into ordered `text` / `local-image` segments and send real images instead of leaking raw local paths.
- Impact:
  - Skill or tool scripts that generate charts or other local images should expose absolute artifact paths and instruct the model to place the exact `file://` URI where the image should appear in the answer
  - `skill_tool` is responsible for validating image artifacts before they become model-visible paths
  - Web history extraction must recognize inline local image markers as attachments
  - Feishu / Telegram / Discord outbound adapters must preserve interleaved `text -> image -> text` order and replace local markers with actual uploaded channel images
- Note: v1 keeps this contract entirely inside the final assistant text and does not introduce a separate SSE media event type.

## D-2026-04-24-01 Route Price Alerts Through Directional Band Lanes

- Status: Accepted
- Decision: Price alerts use daily low/close ids plus directional intraday band ids instead of one `price:{symbol}:{date}` id per day. Intraday bands are keyed as `price_band:{symbol}:{date}:{up|down}:{band_bps}` and default to `6%` with `2pp` steps.
- Impact:
  - A low-magnitude price move can enter digest first, then a later same-day cross of `+6%/+8%` or `-6%/-8%` can still dispatch as a distinct event.
  - Price bands use `price_realert_step_pct` to form intraday lanes and `price_band_min_advance_pct` to decide whether a newer band advances far enough for a direct push.
  - Close price alerts are digest-only by default through `price_close_direct_enabled=false`.
  - Digest buffering treats price alerts as latest-state rows per actor/symbol/date/window, replacing older queued price rows instead of appending duplicates.
- Note: This preserves old event rows in SQLite; it only changes ids and routing for new price observations.

## D-2026-05-11-01 Make LLM Credentials Config-Only

- Status: Accepted
- Decision: LLM provider/profile/auxiliary/agent credentials are configured only through `config.yaml`; runtime must not use `*_API_KEY`, `*_BASE_URL`, or `api_key_env` fallback as a second truth source
- Impact:
  - `llm.providers.<symbol>.api_key/api_keys` is the preferred provider credential path, and `llm.profiles.*.provider` references that symbol
  - Legacy `llm.openrouter.api_key/api_keys` remains readable only as config-owned migration fallback, not as an env bridge
  - CLI/Desktop settings should write inline config keys and mask API-key fields in display/logs
  - Missing credentials should fail with a migration hint pointing to `config.yaml`
- Note: Child-process bridges such as passing a config-owned OpenRouter key into `opencode` are allowed when the underlying CLI has no config API, but Hone must not read parent process env vars as user LLM config.

## D-2026-07-10-01 Project Web Scheduled Results Into A Durable Push Inbox

- Status: Accepted
- Decision: Keep the canonical scheduled-task transcript unchanged for agent context and channel delivery, but project deliverable `web` scheduler results into a separate actor-scoped push store with stable push ids, deterministic summaries, full content, delivery timestamps, and server-owned `read_at` state. Public chat history and SSE render this projection as summary cards; Feishu and other channel adapters keep their existing output.
- Read semantics: Opening push `N` marks every push owned by the same Web actor with an order at or before `N` as read. Opening an older push therefore preserves newer unread state, while opening the latest push clears the aggregate unread indicator. Opening the push center itself acknowledges the latest item known at that instant; pushes arriving afterward remain unread and restore the aggregate red dot.
- API impact: Public list responses carry summaries and unread counts only; full content is fetched from an authenticated actor-scoped detail endpoint. Read state is used to drive the aggregate red dot but is not rendered as per-message read/unread copy.
- Compatibility: Historical scheduled turns without push ids are lazily backfilled on the actor's first push-list request with deterministic `legacy:*` ids and durable read state. The import is actor-scoped and idempotent, preserves any existing `read_at`, and makes pre-upgrade messages available in the same inbox as new scheduler pushes.

## D-2026-07-11-01 Separate The Public macOS App From The Local Runtime Desktop

- Status: Accepted
- Decision: Package the production public user experience as a dedicated remote-only Tauri app in `bins/hone-user-app` instead of extending the full `hone-desktop` bundle. The app opens `https://hone-claw.com/chat`, keeps only first-party Hone navigation in its WebView, and owns no backend, sidecar, ACP, MCP, channel, skill, config, or local-data lifecycle.
- Impact: Public macOS releases use `scripts/build_user_app.sh`; full local-runtime desktop packaging remains a separate lane. The user app is tested and built on macOS rather than included in the default Linux workspace gate.
- Security: The local shell ships a restrictive CSP, permits in-app HTTPS navigation only to Hone-owned hosts, and sends unrelated HTTP(S)/mail links to the system browser. The release bundle must be inspected for unexpected resources and external binaries before distribution.
- Distribution: Unsigned development machines may produce an ad-hoc signed Universal `.app` / `.dmg` for internal use. Public distribution still requires an Apple Developer ID identity and notarization.

## D-2026-07-11-02 Use One HONE Brand And Remote Boundary For Public Apple Clients

- Status: Accepted
- Decision: Public Web, macOS, and iOS clients use `HONE` as the sole user-facing product brand and share one mark/wordmark language. The native Apple shells open the production public Web experience rather than duplicating chat, push, calendar, or account business logic.
- iOS boundary: `apps/hone-ios/` is an independent SwiftUI/WKWebView project. It persists the normal WebKit login store, allows in-app navigation only to HONE-owned HTTPS hosts, hands external links to iOS, and contains no local runtime or sidecar lifecycle.
- Release impact: Tag releases upload the Universal macOS DMG, iOS Simulator app, iOS Xcode source, and Apple checksums in addition to existing CLI assets. A device IPA is only valid when Apple signing/provisioning credentials are configured and must not be inferred from an unsigned Simulator artifact.
- Compatibility: `packages/app` remains the public feature source of truth; backend API, session, push-read, channel, and storage contracts are unchanged by the native shells or brand refresh.

## D-2026-07-11-03 Layer Public Visual Ownership And Version Generated Images

- Status: Accepted
- Superseded in part by: `D-2026-07-12-02`; the CSS ownership decision remains active, while visible-history lazy calendar regeneration and client-side variant selection are retired.
- Decision: The public user client uses a layered CSS ownership model instead of page-local style strings and late ad hoc overrides. `public-foundation.css` owns HONE tokens and interaction foundations, `public-polish.css` owns shared public navigation/push components, `public-chat.css` owns the chat shell, and generated visual artifacts keep component-local styles beside their render component.
- Rendering: Generated mobile finance calendars use one Canvas 2D renderer for both new sends and visible-history lazy upgrades. Text is painted with explicit baselines and coordinates rather than rasterizing DOM line boxes. Every material artifact redesign increments the mobile filename marker; visible older versions are lazily rebuilt in the browser without mutating conversation history.
- Impact: New public visual work must extend the narrowest owning layer rather than adding a `<style>` block to a page component. Shared token changes require public-page and authenticated-chat checks; calendar composition changes require direct Canvas source-size and 390px visual verification on an iOS-compatible path.
- Compatibility: API, persistence, desktop-calendar output, Feishu, and other channel behavior remain unchanged. The version marker affects only which Web mobile image is selected or lazily regenerated.

## D-2026-07-12-01 Reserve Native Agent Runners For Trusted Administrators

- Status: Accepted
- Superseded in part by: `D-2026-07-13-01`; the in-process function-calling fallback is retired, while the native-runner administrator trust boundary and fail-closed requirement remain active.
- Decision: Treat local CLI/ACP runners as trusted-host execution rather than actor sandboxes. When a non-admin actor reaches shared execution preparation and the configured runner can access the host, route the turn through the in-process function-calling runner with an actor-bound tool registry; if no such LLM is available, fail closed. Explicit administrators retain the configured native runner.
- Data boundary: Runtime configuration/data/actor-sandbox roots use owner-only Unix permissions. Skill scripts clear inherited server environment before execution. Public credentialed CORS accepts only HONE production origins, local development origins, and exact operator-configured origins.
- Supply-chain boundary: Keep lockfile security patches current; Discord uses Serenity's native TLS backend so its fixed `tokio-tungstenite 0.21` dependency does not retain the unpatched `rustls-webpki 0.102` branch.
- Impact: Public Web users and non-admin channel users can use registered tools but cannot ask an ACP/CLI agent to inspect repository, config, database, home-directory, or process data. Native ACP remains an administrator trust boundary and must not be presented as a strict filesystem sandbox.
- Verification: Runner-selection tests cover non-admin fallback, admin retention, and fail-closed behavior; permissions, skill environment, CORS, file traversal, and cross-actor push-read tests cover the supporting boundaries.

## D-2026-07-12-02 Persist Finance Calendar Variants And Select Them Server-side

- Status: Accepted
- Supersedes: The finance-calendar rendering and compatibility portion of `D-2026-07-11-03`; generated-artifact CSS ownership remains unchanged.
- Decision: Every new public finance-calendar message must persist validated desktop and mobile PNG paths plus its month in structured session metadata. Public bootstrap/history inspect the authenticated request User-Agent and project exactly one actor-owned `finance_calendar.image_path` and variant. The user client renders that stable path and must not fetch calendar data, repaint Canvas, create a blob URL, or replace the source while restoring visible history.
- Compatibility: Historical calendar messages without metadata are projected server-side by parsing their existing image markers. Mobile requests select the second marker when available; truly desktop-only legacy messages retain the desktop image. Compatibility projection does not mutate the transcript or expose paths outside the authenticated image proxy.
- Cache: Stored image paths are immutable upload artifacts. Authenticated image responses use private immutable browser caching so repeated restores do not refetch or regenerate the artifact.
- Impact: Calendar creation still renders the two artifacts once before upload, but display and history restoration are backend-owned. A send without both validated variants fails instead of creating another incomplete message.

## D-2026-07-12-03 Stream Public Replies From The Active Safe Runner

- Status: Accepted
- Superseded in part by: `D-2026-07-13-01`; function-calling streaming and tool-loop behavior are retired, while native runner streaming, client event handling, and no-replay-after-stream-start remain active.
- Decision: Public chat streaming follows the runner selected by the shared execution boundary. Trusted administrators using Codex/OpenCode ACP keep native `agent_message_chunk` streaming; non-admin actors remain on the strict actor-bound function-calling runner and receive native OpenAI-compatible/OpenRouter SSE content and tool-call deltas. Streaming must never be implemented by slicing an already-complete response.
- Tool-loop contract: Provider streams expose structured content, reasoning, indexed tool-call fragments, and optional usage. Function calling assembles parallel tool ids/names/arguments by index, executes only the actor-bound registry, hides internal reasoning blocks across chunk boundaries, and emits visible `StreamDelta` events. A model preamble emitted before a later tool call is withdrawn with transient `StreamReset`; only the final normalized assistant response is persisted.
- Retry contract: API-key fallback is allowed only before an upstream stream starts. Once any successful streaming response has begun, transport errors surface through the existing run error state and must not replay the request, preventing duplicated output or duplicated tool execution. Providers without native tool streaming retain a single-response compatibility fallback.
- Client impact: The public user client edits the existing in-thread thinking card, batches token deltas once per animation frame, handles reset in place, and preserves an explicit error phase on failed streams. The public SSE protocol therefore includes `assistant_reset` in addition to the existing `run_started / assistant_delta / tool_call / run_error / run_finished` events.
- Security: This decision does not reopen native ACP/CLI access for ordinary users and does not change final transcript ownership, quota, actor isolation, or persistence boundaries established by `D-2026-07-12-01`.

## D-2026-07-13-01 Retire In-Process Function Calling and Multi-Agent

- Status: Accepted
- Superseded in part by: `D-2026-07-15-01` restores the strict actor-bound function-calling safety fallback; `D-2026-07-15-02` restores the full investment workflow and response-format prompt; `D-2026-07-15-03` adds code-level enforcement for deep single-stock replies. `multi-agent` and user-selectable `function_calling` remain retired.
- Supersedes: The in-process fallback portion of `D-2026-07-12-01` and the function-calling stream/tool-loop portions of `D-2026-07-12-03`.
- Decision: Remove the in-process `function_calling` agent and the sequential `multi-agent` runner. Use the unified configured runner for both conversation and transient heartbeat execution, with `codex_acp` as the default local path.
- Default: `gpt-5.6-sol` with `xhigh` reasoning effort through `@openai/codex >= 0.144.1` and `@agentclientprotocol/codex-acp >= 1.1.2`.
- Compatibility: Old `function_calling` and `multi-agent` config values fail explicitly; they do not silently select another runner. Historical records may retain the old names.
- Security: Native CLI/ACP runners remain administrator-only trusted-host capabilities. With the actor-bound fallback removed, non-admin native-runner requests fail closed and should use `hone_cloud`; this change does not present an ACP/CLI subprocess as a strict filesystem sandbox.
- Prompt impact: Keep `soul.md` as a compact persona layer, keep hard runtime policies in Rust, attach only query-relevant skill summaries to the current turn, and use `discover_skills` instead of injecting the full catalog into every system prompt.

## D-2026-07-15-01 Restore The Strict Actor-Bound Safety Runner

- Status: Accepted
- Supersedes: The non-admin fail-without-routing portion of `D-2026-07-13-01`; preserves its retirement of `multi-agent` and of `function_calling` as a user-selectable primary runner.
- Decision: Keep `codex_acp` as the default primary runner for explicit administrators, but route every non-admin persistent conversation or transient scheduler task to the in-process function-calling runner whenever the configured primary runner is a trusted-host CLI/ACP. The fallback receives only the actor-bound Hone tool registry and never launches the native subprocess.
- Security: If the actor-bound LLM is unavailable, fail closed. Do not weaken the administrator boundary or present ACP working directories as filesystem sandboxes.

## D-2026-07-15-02 Restore The Full Investment And Response-Format Prompt

- Status: Accepted
- Supersedes: The compact-`soul.md` portion of `D-2026-07-13-01`; runner retirement, Codex ACP defaults, and query-relevant skill disclosure remain unchanged.
- Decision: Treat `soul.md` as the complete behavioral contract for investment reasoning and answer composition, using the pre-`71a4498e` large prompt as the baseline. It must retain task routing, single-stock and sector analysis order, fact/inference/conclusion/action separation, valuation discipline, Bull/Bear/Base framing, financial-comparison fields, answer ordering, anti-repetition rules, and user-facing capability behavior.
- Layering: Rust prompt policy remains the hard enforcement layer for live-data truth sources, privacy, storage, channels, cron, and security. Channel-specific Markdown/HTML guidance takes precedence over old generic formatting examples.
- Runtime truth: Canonical `soul.md` is authoritative. Effective-config generation refreshes the runtime prompt copy even when a stale destination exists; direct edits to `data/runtime/soul.md` are not durable configuration.

## D-2026-07-15-03 Enforce Deep Single-Stock Evidence And Reply Structure In Code

- Status: Superseded in scope by `D-2026-07-16-01`; its nine-section deep-analysis answer contract and fail-closed final validation remain active.
- Decision: A recognized security plus deep-analysis/outlook intent (including quarter outlook, “起飞”, valuation, fundamentals, earnings, or buyability) is a code-enforced route, not only a system-prompt suggestion. Before the runner starts, Hone resolves the entity and verifies a same-symbol positive quote; deep routes additionally fetch current profile, financial statements, company news, and—when the question is forward-looking—the next 120 days of earnings-calendar evidence.
- Answer contract: The turn suffix requires nine numbered sections in the established order: conclusion; company/business model; moat; industry/competitors; financial quality; at least two suitable valuation methods; Bull/Bear/Base; catalysts/risks/falsification; and an actionable buy/wait/reduce/sell/observe recommendation. It also requires a data timestamp and explicit fact/inference separation.
- Enforcement: The final answer is validated before persistence. A non-conforming candidate is retried once with the missing contract items. Since `D-2026-07-16-02`, candidate text and retry resets remain internal; if the retry still fails, Hone fails closed without first exposing a superficial or unverified investment conclusion.
- Scope: Simple quote-only questions still receive concise answers, but they must pass the same entity and same-symbol quote preflight. Missing or mismatched quote/financial evidence stops numeric conclusions; history, profiles, model memory, and unrelated symbol fields cannot fill the gap. The guard applies only to direct, uniquely identified single-security turns: scheduler/heartbeat envelopes and multi-security comparisons retain their own execution contracts, generic finance/report acronyms are not ticker hints, and entity search must return an exact symbol match rather than silently selecting its first approximate result.

## D-2026-07-16-01 Make Security Entity Resolution The First Investment Stage

- Status: Accepted
- Superseded in part by: `D-2026-07-18-01` replaces this decision's interactive pre-run outcome machine and structured extraction with discovery inside the main Agent tool loop. Its typed scheduled/heartbeat preparation, exact-symbol evidence, asset routing, answer contracts, and provider failure semantics remain active.
- Supersedes: The single-security-only scope, text-envelope detection, acronym denylist, and final-stage ticker inference in `D-2026-07-15-03`. Its deep-analysis evidence set, nine-section answer format, one internal retry, and fail-closed behavior remain active.
- Decision: Every investment turn enters one entity-resolution stage before company-specific planning or generation. The stage produces one of four explicit outcomes: no named security for a genuinely broad macro/industry request, one or more normalized securities, an ambiguous candidate set, or an unresolved entity. Named companies, aliases, tickers, share classes, and comparison lists must be resolved against current-turn DataFetch `search` results; history and model memory cannot establish identity.
- Matching: An explicit ticker requires an exact ticker candidate. A company name or alias may be ranked against provider candidates, but equally plausible listings or share classes remain ambiguous and must be clarified; the runtime must never select the first search result by position. All resolved securities require a positive same-symbol current quote before numeric conclusions.
- Bare ticker path: A normal bare ticker such as `NBIS` or contextual lowercase `nbis` is a first-class lexical candidate and must not depend on auxiliary-LLM JSON success. It becomes a security entity only after DataFetch search returns an exact symbol; assignment keys, report periods, broad industry/metric acronyms, and unrelated lowercase words must remain outside this fast path. Complex names and aliases still use structured extraction, and a valid empty structured result must not be repopulated from unconfirmed uppercase tokens.
- Data flow: `AgentSession` prepares one structured investment contract and carries it through execution, response validation, reset, and retry. `AgentTurnOrigin` is typed as interactive, scheduled, or heartbeat. Scheduled execution passes the original task body separately from its delivery envelope, so metadata such as `REPEAT` can never be parsed as a security and scheduler/heartbeat cannot bypass entity resolution.
- Asset routing: After exact-symbol search and a positive same-symbol quote, every non-crypto security must be classified from the exact profile's structured fields before deep evidence is selected. `isEtf` / `isFund` routes to fund evidence (`profile + ETF holdings + news`), an explicit company profile routes to equity evidence (`profile + meaningful financial statements + news`), and an exact crypto-market search result routes to crypto evidence (`quote + news`). An empty company financial response cannot prove that an unknown instrument is a fund; unknown asset types fail closed. Fund and crypto routes must not call company financials or earnings calendars, and crypto must not require a stock profile.
- Answer contracts: Interactive deep single-security requests retain an asset-specific nine-section contract. Equity answers cover the company/business, moat, competition, financial quality, valuation, scenarios, risks and action trigger; fund answers cover mandate, holdings/concentration, exposure, liquidity, costs/tracking, valuation context, scenarios, risks and action trigger; crypto answers cover market role, structure, liquidity, token/supply evidence when available, valuation context, scenarios, risks and action trigger. Multi-security comparisons must cover every resolved symbol with its own asset-appropriate evidence, state a data timestamp, and include risks or falsification conditions. Scheduled and heartbeat turns use the same entity, quote and asset-route gate but keep their delivery-specific response shape.
- Provider semantics and retry audit: A successful semantic-empty response is distinct from an authentication/provider failure. Empty profile/search/quote/financial/holdings payloads are not cached. Only authentication, quota, or rate-limit failures may rotate FMP keys; transport, parse, ordinary provider, and server errors stop without fanning out across credentials. Forbidden evidence calls are accumulated across the initial draft and retry so a clean retry cannot erase an invalid fund/crypto company-data call. Response-format and numeric validation must use the same sanitized visible content that SSE, persistence, and final delivery expose; raw `<think>` or tool-protocol blocks remain internal and cannot create or hide user-facing sections.
- Failure behavior: Ambiguous or unresolved identity, unavailable entity search, missing same-symbol quotes, or a response that omits required entities or evidence fails closed with a user-facing clarification or availability message. Internal parser labels, provider payloads, and guard diagnostics must not leak into the answer.

## D-2026-07-16-02 Commit Investment Replies Only After Validation

- Status: Accepted
- Superseded in part by: `D-2026-07-18-01` extends the speculative-output boundary to every nonempty interactive turn because named-security scope is discovered inside the Agent loop; `D-2026-07-18-02` removes post-run observation from Interactive validation/publication control; `D-2026-07-18-03` allows one complete canonical line from an eligible Agent-signaled tool-free terminal phase to commit early. Typed noninteractive behavior remains separate.
- Supersedes: The user-visible `StreamReset` withdrawal behavior for investment contract retries in `D-2026-07-15-03` and `D-2026-07-16-01`; their evidence, retry, and fail-closed requirements remain active.
- Decision: Investment runner attempts execute behind a session-owned deferred-output boundary. Progress and sanitized tool status may remain visible, but candidate `StreamDelta`, `StreamReset`, `StreamThought`, and attempt-local `Error` events never reach clients. After entity/evidence checks, contract validation, final sanitization, and attachment processing succeed, the session emits exactly one canonical final answer.
- Retry and failure semantics: Empty-success, transient-runner, investment-contract, and context-overflow retries all reuse the deferred boundary. Attempt flags cannot claim that hidden text or a hidden error was user-visible. A terminal failure is emitted once by the outer session; a successful run emits one terminal result. Web must not synthesize a second `run_finished` after the session listener has handled `Done`.
- Scope: Guarded typed investment turns trade token-by-token display for validated one-shot display, where correctness and stable rendering take priority over draft latency. Interactive tool-capable rounds also remain behind the boundary because they may still branch to tools, but their same-run observation never decides whether the successful answer may ship. Under `D-2026-07-18-03`, only a bounded canonical first line from an explicitly tool-free terminal phase may cross early; the finalized tail still publishes once without reset/replay.

## D-2026-07-16-03 Make Active Chat Runs Server-Authoritative

- Status: Accepted
- Decision: A running Web chat turn is identified by a server-owned `run_id`, `started_at_ms`, phase, safe status text, and update time in an actor/session-scoped active-run registry. Public bootstrap/history and live SSE read the same state. Conversation quota `in_flight` remains a billing reservation count and must never be used as proof that a runner is alive.
- Refresh and interruption semantics: Refresh only reattaches the UI observer to the existing detached runner; it never reposts the prompt. The recovered timer uses the original server start time. When the current process has no matching active run and the persisted interactive transcript still ends with an unanswered user turn, bootstrap returns an explicit interrupted state instead of fabricating a new thinking timer. Scheduled-push projections do not hide an older unanswered interactive turn.
- Progress and security: Deferred investment output remains final-only. `run_progress` and `tool_call.public_status_text` may expose only fixed user-safe phase text; raw tool names, provider messages, reasoning, attempt errors, draft deltas, and resets are not a substitute for progress and must not leak through the public UI.
- Lifecycle and operations: One session may have only one registered active run. An RAII guard clears the matching `run_id` after terminal persistence, including error and early-return paths. Controlled `hone-cli start` shutdown checks `/api/runtime/active-chat-runs` and waits for zero active turns up to a bounded timeout before terminating children. A process crash is represented as an interrupted turn after restart rather than a still-running task.
- Deployment boundary: The current registry is process-local because the production Web runner is single-instance. A future multi-Web-instance topology must replace it with a shared leased/fenced run registry or enforce sticky ownership; quota counters are not an acceptable distributed substitute.

## D-2026-07-17-01 Make Investment Facts And Repair Server-Authoritative

- Status: Accepted; its second model-repair semantics are superseded for supported quote/equity/fund/crypto/market scopes by `D-2026-07-17-02`.
- Superseded in part by: `D-2026-07-18-01` replaces the blocking auxiliary-LLM entity extraction and pre-tool clarification path; its server-owned verified facts, visible prefix, evidence reduction, deferred output, and terminal semantics remain active.
- Supersedes in part: The model-authored timestamp/current-price wording and whole-answer retry behavior left implicit by `D-2026-07-16-01` and `D-2026-07-16-02`. Their entity-first, asset-aware, deferred-output, and fail-closed contracts remain active.
- Entity scope: Every nonempty turn first resolves to exactly one of `securities`, actor `portfolio`, `broad`, `confirmed no entity`, or `needs clarification`. Exact ticker mentions use a deterministic DataFetch exact-symbol path. Possible named companies that remain unresolved may use the auxiliary LLM, but the extraction has a 15-second bound and timeout/provider/malformed/incomplete results fail closed without silently keeping a partial comparison set. The auxiliary schema distinguishes a valid ordinary-finance result (`entities=[]`, `unresolved_mentions=[]`) from an actually named but unresolved security (`unresolved_mentions` nonempty); only the former may enter confirmed-no-entity.
- Portfolio scope: Personal portfolio/watchlist requests read the actor-scoped portfolio tool once as the membership/cost truth source. If the user asks for current price, change, or performance analysis, an explicitly requested ticker, or a bounded service-selected portfolio subset when no ticker was named, must still enter the exact DataFetch entity/quote contract; portfolio state is not live-market evidence. The snapshot records total/included/truncated and market-symbol total/included/omitted counts, and the answer must disclose limited coverage instead of silently describing it as the full portfolio. A read preflight does not satisfy a requested write/delete operation.
- Canonical visible prefix: The server, not the model, owns the first Beijing data-time line, normalized security identity, same-symbol quote, price change, quote source time, and verified-fact labels. Those fields are rendered from the prepared investment contract before the sanitized model body. A model cannot omit, contradict, or falsely deny current quote capability after DataFetch supplied a verified quote; a later format failure retains the verified time/entity/quote instead of rewriting it as a provider outage.
- Evidence semantics: Provider payloads are reduced to typed, symbol-matched evidence before prompt injection. Profile snapshot-price fields are removed; company financial evidence uses explicitly labeled annual income-statement metrics and does not reinterpret net income as cash flow or net cash; missing cash/debt/FCF/capex, consensus, forward, or peer/history valuation data is explicit. Exact asset fields route equity, ETF/fund, and crypto through separate evidence contracts. Entity-filtered news excludes acronym collisions such as mortgage `RMBS`; every claimed current event must match a verified real absolute date and full source domain in the same clause, while mixed-market research uses the relevant exchange-local date for each requested market.
- Repair semantics: The initial sanitized candidate remains the repair source. One contract repair may rewrite missing structure, but it must not re-run the original user request or erase already verified facts. Persistent operations are execute-once: ambiguous native-runner traces suppress automatic replay rather than risk duplicate writes. Guarded attempts remain hidden and publish one canonical answer only after validation.
- Stream terminal semantics: The session `Done` event is the single terminal authority. Web sends at most one `run_finished`, closes the stream immediately after it, ignores late frames, and preserves the server's original run start time across refresh. This prevents a completed answer from flashing into a second run or resetting its elapsed timer.
- Operational diagnosis: Live FMP/DataFetch probes and Tavily search diagnostics succeeded during this repair. The reported NBIS/RMBS/INTL symptoms were caused by Hone's internal entity/asset routing and response-format validation/repair path, not by a general FMP or Tavily outage.
- Migration and rollback: This decision changes runtime orchestration, validation, and Web stream handling only; it has no database or durable-storage migration. Roll back by restoring the prior server/frontend revision, rebuilding assets/binaries, and performing the controlled runtime restart. No actor session, portfolio, or other durable data transformation is required.
- Verification: Unit and integration coverage must include lowercase/common tickers (`RMBS`, `NBIS`, `INTL`), exact symbol and asset routing, quote timestamp/freshness, unsupported financial claims, market/sector templates, repair preservation, execute-once operations, terminal fencing, and live DataFetch probes.

## D-2026-07-17-02 Use Contract-Only Fallbacks And Exact Extended-Hours Quotes

- Status: Accepted
- Supersedes: The second whole-answer model repair in `D-2026-07-17-01` for supported quote/equity/fund/crypto/market scopes. Comparison and sector analysis retain their specialized repair path. All entity-first, server-prefix, evidence, deferred-output, execute-once, and terminal-stream decisions remain active.
- Incident evidence: Production RMBS and ISRG turns already had exact same-symbol FMP quotes, but an incomplete first synthesis triggered a second roughly 60-second model attempt and failed after about 120–125 seconds. FMP/DataFetch and Tavily probes were healthy; the latency and false availability wording were internal synthesis/validation failures.
- Deterministic fallback: After a runner returns success but its sanitized answer fails the investment contract, supported scopes render a response only from the prepared server contract. The result is sanitized and validated by the same gate before delivery. It never reuses rejected prose, clears that draft's tool-call and normalized transcript metadata, and emits one final answer. A runner failure remains a failure, and any persistent or uncertain side effect remains fail-closed rather than being hidden behind an investment summary.
- Template scope: Deep equity, fund, and crypto fallbacks preserve the established nine-section asset-specific templates. Broad market preserves its five-section template. Quote-only turns return the server time, exact normalized entity, verified quote, and quote-source timestamp without inventing analysis. Unsupported comparison/sector shapes do not receive a generic fallback.
- Extended-hours path: `data_fetch extended_hours` exposes one bounded normalized minute bar. Explicit US premarket/postmarket questions use it only after exact entity resolution and a standard quote, and only when the leaf carries the exact symbol, the requested `pre` or `post` session matches its New York timestamp, and the timestamp is no more than 45 minutes old. The 16:00 close remains regular; only 16:01–20:00 is postmarket, and only 04:00–09:29 is premarket. Every factual body claim is reverse-checked against the exact entity, session, price, and any stated currency even when the session marker follows the number or modifiers intervene. A failed match retains the regular quote with an explicit disclosure and may never relabel it as a live extended-hours price. Crypto/night-session requests do not enter this US-equity contract.
- Numeric and event boundaries: Common aliases such as “目前价 / 现在价 / 市价 / 市场价 / market price” are current-price claims and cannot coexist with a conflicting quote. Historical/open/close/high/low prices fail closed until the contract has a dedicated verified historical-price field; dates, source domains, inference labels, and coincidentally equal current values cannot authenticate them. Markdown historical/OHLC headers carry their semantics into every numeric row; target/scenario tables and same-symbol current-price tables without date, historical, or OHLC semantics remain valid. Event inference/condition subheadings govern only their following Markdown list items and reset before prose or unknown headings.
- Operations: Automatic retry, context-overflow recovery, deterministic fallback, and contract repair require every observed call to be explicitly known read-only; an unknown tool fails closed. `deep_research` is always persistent, while bounded actions such as `portfolio view`, `cron_job list`, and `notification_prefs get/get_overview` are read-only. English and Chinese persistent-operation-plus-analysis requests are execute-once. A pre-run no-trace classifier also covers portfolio mutations, completed trades, and research starts without classifying ordinary advice questions as mutations. The safe fallback never justifies replaying a user operation or retaining rejected runner tool metadata.
- Verification: CI-safe tests cover exact lowercase tickers with price/extended aliases, US session boundaries, exact symbol/session/freshness, canonical extended/fallback labels, reverse validation of extended-hours body claims, unsupported historical prices and Markdown OHLC rows, event heading scope, same-gate deterministic outputs for quote/equity/fund/crypto/market, rejected draft metadata clearing, explicit read-only allowlisting, unknown/persistent tool fail-closed behavior, no-trace execute-once classification, and single-attempt fallback behavior.

## D-2026-07-17-03 Preserve Explicit Tickers Across Entity Resolution

- Status: Accepted
- Superseded in part by: `D-2026-07-18-01` replaces auxiliary completion for incomplete comparisons with main-agent tool discovery; exact ticker query integrity and deep-intent routing remain active.
- Clarifies: The deterministic bare-ticker and exact DataFetch matching requirements in `D-2026-07-16-01` and `D-2026-07-17-01`; all multi-entity completeness, same-symbol quote, asset-routing, answer-template, and fail-closed requirements remain active.
- Incident evidence: Three production RKLB turns failed before answer generation even though FMP/DataFetch search, quote, profile, and financial endpoints were healthy. Two turns spent up to the auxiliary entity-extraction timeout because words such as “推荐 / 安全区间 / 中子” remained after the local ticker residual grammar; an identity-binding turn (`RKLB 是前面提到的 火箭实验室`) was not accepted as ticker context. On the one turn where auxiliary extraction returned, its company-name query could replace `RKLB`; FMP exact search succeeded for `RKLB` while several alias/composite strings returned semantic-empty results.
- Decision: A ticker lexically present in the current interactive turn is a first-class identifier. A single-security ticker request proceeds to current-turn exact-symbol DataFetch even when unrelated business prose is not consumed by the local grammar, and explicit identity bindings in either case are accepted. Requests that explicitly signal an incomplete comparison still require auxiliary completion so a named peer cannot be silently dropped.
- Query integrity and fallback: DataFetch search for an explicit ticker always uses the exact symbol, never an auxiliary company alias. If that search succeeds but contains no same-symbol candidate, the guard may make one bounded exact-symbol profile request and accept it only through the same exact matcher. Provider/search errors do not trigger this semantic fallback, derivative symbols are not accepted, and a positive same-symbol quote remains mandatory before current-price or company analysis.
- Response-contract follow-up: The first post-restart RKLB probes proved exact entity and quote resolution within about two seconds, but the two “推荐的安全区间价格” turns were incorrectly treated as quote-only because the deep-intent vocabulary did not include safe-range or recommendation language. They consequently bypassed the nine-section deep valuation validator and exposed unverified price-range prose. Safe-range, margin-of-safety, fair-value, recommended-price, buy/entry-range, and equivalent decision wording now enters the deep contract before quote-only classification; production acceptance requires the numbered template and the same evidence validator/fallback as any other deep equity analysis.
- Verification: Unit coverage includes the three observed RKLB phrases, lowercase identity binding, acronym-denylist boundaries, incomplete named comparisons, auxiliary-alias merge behavior, derivative-only search, exact/wrong profile results, and AgentSession proof that explicit RKLB phrases reach DataFetch with zero auxiliary chat calls. The live regression probes RKLB search, quote, profile, and financials alongside RMBS, NBIS, INTL, BTCUSD, and mixed-market symbols.

## D-2026-07-17-04 Resolve Securities Through A Span-Aware Exact-First Pipeline

- Status: Accepted
- Superseded in part by: `D-2026-07-18-01` replaces pre-run auxiliary completeness with additive main-agent tool discovery; the span-aware lexical layer, exact-first provider plan, same-symbol quote, canonicalization, and typed failure semantics remain active.
- Supersedes: The explicit-ticker search-first/profile-fallback sequence in `D-2026-07-17-03`.
- Incident evidence: RKLB was not an isolated outage. Production logs showed a digit-leading `.SH` code degrading to the unrelated US ticker `SH`, while recent NBIS/RMBS/INTL/HONE failures and long pre-answer waits occurred despite healthy FMP search, quote, profile, financial, fund, index, and crypto endpoints. Review also reproduced architecture-level collisions: numeric target prices/years/cadence becoming securities, `$500` becoming a cashtag ticker, one whole-query market hint leaking across multiple numeric codes, and same raw numeric code being deduplicated before index/stock context was resolved.
- Layering: `security_identifier.rs` owns lexical spans and the closed provider dialect registry. The investment guard binds each candidate to typed provenance plus source span, identifier kind, and per-mention numeric market/asset hints. Provider resolution produces canonical symbols; answer preparation and portfolio membership consume only those canonical symbols. URLs, email, source paths, unsupported composite suffixes, technical concepts, scheduler conditions, monetary values, years, percentages, target prices, and cadence values are excluded before provider resolution.
- Exact-first provider plan: Explicit alphanumeric/composite codes generate audited provider variants, while bare numeric codes generate a closed market candidate set. All candidates are exact-quoted in bounded batches; a unique canonical match resolves, multiple matches clarify, successful empty coverage reports no exact coverage, and provider failure reports temporary provider unavailability. Ranked search order never proves an explicit code. Named companies and truly tentative code/name collisions use concurrent semantic search. Verified probe quotes are reused and required profile enrichment runs concurrently, replacing the serial search/profile/quote waterfall.
- Numeric boundary: Numeric market and asset hints bind only within the owning comparison fragment and immediately adjacent exchange/asset grammar. `港股700和日股7203` therefore yields two independent plans; `600519的日本业务` does not turn “日本业务” into a Tokyo exchange hint; `上证指数000001和股票000001` keeps both mentions until canonical resolution. Leading-zero equivalence exists only inside a named exchange rule such as Hong Kong normalization, never across markets.
- Ambiguity and failure semantics: A lowercase/TitleCase token that can be both a ticker and a company name is reconciled against the same search evidence, but the relation is typed rather than based on arbitrary substring score. Strong full-name or word-bounded name-prefix matches can still produce a real clarification such as Ford Motor (`F`) versus ticker `FORD`; partial words such as Apple/Appleseed cannot. A different symbol whose ETF/ETN/leveraged/yield/long-short product name merely embeds the requested ticker (for example CWY containing `CRWV`, or RKLX containing `RKLB`) is an underlying reference, not a company alias, and cannot challenge an exact quote. Semantic candidates below the strong-name threshold are unresolved rather than arbitrary ambiguities. Provider error objects, empty exact coverage, genuine ambiguity, and stale/missing timestamps remain distinct. None may be surfaced as “entity recognition incomplete” or as evidence that the security does not exist.
- Migration and rollback: No database or durable-data migration is required. Rollback restores the prior guard/DataFetch revision and rebuilds/restarts the runtime; actor sessions, portfolios, and scheduled records do not need transformation.
- Verification: Regression coverage includes RKLB/NBIS/RMBS/INTL/HONE/CRWV casing and punctuation; CRWV/RKLB/AAPL embedded-ticker derivative products; Ford/FORD genuine name conflict and Apple/Appleseed word-boundary fallback; BRK classes, A/H/JP/KR symbols, selected index aliases, crypto pairs; numeric ambiguity and per-span hints; names such as Rocket Lab, AT&T, and Johnson & Johnson; URL/path and acronym negatives; scheduled-condition/cadence negatives; provider URL encoding; live exact search/profile/quote/financial/fund/index/crypto probes; full channel tests; and production restart/health/E2E evidence.

## D-2026-07-18-01 Keep Entity Discovery Inside The Main Agent Tool Loop

- Status: Accepted
- Superseded in part by: `D-2026-07-18-02` removes every Interactive post-run publication action from Agent-discovered investment reconstruction. Main-loop discovery, current-query tool evidence, and typed scheduled/heartbeat contracts remain active.
- Supersedes: The blocking auxiliary-LLM extraction, 15-second timeout, `NeedsClarification` pre-run outcome, phrase-bound interactive completeness rules, scan-incomplete fail-closed behavior, and service-owned interactive intent/format routing in `D-2026-07-17-01` and `D-2026-07-17-03`. It does not weaken exact-symbol provider matching, server-owned verified facts, current-turn evidence requirements, or the prompt's established answer patterns. Typed scheduled/heartbeat contracts may continue to enforce deterministic formats; open-ended interactive turns may not.
- Incident evidence: The production turn `分析下crwv和nbis的估值` contained two ordinary lowercase ticker spans that the scanner correctly found. Residual-language deletion left the single character `下`, and a comparison-binding rule treated `和` as proof that the entity set was incomplete even though both sides were captured. The request then waited on the auxiliary entity LLM for about 15 seconds and returned “证券实体解析暂时未能确认” before `agent.run` or any DataFetch call. Same-window NBIS, NVDA, RKLB, and TSLA failures showed that the auxiliary extractor was a shared pre-tool single point of failure. Credentialed DataFetch checks concurrently proved exact fresh CRWV and NBIS results, including one `CRWV,NBIS` batch quote.
- Layering: Lexical scanning is a bounded candidate-seed layer, not an intent parser. For every nonempty interactive turn—including portfolio/watchlist wording—`AgentToolDiscovery` passes those non-factual seeds plus the complete current query to the configured main runner; neither an exact-ticker count, connector, residual word, portfolio phrase, company-name pattern, nor broad-market phrase can close or reject the interactive entity scope before the Agent runs. The Agent calls `portfolio(view)` itself when membership matters. Scheduled and heartbeat turns retain deterministic `Securities` / `Portfolio` / `Broad` preparation under their typed task contract.
- Agent-loop contract: The main Agent reads the complete query and decides which entities, evidence depth, tools, priorities, and answer shape are relevant without emitting or conforming to a closed intent taxonomy. If securities are named, it searches every candidate with current-turn `data_fetch(search)`, including explicit ticker codes; after results enter context it exact-verifies every selected canonical symbol with quote/profile and continues with whichever financials, holdings, news, web, earnings, sector, or extended-hours tools it judges necessary. Search is an iterative Agent-loop operation: an empty broad or enriched query may be followed by an exact-symbol refinement. The service therefore reconstructs its optional dynamic contract from all current-run search calls and their exact provider rows, never freezes the first assistant search group, and ignores earlier empty attempts when later exact evidence succeeds. Strong scope includes exact ticker seeds plus uniquely quote-verified name/alias searches whose query is directly grounded in the user's text. A verified search that is neither a seed nor text-grounded could be a translated alias or an auxiliary comparable; without structured scope provenance the service returns no optional contract rather than silently publishing a subset. Clearly embedded reference products such as CWY for CRWV may be ignored without expanding scope. The service never infers a required tool set from words such as “估值”, “最近”, “安全区间”, or any finite phrase list. High-confidence explicit code seeds remain a completeness minimum, not a closed entity list. A positive same-symbol quote with usable provider timestamp is mandatory to add a security to the dynamic verified contract. Market/sector scope likewise comes from Agent-selected structured tools and verified benchmark types. Wrong-symbol quotes, stale timestamps, search ordering, scanner seeds alone, or historical context cannot establish current facts.
- Clarification boundary: As superseded by `D-2026-07-18-02`, Interactive reconstruction is observation only. Coverage gaps, provider failures, ambiguity, omitted seeds, or missing quote/time may be logged but cannot trigger a hidden continuation, change `success=true`, or alter the Agent body. The Agent owns any concrete no-coverage, ambiguity, missing-data, or qualitative answer from its current tool context. Typed scheduled/heartbeat contracts, runner/auth/quota failures, and persistent-side-effect safety retain their separate fail-closed semantics.
- Output boundary: As superseded by `D-2026-07-18-02`, an Agent-discovered Interactive structure cannot prepend a provider-time/canonical-fact block, normalize text, validate/reject prose, or launch a repair. The successful Agent body is the only Interactive publication candidate. `DeferredUserOutputEmitter` keeps draft-capable deltas/resets and attempt-local errors hidden; `D-2026-07-18-03` later permits only the complete canonical first line from an Agent-signaled tool-free terminal phase to cross early, without weakening the one-answer boundary. Caller-supplied typed scheduled/heartbeat contracts keep their server fact prefix, strict formatter, validation, and safe repair/fallback path.
- Structured-claim boundary: Exact per-claim checking for derived valuation metrics, financial statement facts, holdings, or dated events cannot be made reliable by scanning unconstrained Markdown headings and clauses. If interactive output later needs that enforcement, the Agent/tool protocol must emit structured claim provenance that binds each claim to a tool result; the typed scheduled/heartbeat formatter and its strict field-aware validator remain unchanged in the meantime.
- Production follow-up evidence: The first post-deployment replay of `分析下crwv和nbis的估值` proved the Agent loop and DataFetch path were healthy but was rejected by legacy free-prose parsers inherited from the typed formatter. After commit `8d4fcdd6` removed that output interference, a fresh production replay independently searched, quoted, profiled, loaded financials, and loaded news for both `CRWV` and `NBIS`, logged `contract_built=true entities=CRWV,NBIS`, and completed in 78.405 seconds with one time-first answer, one successful terminal event, no reset/error, exactly one persisted user/assistant pair, and zero remaining active chats. This verifies that arbitrary prose parsing was outside the correct Interactive boundary; it was never an FMP/DataFetch outage.
- Regression incident: Commit `2d6b4be8` also introduced an unrequested `UnsafeIncomplete` publication veto and a first-search-group reconstruction assumption. In the 20:06 production replay, retry search calls for `CRWV CoreWeave` and `NBIS Nebius` returned empty, later exact `CRWV`/`NBIS` searches plus quote/profile/financials/web all succeeded, and the model generated a 6037-character valuation answer. The reconstructor ignored the later rounds, returned no contract, and the veto overwrote that successful body with the fixed refusal. The repair removes the veto and disposition entirely, aggregates current-run search refinements, and regression-gates both the exact production sequence and contract-failure answer preservation. This was a local runtime regression, not an FMP/DataFetch outage or a prompt/configured ban.
- Final acceptance: `fcca5a35` and `54b14068` passed 615 channel tests, full workspace check/test, 265 Web tests, and all CI-safe regressions. After a graceful zero-active-chat restart, fresh actors replayed both `分析下crwv和nbis的估值` and `crwv和nbis的估值怎么看`; each built `CRWV,NBIS`, used verified `73.21`/`177.71`, emitted one answer and one successful terminal event without reset/error/refusal, persisted exactly one user/assistant pair, and returned active chats to zero.
- Prompt contract: `soul.md`, the runtime finance policy, and the canonical `stock_research` skill all describe the same main-agent-loop flow. They treat ordinary tickers as first-class input, require all named securities to be independently resolved, and prohibit claiming that current data is unavailable after a same-symbol quote succeeds. This is a behavioral architecture rule, not a list of accepted question templates.
- Migration and rollback: No database, session, portfolio, quota, or other durable-data migration is required. Rollback restores the prior guard/prompt revision and rebuilds/restarts the runtime, but would reintroduce the observed pre-tool timeout and is therefore not a safe operational workaround.
- Verification: Unit coverage proves a CRWV request cannot be satisfied by quoting only the CWY embedded-product result, an explicit CRWV+NBIS query cannot silently omit NBIS, a complete two-entity trace preserves provider timestamps, and partial quote coverage fails. Dynamic-contract tests use deliberately unmodeled wording and prove that financial, news, and web evidence is recorded from the observed tool trace without selecting a presentation route. AgentSession coverage proves arbitrary surrounding wording, Chinese company names, the exact production phrase, one visible answer, Agent-owned no-coverage/ambiguity explanations, and repair trace safety with zero auxiliary chat calls. The finance CI contract forbids restoring the auxiliary timeout/failure helper, interactive wording classification, fixed-format validator, deterministic whole-answer fallback, or arbitrary-prose financial/event validators. Credentialed live regression requires exact fresh CRWV and NBIS records in one batch quote; production acceptance requires `agent.run start`, tool-backed `contract_built=true entities=CRWV,NBIS`, one time-first answer, one terminal event, and zero remaining active chats.

## D-2026-07-18-02 Keep Interactive Observations Out Of The Publication Path

- Status: Accepted
- Superseded in part by: `D-2026-07-18-03` replaces the temporary whole-body defer for eligible Interactive Web function-calling turns with a narrowly committed tool-free terminal prefix. Observational-only publication control, bounded runtime context, and typed scheduled/heartbeat separation remain active.
- Supersedes: The Interactive dynamic-prefix, deterministic truth-check, omitted-seed continuation, answer-repair, and publication-rejection portions of `D-2026-07-18-01`, plus any earlier rule that treats an Agent-discovered structure as an `InvestmentResponseContract`. It does not change the strict caller-supplied contracts used by scheduled and heartbeat turns.
- Incident evidence: At 21:05 Beijing time, the current production runtime—not an old binary—received `crwv和英伟达什么关系，估值怎么看`. DataFetch remained healthy and returned the verified CRWV quote `73.21`; the incident spent about 123.3 seconds primarily in model generation and post-run repair, not provider I/O. The first model draft was polluted by earlier CRWV+NBIS conversation state, opened as a CRWV/NBIS comparison, and mentioned both exact `73.21` and rounded `73`. Dynamic reconstruction observed CRWV but not the Chinese NVIDIA mention, the post-run checker rejected that draft, a second roughly 37.6-second repair still failed, and the backend replaced the answer with “投研完整性检查” failure copy. The transient error styling seen before history restore was a consequence of that backend terminal failure, not proof of a second frontend run.
- Context root cause: The affected session had more than 650 persisted rows; the runner request restored 653 messages totaling about 288,823 characters, including many scheduler/heartbeat pairs, failed investment turns, and stale NBIS context. Web routes had explicitly disabled the normal direct-message restore limit. The globally configured runner was `codex_acp`, so compaction logic assumed runner-owned context, while this non-admin Web actor actually executed through the strict in-process function-calling fallback. Model-context policy must follow the runner selected for the actor and must never allow durable automation/failure history to drown the current query.
- Decision: The main Agent receives the complete current query, resolves all named instruments through its own tool loop, loads the chosen evidence into that same context, and writes the complete time-first answer once. After it returns, Agent-discovered investment reconstruction is telemetry only. Regardless of whether the observation is complete, partial, ambiguous, or absent, the service must return a successful Interactive body byte-for-byte and must not prefix, normalize, retry the whole run, append a hidden seed continuation, repair, rewrite, reject, or convert success to failure. Observation logs may state `contract_built=true/false`, but they confer no publication authority.
- Typed boundary: Scheduled and heartbeat execution keeps explicit typed `InvestmentResponseContract` preparation, deterministic server-owned time/entity/quote fields, strict field-aware validation, and its safe fail-closed repair/fallback behavior. Those contracts are created before/for a typed task and are not inferred from an open-ended Interactive answer trace. Runner/auth/quota failures and persistent-side-effect safety also remain real terminal errors.
- Runtime-context boundary: Web direct restore is bounded. Before an Interactive runner request is assembled, complete historical scheduler/heartbeat/job turn groups and complete prior `run_failed=true` groups are pruned from model context; durable session rows, public bootstrap/history pagination, push records, and audit evidence remain untouched. Current-query priority is therefore enforced at the actual model-input boundary rather than by deleting user history or adding a phrase classifier.
- Output and latency: The correctness repair kept `DeferredUserOutputEmitter` around the whole body so draft deltas, resets, and attempt-local errors could not create a second-looking response. Production acceptance proved the boundary correct but left first visible body text late. `D-2026-07-18-03` keeps tool-capable rounds deferred and permits only one complete canonical line from an explicitly tool-free terminal phase to cross early.
- Migration and rollback: No database, session, portfolio, quota, scheduler, or other durable-data migration is required. Runtime pruning is read-only and request-local. Rolling back would reintroduce both unbounded stale context and the unrequested Interactive publication gate, so it is not a safe incident workaround.
- Verification: Regression must reproduce the exact CRWV/NVIDIA wording with stale NBIS tool trace and the `73.21`/rounded-`73` draft, prove one runner call and byte-for-byte success with no fixed failure copy, and prove Interactive history pruning removes automation/failed groups without mutating the durable transcript. CI must reject restored Web `restore_max_messages(None)` overrides and any Interactive reference to dynamic truth-retry/repair helpers. Production acceptance requires a fresh exact-query replay that independently resolves CRWV and NVDA, emits one successful answer/terminal with no reset/error/fixed refusal, leaves one user/assistant pair, and returns active chats to zero; full gate, deployment, and health evidence belongs in the same-day handoff.

## D-2026-07-18-03 Use An Agent-Signaled Tool-Free Terminal Stream

- Status: Historical; its Interactive `finish_research` / tool-free-terminal production path is superseded by `D-2026-07-19-08`. Tool-round buffering, provider lifecycle validation, one-output publication, and exact visible/persisted bytes remain active.
- Superseded in part by: `docs/adr/0004-agent-owned-research-loop.md`, refined by `D-2026-07-19-03`, keeps `finish_research` in the same business-tool Agent loop and exposes it only after the request-local structural entity-route floor. That floor controls the explicit finish schema, not DirectFinal or publication: every complete `Stop + Done` body remains the same Agent's DirectFinal without retry, rejection, terminal/second generation. ADR 0004 removes every isolated-control or implicit-error finish path while carrying forward explicit provider lifecycle/fallback telemetry, one buffered empty-tools terminal recovery, the same-Agent evidence context, speculative-output boundary, exact visible/persisted body, and the ban on post-run Interactive publication review. ADR 0003 is historical and superseded.
- Supersedes: The whole-body Interactive defer portion of `D-2026-07-16-02` and `D-2026-07-18-02` only for eligible Web function-calling turns. It does not supersede their no-flicker, one-answer, terminal uniqueness, observational-only Interactive reconstruction, typed scheduled/heartbeat, persistent-side-effect, or failure boundaries.
- Correctness baseline: Commits `25090e88` and `8df08239` passed 620 channel tests, workspace check/test, 265 Web tests, and all CI-safe regressions, then were rebuilt and gracefully deployed. A fresh production replay of `crwv和英伟达什么关系，估值怎么看` independently researched CRWV and NVDA, began with the Agent-owned Beijing data-time/quote-basis line, emitted one answer and one successful terminal with no reset/error/fixed refusal, persisted exactly one user/assistant pair, and returned active chats to zero. Its roughly 67-second total duration establishes the pre-optimization baseline; it is not evidence that this latency decision has passed production acceptance.
- Problem: Ordinary function-calling content is speculative until the model either returns a direct answer or finishes all subsequent tool branches. A round may stream prose and later emit a tool call, requiring a reset. Forwarding those deltas directly would trade late first text for visible preambles, resets, or apparent second responses; retaining a whole-body defer hides safe terminal synthesis long after the last research tool finishes.
- Decision: Give the same in-process function-calling Agent an internal `finish_research` control tool only under a narrow terminal-stream policy. When the Agent emits that control as the sole tool call after loading all required evidence, the same `Agent::run` and in-memory context enter one native streamed terminal completion with an empty tool set. This is a control transition, not a second service repair or a separately reconstructed answer. The terminal prompt may only ask the Agent to synthesize from the same user request and accumulated tool results; the service still has no authority to compose, reject, or rewrite Interactive investment prose.
- Commit boundary: Tool-capable rounds remain ordinary speculative deltas and stay behind `DeferredUserOutputEmitter`. Terminal deltas are buffered until the runner has one complete sanitized line matching `数据时间：北京时间 YYYY-MM-DD HH:MM；行情口径：...\n`, within bounded length. Only that exact line is emitted as an internal `CommittedStreamDelta`, converted back to the normal user-facing stream event, and remembered byte-for-byte. All later terminal deltas remain deferred. After response finalization and attachment handling, `AgentSession` verifies that the final body still begins with the committed bytes and emits only its exact suffix; visible prefix plus suffix therefore equals the persisted assistant body.
- Irreversibility: A committed prefix is a user-visible point of no return. Transient failure retry, empty-success retry, context-overflow recovery, `StreamReset`, and request replay are forbidden afterward. If finalization no longer preserves the exact prefix, the run fails terminally; it may not reset, rewrite, or replay already-visible content. This decision never treats the prefix as validation or publication permission for the rest of the answer.
- Fallback behavior: Under the superseding ADR 0004 contract, every complete nonempty `Stop + Done` body is the same Agent's DirectFinal, regardless of advisory-ledger coverage. It keeps the deferred one-shot path and incurs no retry, publication veto, terminal completion, or second model generation. If `finish_research` appears alongside any business tool, or its arguments are malformed/nonempty, the control call is ignored while real tools execute normally; it is not persisted as a business tool, sent to the actor registry, counted against tool budgets, or exposed through tool progress. Multiple non-sole finish calls also do not enter terminal mode.
- Scope: Enable the policy for Interactive Web-identity turns executed by the strict in-process function-calling runner, with Agent discovery present, no caller-supplied typed investment contract, and reexecution allowed; this includes the OpenAI-compatible public API because it uses the same Web actor identity and must not retain unsafe tool-round drafts. Browser SSE may represent an already-committed double-failure prefix as an explicit partial terminal, while OpenAI streaming/non-streaming remains a failed API result. ACP/CLI runners, other channels, scheduled/heartbeat typed contracts, and execute-once/persistent mutations retain existing output semantics.
- Provider contract: An empty-tools terminal stream must omit `tools`, `tool_choice`, and `parallel_tool_calls` even when generic request options or `extra_body` supplied them, while preserving model, reasoning, token, temperature, audit, and SSE behavior. OpenRouter and generic OpenAI-compatible adapters own this normalization. Generic compatible endpoints may omit literal `[DONE]`; only their adapter may convert error-free clean EOF after exactly one typed finish into one internal DONE, and it must verify raw SSE framing plus reject post-finish content/reasoning/tool payload. EOF without finish, residual framing, duplicate finish, parser/transport error, and the dedicated OpenRouter missing-sentinel path remain incomplete. Any terminal tool-call fragment is a protocol failure, never a reason to execute another tool.
- Migration and rollback: No durable-data migration is required. The control tool, terminal policy, committed event, and remembered prefix are request-local. The safe rollback is to disable the terminal policy and return to whole-body defer; it must not expose ordinary speculative deltas or restore post-run Interactive publication control.
- Verification: Focused tests must cover sole/mixed/direct control behavior, same-context accumulated tool evidence, empty-tools wire payloads, fragmented/invalid/oversized canonical lines, deferred terminal tail, no retry/recovery after commit, prefix/final mismatch, and exact visible/persisted equality. Full repository gates, a graceful zero-active-chat rebuild/restart, health checks, and a fresh exact CRWV/NVIDIA production replay must record first committed `assistant_delta` timing separately from `run_finished`, plus entity/tool coverage, one terminal, no reset/error/refusal, exact two-message history, and zero active chats. Until that production evidence exists, no final latency improvement is claimed.

## D-2026-07-19-01 Keep Research And Finish Ownership In One Agent Loop

- Status: Superseded in production by `D-2026-07-19-08`; one-Agent/current-context evidence ownership remains active, while `finish_research` and terminal synthesis do not.
- Refined by: `D-2026-07-19-02` gives DirectFinal and explicit finish one exact last-mile contract; `D-2026-07-19-05` replaces the empty finish object with a tolerant current-turn provenance handoff. Final review in `docs/adr/0004-agent-owned-research-loop.md` makes entity/quote/asset-route coverage advisory and prohibits using either route state or handoff resolution to veto a complete natural answer. Agent-loop ownership and the prohibition on post-run Interactive semantic review remain unchanged.
- Supersedes: `docs/adr/0003-isolated-agent-research-control.md` and every isolated `continue_research` / `finish_research` completion or control-error-to-terminal transition. It also replaces the sole/mixed routing details in `D-2026-07-18-03`; that decision's same-context evidence ownership, speculative tool-round boundary, tool-free terminal, committed-prefix transport recovery, exact visible/persisted body, and observational-only Interactive publication rule remain active.
- Incident evidence: After the MiniMax clean-EOF adapter repair was fully gated and deployed, a fresh production replay of `crwv和英伟达有什么关系` completed as one clean stream without a reset, second answer, fixed refusal, or missing-DONE error. Its business trace contained only `data_fetch(search)` for CRWV and NVIDIA. The separate control phase then produced an error-labelled implicit finish, and terminal synthesis asserted relationship, concentration, and infrastructure facts that no current-turn relationship source supported. This was a reasoning-protocol failure, not stale deployment, DataFetch/FMP outage, or transport failure.
- Agent-loop ownership: Active finance research uses one `Agent::run`, one context, and one model role. Business tools remain available in every active completion. Once structurally eligible, the internal `finish_research` schema appears alongside those tools; there is no separate `continue_research` model request and no service-authored implicit finish.
- Structural evidence ledger: `data_fetch(search)` is `identity_only`; the ledger separately records post-search quote/profile, crypto, broad-market, Web, and other attempts. This state is prompt guidance and telemetry, not finish or publication authority. Genuine no-coverage is recorded so the Agent can disclose it, while malformed and budget-rejected calls do not pretend that a provider was attempted.
- Sole explicit finish: Exactly one sole `finish_research` call whose arguments parse as an empty JSON object is the only path into empty-tools terminal synthesis. Mixed finish plus business calls executes the business calls; duplicate, malformed, nonempty-argument, or unavailable finish signals are ignored and never enter the actor registry, tool budget, observer, progress stream, or persistence.
- Agent-owned DirectFinal: Any complete nonempty natural-language body ending with provider `Stop + Done` in an active business completion is the same Agent's DirectFinal even when the advisory ledger is incomplete. It publishes once through the deferred Interactive path and triggers no retry, semantic veto, replacement, empty-tools terminal synthesis, or second model generation. Empty output, provider error, timeout, lifecycle error, or iteration exhaustion remain genuine failed runs and can never synthesize finish authority or DirectFinal success.
- Evidence and language boundary: The complete query drives Agent-selected evidence; no ticker list, phrase grammar, or service-side intent template may substitute for that loop. A relationship question requires exact quote/profile evidence for every named entity plus current Web/news/filing evidence for the relationship; a valuation question requires the actual inputs and periods used by its valuation frame. “Not found in this run” is not proof of nonexistence, so negative claims about ownership, customers, contracts, or other relationships require explicit evidence or must be phrased only as a bounded search result.
- Publication boundary: Interactive post-run reconstruction remains observational only. Completed Interactive bodies use a dedicated security-only finalizer that may remove system/internal protocol material, redact unsafe local paths, and stabilize local media references, but it may not rewrite market copy, veto planning-like prose, reconstruct an answer from tool outcomes, or invoke the scheduled/heartbeat completeness guard. The exact finalized body emitted to the user is persisted byte-for-byte. The service owns structural stream/session safety but may not semantically approve, prepend, normalize, retry, repair, reject, replace, or append a fixed “research incomplete/cannot safely publish” answer. Typed scheduled/heartbeat contracts keep their separate deterministic validation and fail-closed path.
- Provider and terminal boundary: The strict requested/effective tool-choice telemetry, typed finish plus internal DONE validation, adapter-local MiniMax clean-EOF normalization, current-run-only terminal evidence, canonical first-line commit, exact suffix publication, one buffered terminal-only recovery, and honest partial failure remain unchanged.
- Migration and rollback: No durable-data migration is required. The ledger, completion-signal availability, and terminal transition are request-local. A safe fallback may disable early terminal streaming and return to whole-body defer, but it must not restore isolated control, implicit finish, speculative tool-round publication, evidence-ledger publication veto, or post-run semantic review.
- Verification: Focused regressions must prove the ledger distinguishes search-only, post-search quote/profile, crypto, broad-market, and Web-only routes without rejecting a complete DirectFinal; business tools and eligible finish share one completion; only a valid sole `finish_research({})` enters terminal; every complete DirectFinal uses no retry, terminal/second generation, or replacement; malformed/mixed/provider-error paths do not authorize terminal; relationship prompts request quote/profile plus Web/news/filing evidence; and Interactive success bypasses semantic rewrite/refusal/tool-result reconstruction while visible and persisted bodies remain identical. Full gates, exact-commit build, zero-active-chat restart, health checks, and fresh CRWV/NVIDIA plus non-finance production canaries remain required. See `docs/adr/0004-agent-owned-research-loop.md`.

## D-2026-07-19-02 Share One Exact Last-Mile Evidence Contract Without A Publication Gate

- Status: Accepted; production acceptance is `in_progress`.
- Refines: `D-2026-07-19-01` and `docs/adr/0004-agent-owned-research-loop.md`. It does not restore any service-side Interactive response validator, rewrite, repair, refusal, or second generation.
- Refined by: `D-2026-07-19-04`; the exact terminal reminder is now owned by the normal tool-free completion, while DirectFinal remains only an unchanged provider compatibility/no-veto fallback.
- Superseded in production by: `D-2026-07-19-08` for the finish/terminal split. Its Session-time, quote-source-time, Web-snippet, and no-publication-gate constraints remain active in the natural DirectFinal prompt.
- Incident evidence: The first `0cd03abb` production replay produced one clean, byte-consistent answer in about 42.4 seconds with healthy DataFetch and Tavily calls, but no profile lookup. A Web snippet alone unlocked DirectFinal; the answer then used the quote-provider time as the query time and expanded snippets into unsupported customer-rank and supply-priority claims. The remaining defect was an asymmetric last mile, not stale deployment, frontend replay, or provider outage.
- Advisory evidence structure: After `data_fetch(search)`, the Agent prompt directs exact quote plus profile/asset-route attempts and the ledger records whether every batched quote symbol has matching profile/snapshot coverage. Crypto and broad-market paths have explicit diagnostic routes; genuine no-coverage is recorded and disclosed. This structure controls neither DirectFinal acceptance nor publication and adds no query phrase, company symbol, or relationship classifier.
- Shared final contract: DirectFinal and explicit terminal synthesis receive the same evidence-limited contract as the last ephemeral user message. Both get the exact literal first-line prefix generated for this turn. One live Beijing clock read supplies both `Session 上下文` and `answer_time_beijing`; quote provider timestamps belong only in `行情口径` and may never replace the Session query time.
- Evidence boundary: Post-DataFetch active requests start at the current-turn boundary, excluding stale prior ticker/price history. Assistant prose beside tool calls is blanked. Provider reasoning fields needed for MiniMax/Mimo tool-followup compatibility remain protocol context but are explicitly non-evidence; the tool-free terminal additionally strips them. The completed Interactive body still publishes and persists unchanged through the security-only finalizer.
- Source discipline: `web_search` accurately advertises basic, low-bandwidth results—at most three titles, URLs, and snippets, with no page body. Snippets and relationship labels may be used only literally and may not be expanded into unstated rights/duties, ranking, importance, exclusivity, guarantees, priority, concentration, or roles. The Agent should continue toward company announcements, filings, or other primary sources when those details matter.
- Verification: Regression must use distinct Session and quote timestamps; prove DirectFinal/terminal share the literal prefix and evidence contract; distinguish Web-only, quote-only, partial-profile, crypto, and broad-market traces without turning them into a DirectFinal veto; preserve provider reasoning replay for live tool follow-up; and prove one answer, no retry/terminal/second generation for DirectFinal, no reset/error/fixed refusal, and exact visible/persisted bytes. Full gates, exact-commit build, zero-active-chat restart, health checks, and fresh relationship/valuation/non-finance production canaries remain required.

## D-2026-07-19-03 Require Per-Entity Route Coverage Before Explicit Finish

- Status: Accepted; local focused verification passed, final deployment and production acceptance are `in_progress`.
- Refines: `D-2026-07-19-01`, `D-2026-07-19-02`, and `docs/adr/0004-agent-owned-research-loop.md`. It does not alter the rule that every complete natural DirectFinal is accepted unchanged and it does not restore an Interactive publication validator, refusal, repair, rewrite, or second generation.
- Refined by: `D-2026-07-19-04`; active tool-enabled prompts no longer invite DirectFinal and the relationship deletion reminder is replaced by terminal sentence-level claim provenance, without changing the no-veto fallback in code.
- Refined by: `D-2026-07-19-08`; route coverage now guides ordinary `Required → Auto` business-tool progression before natural DirectFinal and no longer exposes an explicit finish schema.
- Incident evidence: Deployed `c7c5741f` completed `crwv和英伟达有什么关系` as one clean answer in about 48.54 seconds after healthy DataFetch/Web calls, but quote/profile coverage existed only for CRWV. The structural ledger examined symbols that had already been quoted instead of every successful identity search route, so it exposed `finish_research` while NVIDIA still lacked a quote/profile attempt. Terminal prose then asserted largest/one-of-largest, core-customer, no-equity, preferred-partner, chip-model, contract-size, and party-role details beyond the available snippets. This was a pre-finish evidence-routing and Agent synthesis defect, not an FMP outage or proof that server-side semantic interception is needed.
- Per-route identity floor: The same Agent that reads the complete query assigns every named security a stable, distinct, case-sensitive `entity_route`, gives every individual search a call-scoped `identity_match=exact_symbol|name_or_alias`, and reuses the route verbatim across refinement, quote, profile/snapshot, and later DataFetch calls. A malformed later search cannot borrow an older route's valid match declaration. Any parseable DataFetch call carrying an explicit route immediately registers that route as pending, so it cannot disappear behind another route's coverage; it cannot preload candidates, quote/profile, or post evidence before its own valid search and result. Before explicit finish is available, each nonempty route needs one same candidate symbol in both its route-bound quote and profile/asset-route evidence. Coverage of CRWV cannot stand in for NVDA; quote/profile for CWY cannot satisfy an exact CRWV route; and an untagged symbol is attributed only when it belongs to exactly one active route.
- No natural-language entity parser: The service does not split query text on commas, `and`, `和`, `&`, `/`, or share-class wording, does not infer match mode from case/length/ticker shape, and does not positionally assign leftover provider rows. Legal names and codes such as AT&T, S&P Global, M&T Bank, H&R Block, BRK/B, and `Berkshire Hathaway, Class B` therefore remain one Agent-understood entity unless the Agent declares more. The first Agent-declared exact-symbol search fixes a persistent same-code constraint; the shared audited `hone-core::provider_symbol` equivalence handles only bounded share-class, exchange-suffix, Hong Kong padding, index, crypto-pair, and cashtag dialects. A later name refinement cannot widen CRWV to an ETF/product whose name embeds that code. Agent-declared name mode uses word-boundary grounding so Ford/FORD, Apple/Appleseed, and Tesla/derivative results do not collapse. Legacy untagged searches remain provisional active routes until an explicit search names one exact, case-sensitive old text through the actual `query`, `refines_query`, or `supersedes_query`. `refines_query` and `supersedes_query` are strictly mutually exclusive; a dual-link or mistyped metadata call migrates nothing and leaves its declared route pending. A valid single-source migration never guesses aliases and carries only search/empty attempt history, verbatim aliases, and an already Agent-declared exact-symbol constraint; it never imports provisional candidates, quote symbols, profile/asset-route symbols, or post-identity attempts. A route empty after two current consecutive unsuccessful searches additionally needs a real route-correct attempt after the latest empty result before bounded gap disclosure, while an unscoped no-symbol call is attributable only when there is one active route. Agent omission or reuse of a route remains an explicit Agent trust boundary and never authorizes a post-generation refusal.
- Control boundary: The structural ledger controls only `finish_research` schema visibility and the transition from business-tool `Required` guidance to business-tools-plus-finish `Auto`. It is evaluated before explicit finish, not after answer generation. It is never consulted to accept, reject, retry, suppress, replace, or persist a complete `Stop + Done` DirectFinal, and `AgentSession` still publishes a successful Interactive body through the security-only finalizer without semantic review.
- Relationship deletion check: The final ephemeral prompt for evidence-pending DirectFinal, structurally eligible DirectFinal, and explicit terminal synthesis instructs the Agent to delete every relationship claim not directly stated by current-turn source text or snippets. Titles, generic cooperation wording, purchase facts, and model memory cannot establish rank, preferred/core-customer status, equity ownership or its absence, cross-holdings, exclusivity, guarantees/priority, concentration, chip models, contract values, or party roles. This is Agent last-mile self-review, never a service-side keyword filter, validator, rewrite, or fixed refusal.
- Verification: Focused verification after the final route-order and migration changes is in progress. The regression matrix covers CRWV-only-CWY wrong-product output, CRWV/NVIDIA overlapping/noisy result sets, call-scoped match declarations, Ford/FORD, Apple/Appleseed, Tesla/derivative, shared provider aliases, same-route Chinese-name refinement replacement, mixed explicit/implicit routes without global provisional-route retirement, exact-text `supersedes_query` migration, failed exact/name migration without candidate inheritance, route evidence arriving before that route's search, AT&T/S&P/M&T/H&R/BRK/B/Class B preservation without a query splitter, singleton-only unscoped empty-route attribution, same-symbol quote/profile intersection, and the shared relationship deletion reminder while retaining no-veto DirectFinal behavior. Full repository gates, exact-commit build, graceful restart, health checks, and fresh relationship/relationship-plus-valuation/non-finance production canaries remain required; no deployment or production acceptance is claimed by this decision.

## D-2026-07-19-04 Make Tool-Enabled Research Rounds Evidence-Only

- Status: Accepted; local verification passed, exact deployment and production acceptance are `in_progress`.
- Refines: `D-2026-07-19-02`, `D-2026-07-19-03`, and `docs/adr/0004-agent-owned-research-loop.md`. It adds no service-side answer parser, publication gate, rewrite, retry, or fixed refusal.
- Refined by: `D-2026-07-19-05`; evidence-only rounds now end with a tolerant structured provenance handoff rather than an empty finish object, without changing the DirectFinal no-veto fallback or publication boundary.
- Superseded in production by: `D-2026-07-19-08` for the finish-only handoff and separate terminal. Tool-round buffering, concrete route guidance, bounded Web-snippet semantics, and evidence-limited natural prose remain active.
- Incident evidence: Exact production build `ac2468f9` removed the old completeness refusal and streamed `crwv和英伟达有什么关系` once in 46.300993 seconds with one start/delta/successful finish, no reset/error, byte-identical history, and zero remaining active chats. FMP and Tavily were healthy. The Agent nevertheless stopped after CRWV search+quote, NVIDIA search, and one Tavily query; it skipped CRWV profile and both NVDA quote/profile, then returned a tool-round DirectFinal that expanded snippets into unsupported largest/core-customer, MFN-priority, chip-model, and Forward-PE claims. The remaining defect was contradictory Agent guidance and weak snippet provenance, not deployment staleness, FMP failure, or a reason to intercept the answer afterward.
- Evidence-only rounds: The initial Agent instruction requires rereading the complete user query and, when companies/securities are named, returning only a separate DataFetch search call for every named entity with its own stable `entity_route` and call-scoped `identity_match`. After DataFetch activates finance, a `Required` round with incomplete structural coverage returns only real business-tool calls. Once the floor is satisfied, the `Auto` round returns either more business-tool calls or the sole `finish_research({})`; it does not write data time, summary, draft, explanation, or final prose. The following no-tool completion is the normal single visible answer. A provider may still ignore this protocol and return a complete `Stop + Done` DirectFinal; code accepts it unchanged as a compatibility/no-veto fallback and never converts it into an error or second generation.
- Dynamic route guidance: The request-local ledger now renders concrete per-route missing work: an undeclared/provisional route, a missing call-scoped search, current candidates, missing same-candidate quote or profile/snapshot/crypto quote, a mismatched quote/profile pair, or bounded no-coverage attempts. Route keys and candidate arrays are JSON-escaped and internal prefixes are not echoed. The status explicitly describes calls as attempted; it does not certify provider success, entity-set completeness, or semantic evidence quality, so the Agent must read each actual tool result and the original query before finishing.
- Snippet provenance: Successful `web_search` output is locally capped again to three results and receives a trusted root `hone_search_contract` plus per-result `hone_evidence`. The envelope states basic snippet-only scope, no page body, same-result inline citation scope, non-citable missing URLs, and that search order/score is not real-world rank; locally written metadata overwrites provider-spoofed names. It remains context for the Agent, not an output validator.
- Terminal claim ledger: The normal no-tool terminal requires every external relationship fact to point to the same current result whose title/content/snippet directly states its numbers, direction, rank, role, rights/obligations, product model, or valuation label, with source title and original URL inline beside the fact. A URL only locates evidence. Any judgment beyond literal source text must be a separate sentence beginning `推断：`; unsupported details are deleted. Missing evidence narrows the answer and is disclosed, but never becomes a whole-answer refusal.
- Verification: Agent 74/74, hone-tools 143 passed with one optional `matplotlib` test ignored, hone-channels 642/642, finance contracts 39/39, Web 267/267, the full workspace check/test commands, formatting/diff checks, and the complete CI-safe runner pass. The first full workspace test run caught `soul.md` exceeding the reviewed 12,000-character prompt budget; wording was compressed without removing the full prompt contract, the focused budget test passed, and the full workspace rerun then passed. Exact commit/build/restart, health checks, and fresh relationship/valuation/non-finance production canaries remain required before production acceptance is claimed.

## D-2026-07-19-05 Carry Current-Turn Provenance In finish_research

- Status: Historical; the Interactive production handoff/terminal protocol is superseded by `D-2026-07-19-08`.
- Refines: `D-2026-07-19-01`, `D-2026-07-19-04`, and `docs/adr/0004-agent-owned-research-loop.md`. It replaces only the empty-object finish payload and raw-transcript terminal boundary. Same-Agent ownership, evidence-only tool rounds, the unchanged DirectFinal compatibility fallback, one-output streaming, security-only Interactive finalization, and the ban on post-run semantic review remain active.
- Refined by: `D-2026-07-19-07`; current-turn citable sources are now bound into the finish schema, and an unresolved source locator receives at most one hidden same-Agent correction before terminal synthesis instead of authorizing an unrestricted empty-evidence terminal.
- Incident evidence: Exact production build `3d2474b5` returned `crwv和英伟达有什么关系` as one clean run and one byte-identical persisted answer with no reset, error, fixed refusal, or second generation. FMP and Tavily were healthy. The Agent nevertheless issued an empty finish after one broad relationship search, and terminal synthesis expanded three basic snippets into unsupported customer rank, GPU models, bargaining power, and “no equity relationship”. The terminal had the raw tool transcript, so the defect was not missing context; an empty completion signal did not force the same Agent to identify which current-turn evidence it intended to use.
- Structured handoff: Once the structural route floor is satisfied, a logical finish-only `finish_research` round carries `answer_scope`, `facts`, `inferences`, and `gaps`. The prompt asks for one control call, but provider-duplicated calls in the same finish-only round are tolerated: the runtime selects the first parseable handoff. If none validate, the terminal may use only the first JSON-readable nonempty scope recovered from fixed `facts[*].evidence[*]` paths; syntax-broken or truncated controls contribute no evidence and never invoke another research-model round. Each fact contains only a stable id and current-turn evidence references—there is no free-text fact claim. A Web reference identifies the current `tool_call_id`, 1-based result, and verbatim excerpt; the runtime injects the matched result's title and exact original URL. A DataFetch reference identifies the current `tool_call_id` plus a non-root scalar RFC 6901 JSON Pointer. Inferences cite submitted fact ids; attempted but unresolved dimensions stay in gaps. An inference remains Agent-authored judgment rather than a service-certified fact.
- Tolerant mechanical resolution: The runtime resolves every reference independently by identity, index/pointer, exact text, URL, and bounded-size rules. A stale, fabricated, missing, or malformed individual reference is omitted or annotated; it cannot reject the whole handoff, send the Agent through another completion loop, surface an internal protocol error, or authorize a fixed refusal. Valid references and facts in the same submission remain usable. Compact fallback is confined to the Agent-selected locator: one Web result number or the exact direct DataFetch object around a selected scalar/missing leaf. `data_fetch(search)`, missing current-turn invocations, unselected same-turn tool results, adjacent batched entities or periods, root/array/null selections, and `error/errors/isError/*_error` paths cannot supply terminal evidence. Duplicated invalid controls never union scopes, and a gaps-only handoff imports nothing. Compact fallback is deterministic evidence transport, not relevance ranking, claim validation, or answer composition.
- Terminal isolation: The tool-free terminal is reconstructed from the current system contract, current user request, exact Session-time anchor, and resolved handoff evidence/inferences/gaps only. It receives no raw business-tool transcript, prior assistant/tool messages, earlier user evidence, discarded drafts, or provider reasoning. The terminal derives factual wording directly from resolved excerpts/fields. Missing evidence may narrow or qualify the answer; it must never become a service-owned whole-answer ban.
- Publication boundary: Hone performs protocol and source-location mechanics before terminal synthesis and security/path cleanup afterward. It never semantically approves a proposition, parses or scores final prose, retries because of answer content, rewrites a conclusion, appends a completeness message, or refuses an otherwise successful Interactive answer. A provider that ignores the tool-only protocol and returns complete `Stop + Done` prose remains an unchanged DirectFinal with no terminal, second generation, or veto.
- Control and failure boundary: Mixed finish plus real business calls continues only the real calls and ignores every accompanying finish control. A finish-only round with one or more provider-duplicated controls enters terminal once through its first parseable handoff or, when none validate, the first safely readable fixed-path evidence scope; it never scans or unions the whole current turn. These control calls never enter actor registry, tool budget, progress, observer, audit body, or persistence, and neither duplication nor malformed handoff starts another research loop. Genuine provider error, timeout, incomplete lifecycle, or iteration exhaustion remains a failed run; tolerant evidence resolution does not disguise those transport/runtime failures as success.
- Verification: Regressions cover partial success in one handoff; stale tool ids; wrong result indices, excerpts, and JSON Pointers; multiple valid and invalid references together; no free-text fact claim or model-supplied URL; runtime title/URL injection; inference references; selected-result/object fallback; exclusion of search candidates, missing invocations, unselected NBIS/CWY results, neighboring batched symbols/periods, arrays, nulls, roots, and error paths; syntax-broken empty scope; provider-duplicated controls without scope union or another research loop; mixed business/finish behavior; no-success gaps; terminal messages containing only the current system contract, current user request, and resolved/scoped-fallback evidence; absence of every older conversational turn, raw tool transcript, and reasoning; no invalid-reference loop or public error; DirectFinal no-veto behavior; and one exact visible/persisted body. Focused Agent tests pass 96/96 and finance contracts pass 39/39. Exact-commit build, zero-active-chat graceful restart, health checks, and fresh relationship/valuation/non-finance canaries remain required.
- Migration and rollback: No durable-data migration is required. The handoff and compact evidence are request-local. A safe rollback may disable early terminal streaming and defer the whole Agent body, but it must not restore the empty-object terminal handoff, raw-transcript evidence ambiguity, post-run semantic validation, answer rewriting, or fixed refusal.

## D-2026-07-19-06 Retry Only Idempotent Web Requests And Recover Chat Streams Before Error

- Status: Accepted; local implementation verification passed, exact deployment and production acceptance are `in_progress`.
- Refines: The public-chat run ownership and one-terminal rules in `docs/invariants.md` and `docs/repo-map.md`. It changes browser transport behavior only; backend run registration, quota, SSE wire events, and Agent answer semantics remain unchanged.
- Incident evidence: The shared Web `apiFetch` helper retried every method after a transient 408/502/503/504 response or transport exception. A retry of `POST /api/public/chat` could create a second submission after the first request had already reached the server. Separately, the chat catch path first changed the assistant card to error and only afterward called bootstrap/history recovery, producing the reported brief error flash even when the authoritative run was still active.
- Retry boundary: Automatic browser retry is permitted only for idempotent `GET` and `HEAD`. `POST /api/public/chat` and every other non-idempotent method are submitted exactly once at the browser helper layer, regardless of transient status or transport exception. Recovery observes server state; it never repeats the user operation.
- Stream recovery boundary: A non-Abort SSE interruption keeps the existing assistant card pending with a recovery status, then uses the normal bootstrap/history retry policy to discover the authoritative `active_run`, completed answer, or interrupted state. Only exhaustion of that recovery path renders an error. An explicit user abort/stop closes immediately and does not enter disconnect recovery.
- Run identity and time: Refresh and recovery retain the server-owned `run_id` and original start time. They must not create a new local thinking epoch, replay POST, synthesize a second answer, or accept frames after unique terminal `run_finished`.
- Restore/send ordering: Every bootstrap restore captures both a sync generation and the current local-send generation. A new send synchronously advances both fences, aborts the prior restore, and cancels its retry timer before appending the optimistic user/assistant pair. An older bootstrap therefore cannot reconcile history over the next turn and delete the assistant card before its first delta arrives.
- Verification: Frontend tests prove GET/HEAD transient and transport retry, POST transient and transport one-shot behavior, public-chat single submission, non-Abort recovery-first state, explicit abort immediate closure, error only after recovery exhaustion, original server elapsed time after refresh, ignoring late frames after terminal, and rejection of a previous-turn restore after a newer local send. Web tests pass 274/274 and typecheck/build pass. Production acceptance still requires one user row, one assistant row, one public terminal, no duplicate active run, no lost first delta, and no transient error flash during a controlled stream-disconnect recovery.
- Migration and rollback: No server or durable-data migration is required. Rolling back the method guard or recovery-first ordering would reintroduce duplicate-run and flicker risk and is not a safe production workaround.

## D-2026-07-19-07 Bind finish_research To The Current-Turn Source Catalog

- Status: Historical; the Interactive production source-catalog/correction/terminal protocol is superseded by `D-2026-07-19-08`.
- Refines: `D-2026-07-19-05` and `docs/adr/0004-agent-owned-research-loop.md`. It changes only the pre-terminal source-location protocol. Same-Agent evidence ownership, evidence-only tool rounds, the unchanged DirectFinal compatibility fallback, one-output streaming, security-only Interactive finalization, and the ban on post-answer semantic review, rewrite, refusal, or publication veto remain active.
- Incident evidence: Exact build `ae4d9a8f` passed repository gates, was built with its immutable prompt/skill snapshot, and was gracefully deployed from the repository root with cloud mode, Postgres, object storage, ports, auth boundaries, and enabled channels healthy. A fresh direct actor that omitted `channel_scope` then asked `crwv和英伟达有什么关系`. The run had one start, one visible answer, one successful finish, no reset/error/second answer, byte-identical history, zero remaining active chats, and first/final visible output at about 101.748 seconds. FMP DataFetch and Tavily Web calls were healthy. Content still failed: the finish handoff used generic strings such as `web_search`, `quote`, and `profile` instead of the real opaque current-turn call IDs. The ae4 runtime did not recover the generic Web ID, the DataFetch locators were also malformed, every submitted reference was dropped, and terminal synthesis received `facts=[]`; it then reversed the disclosed capacity-purchase direction and added unsupported customer/supplier, investment, GPU-model, allocation, and ranking claims. This was a source-binding defect inside the Agent loop, not stale deployment, FMP/Tavily failure, frontend replay, or evidence that an answer interceptor is needed.
- Runtime source catalog: Before exposing `finish_research`, the runtime derives a bounded catalog only from matching current-turn invocation/result pairs. It includes successful non-search DataFetch results with usable scalar data and successful Web results that have an original URL plus excerpt-capable text. DataFetch search, failed results, results without a matching invocation, unsupported tools, empty data, and non-citable Web rows are excluded. The active prompt shows only exact opaque IDs plus structural descriptions, never source excerpts, URLs, errors, or a whole-turn evidence dump.
- Dynamic exact-ID schema: When the catalog is nonempty, every `facts[*].evidence[*].tool_call_id` receives an enum containing exactly those current-turn citable IDs; tool names and DataFetch data types are explicitly not IDs. An empty catalog does not emit a provider-hostile empty enum. The evidence schema stays flat for OpenAI-compatible provider support, while the prompt keeps Web fields (`result_number + exact_excerpt`) and DataFetch fields (`json_pointer`) mutually exclusive.
- Mechanical resolution and correction: Exact IDs remain authoritative. For a provider that substitutes a generic Web tool name, the runtime may recover the locator only when the submitted 1-based result number and verbatim excerpt identify exactly one current-turn Web invocation; zero or multiple matches authorize nothing. DataFetch generic names are never guessed. If the current turn has citable catalog entries but the handoff contains an unresolved locator or contains neither a mechanically resolved related fact nor scoped fallback, the same Agent receives one hidden, pre-terminal, locator-only correction containing the exact catalog. This is not a second research role, business-tool replay, answer-content review, post-generation repair, or visible response rewrite. Valid sibling facts survive mechanical validation and may continue to terminal after that bounded correction.
- Failure and publication boundary: A direct gaps-only handoff is legal only when the current-turn citable catalog is empty, meaning the real attempted sources supplied no citable result; it must disclose concrete gaps and imports no evidence. When the catalog is nonempty, gaps alone never authorize terminal: after the one correction, the handoff must retain at least one mechanically resolved related fact or scoped fallback. Remaining invalid siblings may then be omitted and terminal uses only the valid evidence. If neither survives, the run fails as a genuine finish-protocol failure and never enters an unrestricted empty-evidence terminal. Syntax-broken controls still have no implicit whole-turn evidence authority; with citable sources they receive the same single correction, otherwise they fail rather than fabricate. Mixed business/finish rounds continue only the business calls, and provider-duplicated finish controls retain first-parseable/fixed-scope handling without scope union. A complete nonempty `Stop + Done` DirectFinal still publishes unchanged with no terminal, retry, semantic approval, rejection, replacement, or second generation.
- Verification: Focused regressions cover citable-source enumeration, exclusion of search/failure/missing-invocation/unsupported sources, dynamic exact-ID enums, an empty-catalog schema without an empty enum, unique generic-Web excerpt recovery and ambiguous rejection, generic finish IDs corrected before one grounded terminal, malformed finish receiving one hidden same-Agent locator correction, valid sibling evidence, direct gaps-only only for an empty catalog, nonempty-catalog gaps-only correction/failure, current-turn terminal isolation, and unchanged DirectFinal no-veto behavior. `hone-agent` passes 101/101, `hone-channels` passes 642/642, and finance static contracts pass 39/39. Full repository gates, exact-commit rebuild/restart, health checks, and fresh relationship/valuation/non-finance production canaries remain required before acceptance.
- Migration and rollback: No durable-data migration is required; the catalog, enum, correction counter, and resolved handoff are request-local. A safe rollback must not restore raw-transcript terminal authority, unrestricted empty-evidence synthesis, post-answer semantic validation, answer rewriting, fixed refusal, or browser POST replay.

## D-2026-07-19-08 Use The Standard Agent Loop And Natural DirectFinal For Interactive Finance

- Status: Accepted; local implementation and full proportional verification are complete, while exact deployment and production acceptance remain pending.
- Supersedes: The Interactive production use of `finish_research`, structured handoff/source catalog, locator-only correction, tool-free terminal synthesis/recovery, and terminal semantic audit in `D-2026-07-18-03` and `D-2026-07-19-01` through `D-2026-07-19-07`. It preserves their current-turn entity/evidence ownership, route-aware structural ledger, provider lifecycle/deadline safety, speculative tool-round buffering, one-output publication, exact visible/persisted bytes, Web POST one-shot recovery, and typed scheduled/heartbeat boundary.
- Incident evidence: Exact production build `d4d44735` executed ten DataFetch/Web calls successfully for `crwv和英伟达有什么关系`; FMP, DataFetch, and Tavily were healthy. The request still used seven model calls and took 92.435 seconds. Business tools consumed only about 8.9 seconds, while the post-evidence finish locator, locator correction, and tool-free terminal consumed about 13.993, 19.925, and 19.363 seconds respectively. The terminal then inferred “纽交所收盘价” from `market_date_new_york` despite NASDAQ entity evidence and invented unsupported core-supplier/major-customer/high-dependence/lock/multiple-binding claims from an empty inference set. The defect was the service-imposed completion pipeline, not stale deployment or a provider outage.
- Agent ownership: Production Interactive finance uses one ordinary function-calling Agent loop, one context, and one model role. The Agent reads the full query, identifies every named company/security, loads relevant DataFetch/Web/business-tool results into that same context, and then either calls another real tool or returns one natural nonempty `Stop + Done` DirectFinal. The same Agent that selects evidence writes the user answer.
- Production prohibition: The production Interactive tool schema does not expose `finish_research`. The service does not request a completion handoff, correct opaque locators, start a tool-free second generation, run a terminal audit, semantically approve/reject final prose, rewrite market copy, append a completeness warning, or replace the answer with a fixed refusal. The legacy builder/state is compiled only for tests, so a production binary cannot enable it.
- Channel and history boundary: `AgentRunnerRequest.agent_owned_finance_loop` selects this mode independently of terminal streaming policy for Interactive `function_calling` turns on Web, Feishu, Discord, and Telegram. Finance requests use the composed current input plus at most four prior user utterances / 4,000 characters, explicitly labelled as reference-only context. Historical assistant/tool/reasoning content, prices, financials, and conclusions are excluded. A compact summary may already exist inside the composed current input, but neither source is current-turn fact evidence or authority to add an unreferenced entity.
- Structural guidance: Entity routes, exact/name match declarations, identity-only search semantics, bounded no-coverage attempts, and quote/profile/asset-route coverage remain request-local guidance and telemetry. Before the evidence floor, tool choice is business-tool `Required`; after it, the same real tools remain under `Auto`, allowing more evidence work or natural DirectFinal. Lowercase/mixed-case symbols in a securities context still take normalized exact-symbol lookup. The floor is never a publication gate and cannot veto a complete answer.
- Output boundary: Tool-capable rounds remain buffered because their prose may precede another tool call. Only the completed natural DirectFinal becomes visible and persisted, once, with no reset or second answer. Provider/transport/deadline/lifecycle failures remain genuine failures and cannot manufacture finish authority or investment-completeness copy.
- Time and exchange evidence: Security/market/sector answers still begin with current Session Beijing query time and an evidence-based quote basis. Quote-source time should come from `hone_quote_time.beijing`. `market_date_new_york` and generic `new_york` values establish only a market date/timezone; they do not prove NYSE, a close, or regular-session close. Exchange wording must come from structured `exchange` / `exchangeShortName` fields.
- Relationship language: Bounded Web snippets are not page bodies or rankings. Current evidence must directly support relationship direction and strength. When it does not, the Agent uses neutral wording and must not elevate it to core/largest/major customer or supplier, high dependence, locked/exclusive/priority status, multiple binding, ownership/non-ownership, GPU model, contract value, or similar detail.
- Typed boundary: Caller-supplied scheduled/heartbeat investment contracts keep their deterministic preparation, validation, fallback, and fail-closed path. This decision changes only open-ended Interactive finance.
- Verification: Production-path tests expose only real business tools, prove `Required → Auto` around the structural floor, accept one natural DirectFinal with no finish/correction/terminal/audit model call, keep tool-round drafts hidden, and emit/persist one byte-identical body. They also cover channel-independent mode selection, bounded prior-user follow-up wording with historical assistant/tool/price exclusion, hallucinated retired and generic unregistered tool names using ordinary unknown-tool behavior without public tool status, and lowercase `query="crwv"` plus `identity_match="exact_symbol"` resolving canonical `CRWV`. The CRWV/NVIDIA fixture supplies entity-bound quote/profile fields and explicit relationship excerpts so its time, NASDAQ, purchase direction, investor, and MFN assertions cannot be prewritten without evidence. Content tests distinguish Session time, `hone_quote_time.beijing`, `market_date_new_york`, and NASDAQ exchange fields and reject unsupported relationship-strength expansion through Agent prompt behavior rather than a service output filter. The complete pre-rebase workspace test passed; exact rebased commit `e25a2879` passed workspace check, Agent 104/104, channels 643/643, Web 275/275 plus public build, finance contracts 39/39, all CI-safe regressions, formatting, and diff checks. Production acceptance still requires exact-commit build/deploy, zero-active-chat restart, PG/object-store/API/channel health, fresh relationship/valuation/single-ticker/non-finance canaries, no more than five model calls on the normal relationship path, under-60-second first-visible target, one public terminal, exact history, and zero active chats afterward.
- Migration and rollback: No durable-data migration is required. A safe fallback may keep the final natural body fully buffered; it must not reactivate finish handoff, locator correction, tool-free terminal generation, semantic review, fixed refusal, or browser POST replay.

## D-2026-07-19-09 Deliver Authenticated Community Archives From Private R2 At The Edge

- Status: Accepted; implementation and local verification are complete. The initial private production R2 snapshot was published and idempotently rechecked on 2026-07-19 (`662` contents, `833` resources, `754` publication objects, final `no_op=true`). The exact-route Worker is now deployed without a secret or enabling variable and returns the reviewed fail-closed `503`. Implementation commits `385e35b0` and `100f5608` are on `main`; automatic Pages deployment completed with discovery compiled out. Exact backend build `100f5608` is staged locally but has not replaced or restarted the previous process, so backend configuration and user traffic remain unchanged. The remaining actions stay explicit operator steps in `docs/runbooks/backend-deployment.md`.
- Context: The legacy `/api/public/community` path reads PostgreSQL on each feed request, while resource delivery authenticates, looks up metadata, downloads the complete object through the backend, and verifies it before responding. The backend remains the compatibility and personal-state authority, but putting every feed page, image, PDF, and attachment byte on that hot path makes origin latency and bandwidth the user-visible bottleneck. Adding Redis would not remove the binary proxy or provide the durable object delivery layer.
- Decision: PostgreSQL stays the canonical community archive and the existing private R2 bucket stays the durable resource store. A dry-run-first `hone-cli cloud community-publish` projects non-personal feed pages, immutable resource descriptors, and a mutable active-version index into the same private bucket. Apply reads through one repeatable-read/read-only snapshot on the dedicated PG advisory-lock session, fully hashes eligible resource objects before any publication write, rechecks lock liveness, refuses empty/conflicting/oversized output, writes descriptors then `resources/active.json` then cursor pages, and updates mutable `feed/latest.json` last. Mutable writes are read back and explicit unlock failure is fatal. Dry-run checks exact-key HEAD existence only and labels that weaker proof. Redis is deliberately excluded from the binary hot path; it may be considered later only for independently measured metadata or personalized-state contention.
- Authentication and cache boundary: The backend and Worker require the same trimmed 32..1024-byte HMAC secret. The backend issues a short-lived grant only through a scoped HttpOnly cookie and never returns the token, secret, or actor identity in JSON; invalid secret configuration disables and clears grants. The exact-route Worker validates that grant, then checks every resource id/version against uncached `active.json` before any shared-byte cache lookup, so a later revocation cannot be bypassed by cached bytes. R2 remains private with no `r2.dev` or bucket custom domain. Static feed/resource bytes may use a canonical shared cache after authentication, resource cache TTL is at most one hour, while the browser response remains private/Vary Cookie and actor-specific unread/seen state stays on the backend. Errors, inactive resources, invalid indexes/descriptors/integrity results, and legacy responses are not shared-cache entries.
- Compatibility boundary: The backend defaults to `community_delivery.mode=off`; `shadow` can exercise grant issuance without client cutover, and only `prefer` plus a separately enabled Pages build flag activates client discovery. The client validates the fixed base path and each versioned resource path, then immediately falls back to the unchanged legacy feed/resource API on discovery, GET, HEAD, image, PDF, or download failure. The Worker can fall back only on eligible feed/resource GET misses to the fixed legacy origin, requests identity encoding, enforces 8MiB/128MiB legacy response ceilings, and forwards only `hone_web_session`, never the edge or unrelated cookies. Resource HEAD failures and inactive/invalid/integrity states return non-2xx without an internal second download.
- Rollout and rollback: Roll out in layers: backend/Pages remain off; for a brand-new Worker confirm no remote `EDGE_DISABLED` value exists, while any existing/restored Worker must first deploy explicit `EDGE_DISABLED=true`; install the same secret at backend and Worker; publish and verify descriptors/active index/pages/latest; move backend to `shadow`; explicitly deploy `EDGE_DISABLED=false`; validate authenticated edge and origin fallback traffic; move backend to `prefer`; then deploy Pages with discovery enabled. Roll back first by setting `EDGE_DISABLED=true` in the dashboard and deploying (removing it is unreliable with `keep_vars=true`), then return the Pages flag to zero, and finally return the backend to `shadow` or `off` on the next operator-managed restart. Emergency resource revocation disables the Worker, performs a Cloudflare global purge rather than data-center-local `cache.delete`, publishes an active-index tombstone, and verifies the old direct path returns 404 before re-enabling. Existing grants expire within one hour and immutable R2 artifacts are retained for diagnosis/retry rather than deleted during rollback.
- Consequences: Feed and resource bodies can be served without a database lookup or full object relay on the normal edge hit, while current users retain the same routes and visible behavior during a staged cutover. The cache is Cloudflare Cache API locality, not a new source of truth or guaranteed tiered cache; R2 remains the durable fallback. Image transformation/CDN variants are intentionally deferred until origin-size and format measurements justify a separate Cloudflare Images binding and cost model.
- Verification: Required local proof covers Rust grant/config/logout behavior and the Rust-to-Worker golden token, publisher planning/locking/idempotence, Worker fail-closed/auth-before-cache/R2/fallback rules, and frontend discovery/path validation/immediate fallback. Production acceptance additionally requires the phased health, anonymous/authenticated, feed/resource, browser, monitoring, and rollback canaries in the deployment runbook.
