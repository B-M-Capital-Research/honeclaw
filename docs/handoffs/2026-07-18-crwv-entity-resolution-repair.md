# CRWV Exact-Ticker Entity Resolution Repair

- title: CRWV exact-ticker versus embedded-product entity resolution repair
- status: in_progress
- created_at: 2026-07-18
- updated_at: 2026-07-19
- owner: Codex
- related_files: `Cargo.lock`, `agents/function_calling/{Cargo.toml,src/lib.rs}`, `crates/hone-core/src/provider_symbol.rs`, `crates/hone-tools/src/{data_fetch.rs,web_search.rs}`, `crates/hone-channels/src/investment_response_guard.rs`, `crates/hone-channels/src/security_identifier.rs`, `crates/hone-channels/src/response_finalizer.rs`, `crates/hone-channels/src/agent_session/{core.rs,emitter.rs,helpers.rs,tests.rs}`, `crates/hone-channels/src/{execution.rs,mcp_bridge.rs,run_event.rs,tool_trace.rs,prompt.rs,runtime.rs}`, `crates/hone-channels/src/runners/{types.rs,tool_reasoning.rs}`, `crates/hone-llm/src/{provider.rs,openrouter.rs,openai_compatible.rs}`, `crates/hone-web-api/src/routes/{chat.rs,public.rs}`, `crates/hone-core/src/config/{server.rs,tests.rs}`, `packages/app/src/lib/{backend.ts,backend.test.ts,api.test.ts,public-chat.ts}`, `packages/app/src/pages/{chat.tsx,chat.test.ts}`, `soul.md`, `skills/{stock_research,market_analysis}/SKILL.md`, `tests/regression/ci/test_finance_automation_contracts.sh`, `tests/regression/manual/test_entity_search_live.sh`
- related_docs: `docs/current-plans/ticker-resolution-architecture.md`, `docs/decisions.md#d-2026-07-17-04-resolve-securities-through-a-span-aware-exact-first-pipeline`, `docs/decisions.md#d-2026-07-18-01-keep-entity-discovery-inside-the-main-agent-tool-loop`, `docs/decisions.md#d-2026-07-18-02-keep-interactive-observations-out-of-the-publication-path`, `docs/decisions.md#d-2026-07-18-03-use-an-agent-signaled-tool-free-terminal-stream`, `docs/decisions.md#d-2026-07-19-01-keep-research-and-finish-ownership-in-one-agent-loop`, `docs/decisions.md#d-2026-07-19-02-share-one-exact-last-mile-evidence-contract-without-a-publication-gate`, `docs/decisions.md#d-2026-07-19-03-require-per-entity-route-coverage-before-explicit-finish`, `docs/decisions.md#d-2026-07-19-04-make-tool-enabled-research-rounds-evidence-only`, `docs/decisions.md#d-2026-07-19-05-carry-current-turn-provenance-in-finish_research`, `docs/decisions.md#d-2026-07-19-06-retry-only-idempotent-web-requests-and-recover-chat-streams-before-error`, `docs/decisions.md#d-2026-07-19-07-bind-finish_research-to-the-current-turn-source-catalog`, `docs/decisions.md#d-2026-07-19-08-use-the-standard-agent-loop-and-natural-directfinal-for-interactive-finance`, `docs/adr/0003-isolated-agent-research-control.md`, `docs/adr/0004-agent-owned-research-loop.md`, `docs/invariants.md`, `docs/repo-map.md`, `docs/bugs/scheduler_finance_entity_guard_misclassifies_instruction_words.md`, `docs/runbooks/backend-deployment.md`
- related_prs: commits `4d419770`, `b87c4cb7`, `2d6b4be8`, `8d4fcdd6`, `fcca5a35`, `54b14068`, `25090e88`, `8df08239`, `2d02621c`, `1365b31b`, `022981f2`, `0cd03abb`, `d64daf24`, `c7c5741f`, `ac2468f9`, `3d2474b5`, `ae4d9a8f`, `d4d44735`

## Summary

The production failure was not an FMP/DataFetch outage. For lowercase `crwv`, the exact quote probe correctly verified CoreWeave (`CRWV`), while semantic search also returned GraniteShares YieldBOOST CRWV ETF (`CWY`). The old reconciliation score treated any candidate name containing the query as a competing company name, so a product that merely referenced its underlying ticker generated a false identity conflict.

The repair classifies exact ticker identity, strong natural-name relations, and embedded-ticker products separately. Provider-verified `CRWV` now wins over `CWY`; the same rule covers `RKLB/RKLX` and `AAPL/AAPU`. Genuine code-versus-company conflicts such as Ford Motor (`F`) versus ticker `FORD` still clarify. Natural-name fallback uses word boundaries, so Apple resolves to `AAPL` without treating Appleseed Fund (`APPLX`) as an Apple-name match.

## What Changed

- Tentative ticker reconciliation now considers only strong full-name or word-bounded name-prefix relations; low semantic scores are unresolved instead of arbitrary ambiguity lists.
- ETF, ETN, fund, leveraged, yield, long/short, covered-call, option, warrant, and similar names that embed a different requested ticker are classified as reference products and cannot challenge an exact same-symbol quote.
- Derivative-only semantic results cannot replace a missing exact ticker, while full product names can still resolve to the product itself through the normal named-entity path.
- Added deterministic regressions for `CRWV/CWY`, `RKLB/RKLX`, `AAPL/AAPU`, `Ford/FORD`, `Apple/Appleseed`, derivative-only search, and named-company fallback.

## Verification

- `cargo test -p hone-channels investment_response_guard::tests::` = 83 passed after the word-boundary repair.
- `cargo test -p hone-channels` = 596 passed; the proportional full workspace run also completed successfully before the final word-boundary refinement.
- `bash tests/regression/run_ci.sh` passed; finance automation contracts = 26/26.
- Credentialed `tests/regression/manual/test_entity_search_live.sh` passed. Live DataFetch returned CoreWeave for exact `CRWV`, quote `73.21`, a non-ETF CRWV profile, and three different-symbol products whose names reference CRWV, proving the provider was healthy and the ambiguity was local logic.
- Rebuilt `hone-cli`, `hone-console-page`, `hone-discord`, `hone-feishu`, and `hone-mcp`. Gracefully stopped supervisor `39101` only after active chat count reached zero, then started supervisor `85148` with backend `85163`, Discord `85418`, and Feishu `85439`.
- Production `/api/chat` probe `crwv当前价` completed in 11.8 seconds with CoreWeave/CRWV, current quote `73.21`, time-first output, one `assistant_delta`, one successful `run_finished`, and no reset/error.
- Production probe `crwv预计估值多少` completed in 69.6 seconds with CoreWeave/CRWV, server-owned time and quote, the nine-section equity template, one final answer, and one successful terminal event. Active chat count returned to zero.
- Final health: Postgres and S3 connected; 8077 and 8088 served from backend `85163`; Web, Discord, and Feishu each reported one current process; public root returned HTTP 200.

## Risks / Follow-ups

- This incident is closed, but the umbrella cross-market ticker plan stays `in_progress`: the post-restart scheduler window still reproduced the existing P2 where task prose can surface `800G`, `AST`, `SEC`, `NAND`, or named-company listing ambiguity. That separate live defect remains tracked in `docs/bugs/scheduler_finance_entity_guard_misclassifies_instruction_words.md`; it is not an FMP outage and was not hidden by this CRWV handoff.
- The deep CRWV valuation request used the deterministic evidence-safe fallback because the model draft failed the full response contract. It intentionally disclosed unavailable valuation inputs instead of inventing a target price.
- No database, session, portfolio, quota, or storage migration was required.

## Next Entry Point

For tentative ticker/name reconciliation, start at `resolve_tentative_named_match`, `reconcile_tentative_entity_match`, `candidate_is_embedded_ticker_reference`, and `tentative_name_candidate_score` in `crates/hone-channels/src/investment_response_guard.rs`. Continue the remaining scheduler/entity work from `docs/current-plans/ticker-resolution-architecture.md` and the linked P2 bug record.

## Phase 2 — Multi-Ticker Agent-Loop Entity Discovery

> Historical design note: Phase 4 and ADR 0004 supersede the optional Interactive publication contract, hidden omitted-seed continuation, and read-only answer repair described below. Main-Agent discovery, non-factual lexical seeds, and current-turn tool evidence remain active.

### Incident and Root Cause

The production turn `分析下crwv和nbis的估值` failed at 2026-07-18 15:03 Beijing time after about 16.2 seconds. The lexical scanner had already captured both `CRWV` and `NBIS`, but residual-language deletion left `下`; the comparison-binding rule then treated `和` as evidence that the set was incomplete even though both sides were present. The request entered the auxiliary entity LLM, hit its fixed 15-second timeout, and returned the generic entity-recognition failure. Logs contained no DataFetch start, batch probe, or `agent.run start` for the turn. Same-window failures across unrelated symbols confirmed an architecture-level single point, while live FMP results showed both securities were healthy.

### Architecture Change

- Removed the current-turn blocking auxiliary entity extraction, timeout, generic availability error, and `NeedsClarification` pre-run outcome.
- Lexical results are now explicitly candidate seeds. For interactive traffic they never close the scope, trigger clarification, or start a DataFetch/portfolio preflight; every nonempty wording becomes `AgentToolDiscovery` so the configured main runner reads the complete query. Portfolio membership is loaded by the Agent's own `portfolio(view)` call in that same loop.
- If the Agent finds named securities, its tool loop searches every candidate—including explicit tickers—and exact-verifies all selected symbols with quote/profile before problem-specific evidence. High-confidence explicit code seeds are checked only after the run as a minimum that cannot be silently omitted; they are not a closed entity list. Search may refine across multiple assistant rounds, and an earlier empty broad query cannot hide later exact evidence. Ordinary finance or non-security turns can simply continue without an irrelevant DataFetch call.
- After the same run completes, the service derives an optional entity contract from all current-run search/refinement calls and structured tool facts. Every contracted entity must intersect a later positive same-symbol quote with a usable provider timestamp. The actual financials, holdings, news, web, earnings, sector, market, or extended-hours results selected by the Agent remain in its context for synthesis; user wording does not select a server-side depth route, and tool presence does not authorize a fixed chapter parser.
- DataFetch response envelopes are excluded from candidate rows, fixing the Chinese-name case where the wrapper query `英伟达` previously hid the returned provider symbol `NVDA`. CRWV/CWY is resolved only when CRWV itself has later exact quote evidence; quoting only CWY fails. A complete current trace must cover named NBIS and later exact quote evidence before it can create a two-sided verified comparison contract.
- Dynamic contracts follow observed search/quote scope only. The service no longer infers tool calls from “估值”, “最近”, “盘前”, or any other wording vocabulary, and it does not force a fixed comparison/deep template. Financials, holdings, news, and web results are evidence in Agent context, not cues for parsing its natural-language chapters.
- Dynamic interactive validation is limited to deterministic boundaries: server time, resolved entity, exact current quote/provider time, exact extended-session identity/session/freshness when present, false market-data denial, and one session/history/SSE result. The Agent retains control of scope, depth, structure, length, and priorities. Typed scheduled/heartbeat contracts retain their deterministic formats and strict field-aware validators. Any future strict validation of individual financial, holding, valuation, or event claims requires structured provenance tied to tool results.
- Genuine provider no-coverage/failure, equal-candidate ambiguity, omitted explicit seeds, and missing exact quote/time all preserve a successful Agent response. Omitted seeds may receive one hidden read-only continuation, but failure to build the optional contract afterward is diagnostic only and never authorizes a fixed refusal.
- All interactive drafts use the deferred output boundary. Tool progress remains visible, but draft deltas/resets are withheld; one deterministic-fact, read-only repair may preserve and correct the Agent answer, and the dynamic path never replaces it with a service-authored whole-answer fallback. Exactly one final answer is published.
- Repair retains the initial and retry tool traces in execution order and rejects unknown or persistent repair calls before accepting a revised answer.
- Programmatic `FmpConfig::default()` now matches serde defaults (`base_url` plus a 60-second timeout). This closes a separate zero-timeout path exposed by the strict tool-loop regression fixture; credentialed live checks continued to show the production FMP provider itself was healthy.
- The same main-agent-loop rule is now encoded in `soul.md`, the runtime finance policy, the canonical stock-research skill, repository invariants, repo map, and `D-2026-07-18-01`.

### Verification So Far

- Focused pure tests cover whole-trace search refinement, CRWV/CWY exact disambiguation, CWY-only rejection, explicit NBIS omission, CRWV+NBIS provider timestamps, per-equity financial/news tool-trace capture, tool aliases, partial quote rejection, contract-none answer preservation, and a generic no-search turn.
- AgentSession tests prove arbitrary surrounding wording, the exact production phrase, Chinese company names, and the dedicated financial-evidence round reach the real function-calling tool loop with `chat_calls() == 0`; exact DataFetch results produce a provider-time prefix and one visible answer. Repair tests cover merged traces plus unknown/persistent retry rejection.
- `FmpConfig::default()` is regression-covered against empty serde deserialization, including the nonzero timeout.
- Finance CI contract = 27/27, including a guard that rejects restoration of the auxiliary timeout/failure path.
- Credentialed live DataFetch regression passed. CRWV exact-resolved CoreWeave at `73.21`, NBIS exact-resolved Nebius at `177.71`, and one `CRWV,NBIS` batch quote returned fresh positive same-symbol records for both. Provider health was not the incident cause.
- `cargo test -p hone-channels --all-targets` = 611 passed after the output-boundary hotfix. Added regressions preserve Agent-owned no-coverage and equal-candidate clarifications, emit one final delta, prove shared-heading/free-form CRWV+NBIS valuation prose passes with observed financial/news evidence, reject a forged CRWV `15 USD` current price, and cover omitted-seed read-only continuation while retaining the complete audit trace.
- `cargo check --workspace --all-targets --exclude hone-desktop --exclude hone-user-app` and `cargo test --workspace --all-targets --exclude hone-desktop --exclude hone-user-app` passed. The only compiler diagnostic was the pre-existing unused-function warning for `feishu_direct_actor_contact_targets_from_records`.
- `PATH="$HOME/.bun/bin:$PATH" bun run test:web` passed 265/265; `bash tests/regression/run_ci.sh` passed every CI-safe regression, including finance contracts 27/27.
- Main-agent discovery commit `2d6b4be8` and output-boundary commit `8d4fcdd6` were pushed. The five runtime binaries were rebuilt; with active chat count at zero, supervisor `62866` was stopped by SIGINT without killing children individually and replaced by supervisor `78710`, backend `78722`, Discord `78978`, and Feishu `78997`. Ports 8077/8088 returned HTTP 200, and `/api/meta` reported healthy Postgres and S3 connections.
- The first deployed production replay of the exact phrase `分析下crwv和nbis的估值` reached the intended Agent loop. It searched, quoted, profiled, loaded financials, and loaded news independently for `CRWV` and `NBIS`; the log recorded `entity_resolution.agent_loop contract_built=true entities=CRWV,NBIS`. Provider/entity discovery therefore succeeded.
- That first replay was a failed acceptance result: legacy free-prose financial/event checks rejected the Agent answer, no final `assistant_delta` was published, and the run ended with one unsuccessful terminal event. Review also found the repair suffix indirectly reused the typed fixed comparison enforcement block. Commit `8d4fcdd6` removed both strong-interference paths while retaining exact quote/time/session checks.

### Final Production Acceptance

- A fresh production Web actor replayed the exact query `分析下crwv和nbis的估值` after the hotfix deployment. The main Agent made 10 DataFetch calls: independent search, quote, profile, financials, and news calls for both CRWV and NBIS. The log recorded `contract_built=true entities=CRWV,NBIS`.
- Run `8da0538d-8bad-42f0-a700-373f0a9edb83` completed successfully in 78.405 seconds. Its visible answer began with server Beijing time, named both verified entities, and included exact current quotes `73.21 USD` and `177.71 USD`. SSE contained exactly one `run_started`, one `assistant_delta`, and one successful `run_finished`, with no `assistant_reset`, `run_error`, or generic `error`.
- Persisted history contained exactly `user,assistant`; the user text matched byte-for-byte, and active chat count returned to zero. This closes the Interactive CRWV+NBIS phase. The umbrella ticker plan remains active only for the separately documented scheduler task-prose P2.

## Phase 3 — Remove The Unrequested Interactive Publication Ban

### Reopened Incident

At 20:06 Beijing time, production received `分析下crwv和nbis的估值` again. This was not an FMP/DataFetch outage: the retry trace contained empty exploratory searches for `CRWV CoreWeave` and `NBIS Nebius`, followed by successful exact `CRWV` and `NBIS` searches, exact quotes `73.21` and `177.71` with provider timestamps, both profiles and financial statements, and two web searches. The model completed a 6037-character valuation answer.

The response was lost locally. Commit `2d6b4be8` had introduced two coupled assumptions: dynamic reconstruction selected only the first assistant search group, and `AgentDiscoveryDisposition::UnsafeIncomplete` then converted an otherwise successful Interactive result into failure and replaced its content with a fixed “cannot safely publish” sentence. The first assumption ignored the Agent's successful refinement; the second was an unrequested publication ban that did not exist in the product prompt or configuration.

### Architecture Correction

- Interactive search evidence is now collected from the entire current runner trace. Earlier empty/failed broad or enriched attempts are skipped; later exact-symbol refinements remain authoritative when joined to positive same-symbol quotes and usable provider timestamps.
- Explicit ticker seeds bound the strong dynamic-contract scope. A CRWV request can use later CRWV refinement evidence without absorbing a separately researched CWY ETF; a CRWV+NBIS request must retain both exact seeds.
- A mixed ticker/name request may also include a uniquely verified search query grounded in the user's literal company-name text. If a quote-verified search is not tied to either a seed or that text, it could be a translated alias or an auxiliary comparable; the service returns no optional contract rather than building a one-sided subset. A regression locks `比较 CRWV 和英伟达` with `search(NVIDIA)` to contract-none while the existing answer-preservation path keeps the full Agent response.
- The `UnsafeIncomplete` enum, fixed refusal constant, and success-to-failure response mutation are deleted. `contract_built=false` is logged as `answer_preserved=true`, then the existing normal path returns the successful Agent body unchanged.
- A dynamic Interactive contract is optional server fact enhancement, not permission to answer. Typed scheduled/heartbeat contracts, real runner/auth/quota errors, persistent-side-effect protection, and deterministic checks on a contract that was successfully built remain unchanged.
- The hidden omitted-seed continuation now explicitly tells the Agent to search an explicit ticker using the original code alone rather than concatenating a company name. The implementation still does not depend on prompt compliance: all later search refinements are considered structurally.
- Finance CI now rejects restoration of first-search freezing, `AgentDiscoveryDisposition`, or `UNSAFE_AGENT_DISCOVERY_MESSAGE` and requires both production-shape refinement coverage and contract-none answer preservation.

### Regression Evidence

- Guard coverage reproduces `CRWV CoreWeave` / `NBIS Nebius` empty results followed by exact CRWV/NBIS results, quotes, profiles, and financials; the rebuilt contract contains exactly CRWV and NBIS with `73.21` / `177.71`.
- AgentSession coverage starts with a no-tool historical draft, triggers the bounded seed retry, performs both empty enriched searches and later exact searches, builds the two-entity contract, drops the stale draft, and emits one successful final body.
- A separate AgentSession regression deliberately returns a CRWV quote without a usable timestamp so the optional contract cannot build; the Agent's concrete valuation/data-gap answer remains `success=true`, byte-for-byte intact, with one visible delta and no error.
- Existing Agent-owned no-coverage and equal-candidate clarification tests continue to pass. Finance automation contracts pass 27/27.
- `cargo test -p hone-channels --all-targets` passes 615/615; workspace `cargo check` and `cargo test` pass with the existing unrelated Feishu dead-code warning only. Web tests pass 265/265 and `tests/regression/run_ci.sh` passes every CI-safe contract.

### Final Production Acceptance

- Commits `fcca5a35` and `54b14068` were pushed to `main`. The latter closes the independent review finding where `CRWV + 英伟达` plus `search(NVIDIA)` could otherwise build a CRWV-only subset contract; an unlinked verified alias/comparable now yields contract-none and preserves the full Agent answer, while embedded products such as CWY remain safely ignorable.
- Rebuilt `hone-cli`, `hone-console-page`, `hone-discord`, `hone-feishu`, and `hone-mcp`. With active chat count zero, supervisor `78710` received SIGINT and exited with its children in two seconds; no child was killed individually. The new screen `62798` runs supervisor `62801`, backend `62812`, Discord `63047`, and Feishu `63065`.
- Post-restart health passed: 8077/8088 listen from backend `62812`; `/api/meta` reports Postgres and S3 connected; local/public auth probes return 401 as expected, local/public `/chat` returns 200, and active chats are zero.
- Fresh actor `codex-regression-54b14068-a-1784378834` replayed `分析下crwv和nbis的估值`. The trace logged `contract_built=true entities=CRWV,NBIS`; history contained exactly `user,assistant`; the 3349-character time-first answer used verified prices `73.21` and `177.71`. SSE contained one `run_started`, one `assistant_delta`, one successful `run_finished`, no reset/error, and no fixed refusal.
- Fresh actor `codex-regression-54b14068-b-1784378953` replayed `crwv和nbis的估值怎么看` with the same verified entity/price outcome. Its 4059-character time-first answer, SSE terminal counts, two-message history, forbidden-copy audit, and zero-active-chat check all passed.
- First-visible-text latency remains a separate follow-up because Interactive body deltas are still deferred until the tool loop and optional contract handling finish. That optimization must preserve the no-flicker one-answer boundary and must not restore a service-owned publication veto.

## Phase 4 — Remove Interactive Post-Run Publication Control

### 21:05 Reopened Incident

Production persisted `crwv和英伟达什么关系，估值怎么看` at 21:05:56 Beijing time and the failed assistant result at 21:08:00. The turn ran for about 123.3 seconds on the current deployed runtime; this was not a stale-deployment incident. DataFetch was healthy and spent only a small fraction of the turn, including a verified CRWV quote of `73.21`. Most latency came from the model's initial synthesis and a second post-run repair.

The model request restored 653 messages totaling about 288,823 characters from a session with more than 650 rows. That context included many scheduler/heartbeat pairs, prior `run_failed=true` turns, and stale CRWV+NBIS analysis. Web chat had explicitly disabled the normal direct restore limit. The globally configured runner was `codex_acp`, so compaction was skipped as if the runner owned its context, but this non-admin Web actor actually used the strict in-process function-calling fallback. The resulting draft opened as a CRWV/NBIS comparison and only briefly covered NVIDIA.

Post-run handling turned context contamination into a user-facing failure. Dynamic reconstruction observed CRWV but did not connect the Chinese NVIDIA mention to a current-run NVDA search. The draft also contained exact `73.21` and a rounded `73`, which the numeric truth checker treated as a conflicting current price. A second roughly 37.6-second repair still failed, and the backend replaced the answer with the fixed “投研完整性检查” sentence. The brief error styling followed that terminal backend failure and immediate history restoration; it was not evidence that the frontend independently launched another run.

### Architecture Correction

- Interactive entity resolution, evidence selection, and final synthesis remain one main Agent loop over the complete current query. The Agent itself owns the prompt-required first visible Beijing data-time line and the rest of the answer.
- Agent-discovered investment reconstruction is now observational telemetry only. It may record current-run search/quote coverage, but it can never add a prefix, normalize the body, start an omitted-seed continuation or whole-run retry, invoke a truth repair, rewrite content, reject the answer, or turn `success=true` into failure. This rule applies whether the observation is complete, partial, ambiguous, or absent.
- Caller-supplied typed scheduled/heartbeat contracts remain separate and strict: deterministic preparation, server-owned time/entity/quote fields, field-aware validation, safe repair/fallback, and fail-closed semantics are unchanged. Runner/auth/quota failures and persistent-side-effect protection also remain genuine failures.
- Web direct-message restore returns to its bounded default. Before an Interactive runner request is assembled, complete prior scheduler/heartbeat/job groups and complete prior failed terminal groups are removed from model context only. Durable session history, UI bootstrap/history pagination, scheduler records, push rows, and audit evidence remain unchanged.
- `DeferredUserOutputEmitter` stays in this correctness phase to preserve one final body and one terminal event without draft/reset/error flicker. It still delays first visible body text; a safe streaming design remains a separate latency follow-up and must not restore post-run publication control.

### Verification Status

- Commit `25090e88` makes all Agent-discovered Interactive contracts observational and adds bounded Web restore plus request-local pruning for historical automation and failed terminal groups. Commit `8df08239` strengthens the Agent-owned terminal format: when the Agent semantically identifies an investment request, its first visible character is `数` and its first nonempty line is the current Beijing data-time plus Agent-written quote basis. The service does not prepend or post-hoc normalize that line.
- Regressions reproduce the exact CRWV/NVIDIA incident with stale NBIS context and the `73.21`/rounded-`73` text, preserve the successful answer byte-for-byte with one runner call, and prohibit fixed refusal copy. Context tests prove prior automation/job and `run_failed=true` groups are removed only from current Interactive model input while durable session rows remain intact.
- All 620 `hone-channels` tests, workspace check/test, 265 Web tests, and every CI-safe regression passed. Five runtime binaries were rebuilt; with active chats at zero, supervisor `91431` started backend `91506`, Discord `92217`, and Feishu `92239`. Postgres/S3 health passed, authenticated endpoints returned the expected `401` without credentials, and active chats remained zero.
- Fresh actor `codex-regression-8df08239-crwv-nvda-1784383806` replayed `crwv和英伟达什么关系，估值怎么看`. The same Agent run used three searches; exact CRWV and NVDA quotes and profiles; CRWV news and financials; relationship web evidence; and an actor-scoped portfolio view. It did not drift to NBIS. The log recorded `contract_built=false answer_preserved=true mode=observational` and completed successfully.
- The visible answer's first code point was `数`; its first line was `数据时间：北京时间 2026-07-18 22:10；行情口径：NASDAQ 常规交易收盘价（2026-07-17），最新可得、非逐笔。`. SSE contained one `assistant_delta` and one successful `run_finished`, with no reset, run error, or fixed refusal. Persisted history was exactly `user,assistant`, active chats returned to zero, and the run took roughly 67 seconds end to end. This completes Phase 4 correctness acceptance; that duration is only the baseline for Phase 5 and is not a claimed latency result.

### Risks / Next Entry Point

- Do not diagnose another occurrence as FMP/DataFetch failure without separating provider duration/results from restored-context size, Agent trace, and post-run response handling. This incident had healthy provider evidence and current binaries.
- Do not “fix” first-visible-text latency by exposing draft deltas from a tool-capable round. Correctness requires one Agent-owned answer with no service-authored second pass; Phase 5 uses an explicit tool-free terminal boundary instead.
- Continue latency implementation and regression review from `agents/function_calling/src/lib.rs`, `runners/tool_reasoning.rs`, `agent_session/{emitter.rs,core.rs}`, and `D-2026-07-18-03`. The handoff remains `in_progress` until a rebuilt production runtime records first-visible timing separately from terminal completion.

## Phase 5 — Agent-Signaled Tool-Free Terminal Streaming

> Historical design note: Phase 6 supersedes the sole/mixed `finish_research` routing and absolute no-recovery-after-prefix rule below. The same-Agent context, hidden speculative tool rounds, tool-free terminal, and exact visible/persisted body requirements remain active.

### Latency Root Cause

Correctness deployment removed the repair/refusal loop, but first visible text remained late because `DeferredUserOutputEmitter` intentionally swallowed every ordinary content delta until the Agent tool loop and final synthesis had completely ended. Directly forwarding those deltas is unsafe: a function-calling model may emit a prose preamble and then a tool call in the same round, requiring `StreamReset`. Exposing that speculative branch would restore the exact flicker/second-answer behavior this incident removed.

### Architecture In Progress

- Interactive Web function-calling turns that are re-executable, use Agent discovery, and have no caller-supplied typed investment contract receive a narrow terminal-stream policy. Scheduled/heartbeat contracts, ACP runners, execute-once mutations, and other channels keep the existing behavior.
- The function-calling Agent exposes an internal `finish_research` control tool. Only when it is the sole call after all required evidence is already in the same in-memory Agent context does the same `Agent::run` enter one terminal synthesis request. That request carries the accumulated current-turn context but sends an empty tool set; provider request builders remove `tools`, `tool_choice`, and `parallel_tool_calls` while preserving generation options and native SSE.
- Tool-capable rounds remain speculative and fully deferred. In the tool-free terminal stream, the runner buffers until it has one complete, sanitized line matching `数据时间：北京时间 YYYY-MM-DD HH:MM；行情口径：...\n`. Only that exact line becomes an irreversible committed delta. The remaining terminal deltas stay behind `DeferredUserOutputEmitter`; after final sanitization and attachment handling, `AgentSession` emits exactly the persisted response suffix so the concatenated visible text and stored body are byte-for-byte equal.
- A direct answer that never calls `finish_research` uses the old deferred one-shot publication and does not incur another model request. A mixed `finish_research` plus business-tool round ignores the control signal, executes the real tools normally, and does not consume business tool budget or expose the internal control call. Multiple non-sole finish calls likewise cannot enter terminal mode.
- Once any canonical prefix is committed, transient/empty retries, context-overflow recovery, reset, and replay are prohibited. Finalization must preserve that exact prefix; a mismatch is a terminal error rather than authority to rewrite or replay the already visible text.

### Verification Pending

- Complete focused Agent, provider, runner/emitter, AgentSession, and CI contract tests for sole/mixed/direct control behavior, empty-tools wire payloads, fragmented/invalid headers, no retry after commit, and exact visible/persisted equality.
- Run all repository gates, rebuild the five runtime binaries, drain/restart through the supervisor, and repeat storage/channel/API health checks.
- Replay the exact CRWV/NVIDIA production query with a fresh actor. Record the first committed `assistant_delta` time separately from `run_finished`, along with tool/entity coverage, no reset/error/refusal, one terminal, exact history equality, and zero active chats. No production latency improvement is claimed until this evidence is added.

## Phase 6 — Isolated Research Control And Native Stream Completion

> Historical design note: Phase 8 and ADR 0004 supersede the isolated control completion and every control-error-to-degraded-terminal transition below. The provider lifecycle, current-turn terminal evidence boundary, tool-free terminal, committed-prefix recovery, and exact visible/persisted body remain active.

### Reopened Production Symptom And Root Cause

The latest user turn `crwv和英伟达有什么关系` returned one verified CRWV quote followed by the fixed “投研完整性检查” refusal, with a brief error state before history restore. A fresh exact replay on the then-current `022981f2` runtime proved that this was neither a stale binary nor an FMP/DataFetch outage. Search, quote/profile, financial, news, and relationship evidence calls completed; four evidence rounds consumed about 17 seconds. The configured OpenAI-compatible provider then ignored the requested required-tool behavior, spent about 46 seconds producing a 4,618-character direct draft, and the prior Agent loop accepted that tool-capable round as final content.

The direct draft was not safe merely because it was long. It mixed unverified CRWV/NVIDIA customer/GPU/contract claims with mislabeled market-cap, revenue, backlog, P/E, EV, annualization, EBITDA, target-price, probability, position-size, stop, and support assertions. Post-run prose inspection could only turn that problem into another brittle refusal/rewrite loop. The fix therefore changes the Agent protocol rather than adding another company, phrase, or Markdown checker.

### Architecture Correction

- Every nonempty Interactive request remains open-ended and sees actor-bound tools with `Auto`. The finance protocol activates only when the current turn actually attempts DataFetch, the structural current-evidence step required by the investment prompt; an unrelated Web/file/skill tool therefore stays in the ordinary Agent loop. After DataFetch, research enters an alternating state machine inside the same `Agent::run` and `AgentContext`.
- A silent control completion sees only ephemeral `continue_research` and `finish_research` schemas. A continue decision is followed by a bounded business completion that sees only business tools; the control schemas never enter the registry, tool budget, observer, persisted `ToolCallMade`, user stream, or model audit body. A finish decision enters terminal synthesis. Provider content-only bypass, timeout, or lifecycle error does not become a refusal: it enters a degraded terminal that answers normally from earlier successful tool evidence and discloses real gaps.
- Provider streams now expose `ToolChoiceMetadata { requested, effective, fallback }`, a typed finish reason, and an explicit `Done`. Every Agent phase validates the sequence and expected finish reason. Generic OpenAI-compatible and OpenRouter adapters retry exactly once with `Auto` only for a narrow 400/422 rejection that explicitly identifies unsupported `tool_choice=required`; the classifier reads only explicit provider error/detail fields, never an echoed request body. Auth, rate-limit, 5xx, transport, unrelated validation failures, and invalid-model errors therefore do not silently downgrade.
- Terminal synthesis receives only this run's user message, assistant tool calls, and accumulated tool results with tools removed. Earlier conversation remains available to initial/control/business rounds for follow-up understanding, but stale assistant prose, ticker rows, prices, and tool results are structurally excluded from terminal fact evidence. At the terminal boundary, content from every assistant message that also carries tool calls is blanked—even if an earlier Web round preceded DataFetch—while its real tool result remains. Hidden reasoning, discarded drafts, control text, and model memory are likewise non-evidence; terminal reasoning is not persisted into the next turn. The Agent must use the existing time-first answer shape, distinguish identity evidence from relationship/event evidence, label FY/quarter/TTM and valuation inputs honestly, and continue with supported analysis when a field is missing rather than refuse the whole question.
- A complete canonical first line may be committed as soon as the tool-free terminal emits it. If that same terminal transport later fails, the already-visible prefix prevents an outer Agent retry. Exactly one terminal-only recovery may instead run fully buffered against the same messages and no tools. Its result must begin byte-for-byte with the committed prefix and contain a nonempty tail; the Session emits only that tail. Recovery never restreams, resets, reruns DataFetch/business tools, or publishes internal error copy, and it has a separate audit operation.
- If both terminal attempts fail after the prefix crossed the user boundary, the outcome is explicitly partial rather than falsely successful. `AgentSessionResult` stays failed for callers; public OpenAI streaming ends with `finish_reason=error`, non-streaming returns its sanitized failure response, and neither path gets a second internal-error content chunk. Browser SSE receives `run_finished { success:false, partial:true }`, retains the exact already-visible prefix without an error card, and refresh reads the same bytes. Persistence preserves the trailing newline and records `run_failed=true` plus `terminal_stream_incomplete=true`; the underlying diagnostic remains in logs/audit.
- Interactive post-run entity reconstruction remains observational only and returns the Agent body unchanged. The old fixed refusal and strict repair/fallback code remains scoped to caller-supplied typed scheduled/heartbeat contracts; an open-ended Web Agent request cannot enter it.

### Focused Verification

- `cargo test -p hone-agent --lib`: 36 passed. Coverage includes DataFetch-only protocol activation, a non-finance Web-tool counterexample, isolated continue/finish decisions, stale NBIS transcript removal from the CRWV/NVDA terminal, pre-DataFetch Web/tool-draft removal with both real tool results retained, provider preamble followed by a valid control tool, Required fallback telemetry, control/business timeout and content-bypass terminal reasons, strict finish/DONE handling, terminal reasoning/inline-think removal, header-only/duplicate-prefix rejection, successful one-shot terminal recovery, and exact-one recovery failure.
- `cargo test -p hone-llm --lib`: 27 passed. Coverage includes top-level SSE errors, explicit DONE, missing-DONE detection, empty-tools request normalization, narrow same-client `Required → Auto` fallback for both provider adapters, and JSON detail-array request-echo negatives.
- `cargo test -p hone-channels --lib`: 631 passed. The Session recovery integration proves four provider calls (business, control, failed terminal, one recovery), one exact visible header plus recovered tail, no duplicate header, no reset/error, and byte-identical persistence. A separate double-failure integration proves one committed header, one honest `PartialDone` and failed caller result, no ordinary `Done`/Reset/Error, exact persistence including the trailing newline, and durable incomplete-terminal metadata without internal copy.
- Focused `hone-web-api` tests prove Browser `partial=true` presentation does not flash an error, public OpenAI ignores `PartialDone` as answer/success, failed results use an error finish, and shared failure formatting cannot expose terminal/provider protocol detail. `packages/app/src/pages/chat.test.ts` passes 48/48 including partial-versus-failure UI state.
- `bash tests/regression/ci/test_finance_automation_contracts.sh`: 32/32. The architecture contract locks isolated control preamble handling, current-turn/tool-result-only terminal evidence, narrow provider fallback, committed-prefix recovery/honest partial close, Runner prefix introspection, suffix-only Session publication, Browser partial presentation, public API sanitization/failure semantics, and explicit CLI/TypeScript consumption of `PartialDone` without calling it success.
- `cargo check --workspace --all-targets --exclude hone-desktop --exclude hone-user-app` and `bun run typecheck:web` pass. The workspace check caught and closed the initially omitted CLI probe enum branch before release; only the pre-existing unrelated Feishu dead-code warning remains.

### Deployment / Acceptance Still Required

- Run the complete repository gates from a clean detached worktree at the exact repair commit; do not include the unrelated public-community work currently present in the shared working tree.
- Rebuild the five runtime binaries from that exact clean commit, wait for zero active chats, stop only the supervisor with SIGINT, start the replacement runtime, and recheck PG/S3, ports, auth boundaries, process uniqueness, and zero active chats.
- Replay fresh actors for `crwv和英伟达有什么关系`, `crwv和英伟达什么关系，估值怎么看`, and a non-finance control. Acceptance requires one run/answer/terminal, exact visible/persisted equality, current relationship evidence, no unsupported valuation labels or trade prescriptions, no fixed refusal/internal copy, and materially better first-visible timing than the 72.9-second pre-fix replay. This handoff stays `in_progress` until those production results are recorded.

## Phase 7 — MiniMax Clean-EOF Compatibility

The exact `2d02621c` build passed all detached-worktree gates and was deployed from `target/deploy-2d02621c` after active chats reached zero. Backend, Discord, Feishu, ports, PG/S3 metadata, auth boundaries, and process uniqueness were healthy. A fresh exact production actor then failed in the initial tool-capable model stream after 7.019 seconds, before DataFetch or FMP executed, with the internal diagnostic `chat_with_tools stream ended before Done`. Its Browser stream contained one start, one sanitized failure, and one failed terminal—no second answer—so deployment was current but the business canary correctly failed.

Direct capture against the effective `main → minimax(openai_compatible) → MiniMax-M2.7-highspeed` route showed the missing protocol variant: MiniMax returned the complete final tool-argument fragment together with `finish_reason:"tool_calls"`, then closed the chunked HTTP/SSE response cleanly without sending `data: [DONE]`. The production audit's `provider=openrouter` label is a separate hard-coded audit-label defect; startup/effective config and raw-wire evidence identify MiniMax. The injected first request already contained DataFetch and Web tools, so this incident is not an FMP outage, entity-recognition failure, or missing tool registration.

The repair stays at the generic provider adapter boundary. `reject_incomplete_sse_framing` tracks raw CR/LF event boundaries so the third-party parser cannot silently discard a truncated final frame; `normalize_clean_eof_after_finish` emits one internal `Done` only when that framed lower stream reaches error-free EOF after exactly one typed finish and no earlier explicit Done. Literal `[DONE]` terminates once; post-finish content/reasoning/tool payload, EOF without finish, duplicate finish, residual framing, and parser/transport error—including an error after finish—never synthesize completion. `FunctionCallingAgent::require_complete_stream` remains strict, and the dedicated OpenRouter adapter keeps its explicit-sentinel behavior. Regression coverage includes the exact MiniMax fragmented tool/finish shape, normal stop completion, literal-DONE deduplication, no-finish EOF, duplicate finish, finish-then-transport-error, finish-then-truncated-tail, post-finish payload, and CRLF split across chunks. Focused `hone-llm` tests pass 35/35 and the architecture contract passes 33/33.

Follow-up commit `1365b31b` passed rustfmt, full workspace check/test, Web typecheck, all 267 Web tests, and every CI-safe regression; it was pushed, built in an isolated target, and gracefully deployed after active chats reached zero. Backend, Discord, Feishu, ports, PG/S3 metadata, auth boundaries, and active-run health passed. The first fresh production canary then completed with one start, one answer, one successful terminal, no error/reset/second answer, and byte-identical visible/persisted content. This closes the MiniMax clean-EOF transport incident. The canary's research quality failure reopens the Agent-loop architecture separately in Phase 8.

## Phase 8 — One Agent-Owned Business And Finish Loop

### Production Evidence And Root Cause

The exact post-deployment canary `crwv和英伟达有什么关系` proved that the runtime was current and DataFetch identity lookup worked, but it did not prove a good answer. The Agent called only `data_fetch(search)` for CRWV and NVIDIA. It never requested a quote/profile or relationship Web/news source. The isolated control request then completed with an error-labelled `implicit_finish`, which ADR 0003 mapped into a degraded terminal. Terminal synthesis asserted CRWV/NVIDIA relationship, concentration, customer, and infrastructure details that were not present in the current-turn tool results.

The full first-visible latency was about 44.16 seconds: roughly 6.28 seconds in the initial model call, 1.57 seconds in DataFetch, 7.14 seconds in the failed isolated control request, and 28.46 seconds in terminal generation. The transport emitted one clean answer, so the remaining defect is neither a missing deploy, MiniMax EOF, FMP/DataFetch outage, nor frontend double-run. A separate read-only `diagnose_fmp_tavily` run against production config returned healthy CRWV FMP coverage (1/1, about 1.204 seconds, one record) and a healthy Tavily relationship query (1/1, about 1.917 seconds, one result). No credential or quota failure was present. The root cause is the architectural separation between the Agent that chooses evidence and the completion that decides whether research is finished.

### Superseding Architecture

- ADR 0004 replaces the isolated control state machine with one active Agent business-tool loop in the same `Agent::run` and context. There is no `continue_research` completion and no implicit/degraded finish.
- A request-local evidence ledger classifies `data_fetch(search)` as `identity_only` and records post-search quote/profile, crypto, and broad-market routes. It guides the Agent and produces audit telemetry; it is not publication authority and cannot reject a complete natural answer.
- After the first accepted active business-tool follow-up, `finish_research` appears alongside the same actor-bound business tools. Only exactly one sole call whose arguments parse as an empty JSON object enters the empty-tools terminal. Mixed finish/business calls execute the business calls; duplicate, malformed, nonempty-argument, or unavailable finish signals do not transition or enter registry/budget/observer/persistence.
- After the first accepted active business-tool follow-up, `finish_research` appears alongside the business tools under `Auto`. Any complete nonempty natural-language body with provider `Stop + Done` is the same Agent's DirectFinal regardless of advisory-ledger coverage. It publishes through the ordinary deferred Interactive boundary without retry, rejection, replacement, terminal synthesis, or a second model generation. Empty response, provider error, timeout, lifecycle failure, protocol error, or iteration limit remain genuine failed runs and cannot authorize terminal or DirectFinal success. Genuine no-coverage narrows the Agent's claims; it does not create a service-authored completeness refusal.
- The ledger is a structural diagnostic, not a semantic checker or completion minimum. The complete query and Agent prompt drive question-specific evidence: a relationship query requires exact quote/profile evidence for CRWV and NVDA plus a current Web/news/filing source for the claimed relationship, and a valuation query needs the inputs used by its valuation frame. “Not found” does not prove “does not exist”; negative equity/customer/contract/relationship claims require explicit evidence or must be phrased only as a bounded current-search result.
- Interactive post-run reconstruction remains audit-only. The dedicated Interactive finalizer removes only leaked system/internal protocol material and unsafe local paths, then stabilizes media references; it cannot normalize market copy, veto planning-like prose, rebuild an answer from tool outcomes, run typed completeness validation, retry, repair, refuse, replace, or append failure copy. The exact finalized body emitted to the user is persisted byte-for-byte. The tool-free terminal, current-run-only evidence slice, provider finish/DONE rules, canonical-prefix commit, exact suffix, terminal-only recovery, and honest partial failure remain unchanged.

### Verification / Next Entry Point

- Update Agent and finance-contract regressions to prove the ledger distinguishes search-only, Web-only, quote/profile, crypto, and broad-market routes without becoming a publication veto; eligible finish and business tools share one completion; only valid sole `finish_research({})` enters terminal; every complete natural DirectFinal uses no retry, terminal/second generation, refusal, or replacement; and malformed/mixed/empty/error/timeout paths never enter terminal.
- Add behavioral coverage for the exact CRWV/NVIDIA relationship query: identity search must be followed by exact quote/profile for both entities plus current Web/news/filing relationship evidence before synthesis, and the answer must not infer nonexistence, ownership, customer concentration, contracts, or GPU deployment from identity rows.
- Prove the Interactive publication boundary end-to-end: legacy market-copy normalization, planning-sentence vetoes, tool-result reconstruction, and typed completeness repair/fallback are bypassed; the one visible finalized body, returned response, and persisted assistant body are identical.
- Run the full repository gates on the exact implementation commit, build from that exact commit, wait for zero active chats, restart only the runtime supervisor, and repeat PG/S3/ports/auth/process/active-run health checks.
- Replay fresh production actors for `crwv和英伟达有什么关系`, `crwv和英伟达什么关系，估值怎么看`, and a non-finance query. Acceptance requires one answer and one public terminal event, no reset/error/refusal, exact visible/persisted content, current relationship and valuation evidence where asked, no unsupported negative/concentration/contract/GPU assertions, and first-visible timing recorded separately from completion.
- This handoff and the umbrella ticker-resolution plan remain `in_progress`; do not archive them until these canaries pass and the separately tracked scheduler entity P2 is resolved.

## Phase 9 — Shared Last-Mile Contract After The First ADR 0004 Canary

### What The Deployed Canary Proved

Commit `0cd03abb` passed the complete repository gates, was pushed, built into an exact-commit target directory, and gracefully deployed after active chats reached zero. Backend, Discord, Feishu, ports, PG/S3 metadata, auth boundaries, process uniqueness, and active-run health passed. Fresh actor `codex-canary-0cd03abb-rel` then asked `crwv和英伟达有什么关系`. The run emitted one start, one `assistant_delta`, and one successful finish with no reset, error, fixed refusal, second answer, or second generation; visible and persisted content matched, history was exactly one user/assistant pair, active chats returned to zero, and first/final output arrived at about 42.434 seconds.

That was a transport and publication pass, not a content pass. Five Agent calls searched CRWV/NVIDIA, ran one `web_search`, fetched batched CRWV/NVDA quotes, and produced DirectFinal. FMP/DataFetch and Tavily returned successfully. The answer nevertheless opened with the quote-provider timestamp `2026-07-18 04:00` as its data time instead of the current Session query time, never called profile, and expanded secondary snippets into unsupported customer-rank and supply-priority claims. `web_search` had been enough to increment the generic post-identity counter and expose DirectFinal. Explicit terminal synthesis received a strong final user contract, while DirectFinal saw only an earlier system reminder after a long tool transcript.

### Follow-Up Architecture

- DirectFinal and explicit terminal synthesis now share `FINAL_ANSWER_EVIDENCE_CONTRACT`, delivered as the last ephemeral user message. Both receive the exact literal prefix already generated for this turn rather than a `YYYY-MM-DD HH:MM` placeholder.
- `PromptBundle` performs one Beijing clock read and stores `answer_time_beijing`; `Session 上下文`, Interactive answer guidance, Agent DirectFinal, and explicit terminal therefore use one query-time anchor. Quote provider timestamps may appear only inside `行情口径`.
- Active finance requests use the current-turn message boundary, excluding stale prior ticker/price history. Assistant prose beside tool calls is blanked. MiniMax/Mimo reasoning fields remain in live tool-followup requests for provider compatibility but are explicitly non-evidence; the tool-free terminal still strips them.
- The structural ledger records whether exact quote plus profile/asset-route attempts cover the symbols observed after search, including crypto and broad-market exceptions. That state drives the last-mile prompt and audit only. It never blocks DirectFinal, retries a complete body, or generates fixed failure copy; genuine provider no-coverage is disclosed and the Agent answers with the supported subset.
- The first accepted active business-tool follow-up exposes `finish_research` beside the same business tools and switches the request to `Auto`. The prompt prefers that sole signal once research is complete so the canonical terminal header can stream without buffering an entire long DirectFinal, while a provider's natural DirectFinal remains fully valid and untouched. This removes the unnecessary long `Required` generation path while leaving evidence selection and finish authority with the Agent.
- Context-overflow recovery reuses the original structured Session clock instead of taking a second timestamp. The final evidence boundary keeps up to three bounded prior user requests for pronoun/analysis-method continuity, but excludes prior assistant/tool facts. MiniMax/Mimo reasoning signatures remain available only where the provider needs them for tool-result replay and are explicitly non-evidence/non-persistent.
- The strict FunctionCalling runner now consumes `AgentRunnerRequest.timeout` (or its configured overall fallback) as one absolute deadline created once at Agent entry and also honors the converged configured step timeout. Initial discovery, business calls, tool execution, observer callbacks, terminal, and recovery share the same remaining overall budget; every individual await is step-bounded. Tool execution failure is recorded as a failed `ToolCallMade`; timeout records `timeout=true`. A shared `hone-core` side-effect classifier stops an uncertain persistent failure inside the same Agent loop before the model can replay it, while read-only DataFetch/Web failures remain available to the Agent for honest disclosure. Failed progress now emits `failed / 执行失败`, never a transient “执行完成”.
- This is still an Agent protocol, not a service semantic interceptor. No output keyword filter, company-specific classifier, fixed refusal, post-run repair, market-copy rewrite, or second model generation was added.
- `web_search` now tells the model its real evidence boundary: basic search, at most three titles/URLs/snippets, no page body. The shared contract forbids expanding relation labels or transaction facts into unstated rights/duties, rank, importance, exclusivity, guarantees, priority, concentration, or roles, and prefers primary sources when those claims matter.

### Regression And Remaining Acceptance

- `cargo test -p hone-agent --lib`: 53/53 pass. Coverage includes advisory search/quote/profile/crypto/broad routes, natural DirectFinal before the advisory floor with no service veto, an empty `Auto` completion followed by one preserved DirectFinal, weak relationship snippets in both completion paths, stable prior-intent handling, no terminal/second generation for DirectFinal, step and absolute overall deadlines on the initial model stream, configured step precedence over the legacy active fallback, a bounded hanging observer, one failed/timeout trace for a hanging persistent tool, and no same-loop replay after an uncertain write error.
- `cargo test -p hone-channels --lib`: 642/642 pass, including Interactive contract bypass, exact one-body emission/persistence, context-overflow stable time, committed-prefix recovery/double-failure, no-coverage, ambiguity, omitted-seed cases, and distinct success/failure tool progress.
- `cargo test -p hone-tools web_search --lib`: 16/16 pass, including the truthful title/URL/snippet-only capability description.
- `bash tests/regression/ci/test_finance_automation_contracts.sh`: 37/37 pass, including the advisory-ledger/no-veto boundary, one-clock overflow-stable time anchor, shared last mile, weak-source examples, current-turn DirectFinal boundary, actual Web capability, continued prohibition on Interactive semantic rewriting, step/request-timeout wiring, one absolute overall deadline, bounded observers, shared write-effect classification, failed progress, and failed/timeout `ToolCallMade` retention.
- Next: run all repository gates, commit/push, build the exact new commit, wait for zero active chats, restart only the runtime supervisor, repeat health checks, and replay fresh relationship, relationship+valuation, and non-finance canaries. Acceptance requires exact current Session time on line one, separate quote-source time, quote/profile coverage for CRWV and NVDA, source-bounded relationship claims, no unsupported rank/guarantee/priority/role expansion, one answer/terminal, no reset/error/refusal, exact visible/persisted bytes, active chats back to zero, and TTFT recorded against the 42.434-second baseline.

### First Follow-Up Canary And Active-Deadline Correction

Exact commit `d64daf24` passed the full gates, was pushed, built, and gracefully deployed after zero active chats. Runtime/channel/PG/OSS/ports/auth health passed, and direct provider diagnosis reported FMP 1/1 healthy (CRWV record in about 1.174 seconds) plus Tavily 1/1 healthy (relationship result in about 2.321 seconds). Fresh relationship canary `codex-canary-d64daf24-rel-*` then failed at about 39.3 seconds: it had already completed CRWV/NVIDIA search, relationship Web evidence, and CRWV quote, but the next active model phase hit `active business stream timed out`. The Browser emitted one technical `run_error` and failed terminal; there was no fixed investment-completeness rewrite. Root cause was the legacy hard 20-second active-business cap taking `min` precedence over the newly wired configured 180-second step deadline.

The correction makes that 20-second value fallback-only for direct Agent constructions that supply neither step nor overall deadline. Production FunctionCalling always receives the converged configured step/overall pair, so every active model phase now uses the same 180-second step semantics as other runner awaits while the one 1200-second absolute overall budget still never resets. A regression explicitly proves a configured 100ms step is not shortened to the 25ms test fallback. The failed canary is not acceptance; rebuild, restart, and all three fresh canaries remain required.

## Phase 10 — Per-Entity Evidence Routes Before Explicit Finish

### `c7c5741f` Content Canary Failure

The timeout correction was built and deployed as `c7c5741f`. A fresh exact `crwv和英伟达有什么关系` canary emitted one `assistant_delta` and one successful finish with no reset, error, second answer, or fixed completeness refusal; first and final output arrived at about 48.54 seconds. That transport pass still failed content acceptance. The Agent independently searched CRWV and NVIDIA, but fetched quote/profile only for CRWV before calling `finish_research`. The structural ledger derived its floor from symbols already present in quote/profile attempts, so the successful NVIDIA candidate route was invisible to the finish decision.

The terminal answer then described CoreWeave as one of NVIDIA's largest buyers, a core customer and preferred partner, asserted no equity relationship, and supplied chip-model, multi-billion contract, and party-role details that the current Web titles/snippets did not directly establish. FMP, Tavily, streaming, and deployment were healthy; the defect was the Agent's pre-finish entity-route accounting plus over-expansion of weak relationship sources. Adding a post-generation semantic interceptor would only recreate the previously removed refusal/rewrite failure class.

### Architecture Refinement

- The same Agent that reads the complete query now assigns every named security a stable, distinct, case-sensitive `entity_route`, then reuses that key verbatim on the security's search, refinement, quote, profile/snapshot, and later DataFetch calls. Every search carries a call-scoped `identity_match`; an old declaration cannot authorize a malformed later search. Any parseable explicit route mention immediately registers that route as pending, but it cannot preload route evidence before its own valid search and result. The request-local ledger retains independent current candidate and evidence state per route. Before explicit finish appears, every nonempty route needs one same selected symbol in its route-bound quote and profile/asset-route evidence. CRWV coverage cannot satisfy NVIDIA, a quote for one candidate plus a profile for another cannot form a pair, and overlapping provider result sets do not merge routes.
- The service no longer tries to recover missing routes by parsing natural-language separators, ticker shape, or positionally assigning provider rows. It does not split commas, `and`, `和`, `&`, `/`, or share-class wording, avoiding false routes for AT&T, S&P Global, M&T Bank, H&R Block, BRK/B, and `Berkshire Hathaway, Class B`. Agent-declared `exact_symbol` searches enforce a persistent same-code constraint, so even a result set containing only `CWY / ... CRWV ETF` leaves the CRWV route empty; `name_or_alias` searches use query grounding. A later successful search replaces earlier empty/noisy candidates and resets the current empty streak. Legacy calls without `entity_route` stay as active provisional routes and are retired only when an explicit search selects one case-sensitive verbatim source through the actual `query`, `refines_query`, or `supersedes_query`; the latter two are strictly mutually exclusive. Migration retains only search/empty attempt history, verbatim aliases, and a previously Agent-declared exact constraint—never candidates, quote symbols, profile/asset-route symbols, or post attempts. DataFetch execution and ledger observation share the same effective field precedence, JSON-type handling, symbol batch validation, and search-query validation. One partially tagged entity cannot globally hide the others. A route still empty after two current consecutive unsuccessful searches additionally needs a real route-correct attempt after the latest empty result before bounded disclosure.
- Iterative independent pre-deploy review rejected the earlier counter-only and query-fragment drafts. Those versions allowed unrelated empty-route counters to cancel, Web-only bypass, self-labelled refinement, missing fragments, overlapping candidates, or legal company names/share classes to be mis-split. The final design removes the fragment parser entirely and regression-locks Agent-declared route identity, exact-ticker-only selection, wrong-product rejection, overlap/noise isolation, same-route replacement, partial-tag compatibility, and per-route bounded no-coverage. If the Agent omits an entity or reuses one route for two entities, that remains an explicit Agent semantic trust boundary; it must be caught by prompt/behavioral canaries and never by a post-generation refusal or rewrite.
- This ledger is a pre-finish control structure only. It decides whether `finish_research` is included and whether the business completion advances from `Required` guidance to business-tools-plus-finish `Auto`. It is never consulted when a provider returns a complete nonempty `Stop + Done` DirectFinal, and it never participates in Interactive publication, persistence, retry, rejection, repair, or replacement. `AgentSession` retains the same security-only finalizer and byte-identical visible/persisted body contract.
- Both evidence-pending and eligible DirectFinal reminders, plus the explicit terminal prompt, now carry a relationship-claim deletion self-check. The Agent must delete any statement not directly supported by current source text/snippets; generic titles, cooperation labels, purchase facts, or model memory cannot establish largest/one-of-largest, preferred/core-customer status, equity ownership or its absence, cross-holdings, exclusivity, guarantees/priority, concentration, chip models, contract values, or party roles. This is Agent prompt guidance, not a service-side keyword filter, semantic validator, rewrite, or refusal.

### Local Verification And Remaining Acceptance

- Pre-deployment verification passes after the final Agent-declared match, pending-route, single-source provisional-migration, shared-parser, and current-generation empty-route fixes: Agent 72/72, hone-core 120/120, hone-tools 141 passed with one optional-dependency test ignored, channels 642/642, Web-search 16/16, finance static contracts 39/39, Web 267/267, the complete workspace Rust test command, formatting/diff checks, and the complete CI-safe runner. These regressions cover route isolation, CRWV-only-CWY rejection, Ford/FORD case-sensitive query and route separation, mixed explicit/implicit preservation, mutually exclusive exact-text migration, malformed metadata rejection, migration with no candidate/quote/profile/post inheritance, candidate-generation reset, executor/ledger target parity, same-symbol quote/profile intersection, bounded current-generation empty routes, unchanged DirectFinal acceptance, DataFetch schema compatibility, and channel no-refusal/no-replay behavior.
- The latest repair has not yet been committed as an exact deploy artifact, restarted into production, or accepted by a production canary. Remaining work is the exact-commit build, zero-active-chat graceful supervisor restart, PG/OSS/channel/port/auth/process health checks, and fresh `crwv和英伟达有什么关系`, relationship-plus-valuation, and non-finance replays. Acceptance still requires separate CRWV/NVDA search plus quote/profile coverage, current relationship evidence, no unsupported rank/ownership/contract/chip/role claims, exact Session time before quote basis, one answer/terminal with no reset/error/refusal, byte-identical visible/persisted text, active chats back to zero, and TTFT/final timing recorded. This handoff remains `in_progress` and is not archived.

## Phase 11 — Evidence-Only Tool Rounds And Snippet Claim Provenance

### `ac2468f9` Canary: Transport Passed, Content Failed

The Phase 10 implementation was committed and pushed as `ac2468f9`, built with an exact `soul.md` plus 27-skill runtime snapshot, and gracefully deployed without touching the unrelated dirty shared checkout. Fresh actor `codex-canary-ac2468f9-rel-1784427584` asked the exact `crwv和英伟达有什么关系`. SSE contained one `run_started`, one `assistant_delta`, one successful `run_finished`, eight progress events, and eight tool events, with no reset, error, fixed refusal, second answer, or visible/history mismatch. Active chats returned to zero and the only visible answer arrived in 46.300993 seconds.

That was not content acceptance. The actual Agent sequence was CRWV DataFetch search, CRWV quote, NVIDIA search, and one Tavily relationship query, followed by DirectFinal. It omitted CRWV profile and both NVDA quote/profile. The answer expanded limited search snippets into unsupported largest/core-customer ranking, MFN supply priority, H100/H200/GB200 details, and Forward-PE language. Direct production diagnosis returned healthy FMP 1/1 in about 1.198 seconds and Tavily 1/1 in about 2.000 seconds. The failure was therefore contradictory tool-round prompting and weak snippet provenance—not stale deployment, FMP/Tavily outage, credentials, quota, frontend replay, or an absent service-side ban.

### Architecture Correction

- The first Agent instruction now asks it to reread the complete current query and, if securities/companies are named, return only separate DataFetch search calls for every named entity, each with its own stable `entity_route` and call-scoped `identity_match`. Non-finance turns remain open and are not forced into DataFetch.
- Once finance activates, a route-incomplete `Required` round can return only real business tools. A structurally eligible `Auto` round can return only more tools or sole `finish_research({})`; no tool-enabled prompt contains the old invitation that natural prose will be accepted, a terminal time prefix, or a draft/final-answer request. The following no-tool completion is the normal unique final. Code still accepts any unexpected complete `Stop + Done` DirectFinal unchanged as a compatibility/no-veto fallback, proving this is Agent guidance rather than a service interceptor.
- `ResearchEvidenceLedger::agent_guidance_summary()` renders JSON-safe concrete state per raw Agent route: provisional/missing search, candidates, missing same-candidate quote, missing profile/snapshot or crypto quote, mismatched quote/profile symbols, covered structural attempts, and bounded no-coverage attempts. It never echoes internal `route:` prefixes or claims that a call succeeded, the entity set is complete, or the question's semantic evidence is sufficient.
- Successful Tavily output is capped again to three results and receives a locally trusted `hone_search_contract` plus per-result `hone_evidence`. The contract marks basic snippets, no full-page body, same-result citation scope, non-citable missing URLs, and that search score/order is not a real-world rank; provider-spoofed metadata is overwritten. The tool-free terminal now requires sentence-level claim provenance: each relationship fact must be directly stated by the same current result and cite its title plus original URL inline; a URL alone proves nothing, extra judgment begins a separate `推断：` sentence, and unsupported detail is removed.
- `soul.md`, the Interactive runtime suffix, the function-calling system instructions, and `stock_research` now express the same tool-only/sole-finish/claim-provenance workflow. No final-answer keyword parser, semantic gate, retry, repair, rewrite, or fixed refusal was added.

### Verification And Remaining Acceptance

- Focused results: Agent 74/74; hone-tools 143 passed with one optional `matplotlib` test ignored; hone-channels 642/642; finance contracts 39/39; workspace `cargo check` passed. The first full workspace test run caught `soul.md` exceeding the reviewed 12,000-character full-prompt budget; the new text was compressed without dropping the restored large prompt, and the focused budget test now passes exactly at the enforced ceiling.
- The full workspace test rerun, Web 267/267, complete CI-safe regression, and final format/diff checks pass. Exact commit/build/deploy, health checks, and the three fresh production canaries remain pending. Do not call this phase accepted or archive the plan until the relationship, relationship-plus-valuation, and non-finance probes all meet the one-output, current-evidence, no-unsupported-claim, exact-visible/history, and zero-active-chat criteria.

## Phase 12 — Tolerant Structured Evidence Handoff And One-Shot Browser Submission

> Historical phase, refined by Phase 13 / `D-2026-07-19-07`: selected-scope fallback and terminal isolation remain active, but unresolved locators now bind to a real current-turn source catalog and receive at most one hidden pre-terminal correction; repeated all-invalid evidence no longer authorizes an empty-evidence terminal.

### `3d2474b5` Canary: Clean Transport, Unsupported Terminal Claims

Exact production build `3d2474b5` was current and healthy when a fresh actor asked `crwv和英伟达有什么关系`. Browser SSE contained one run start, one visible answer, and one successful finish with no reset, error, fixed completeness refusal, or second output; persistence was byte-identical and active chats returned to zero. FMP and Tavily were healthy. This rules out stale deployment, quote/search outage, and post-run answer rewriting.

The Agent still finished after one broad relationship search. The three basic snippets supported only a preferred-CSP label, NVIDIA-platform AI factories, and a 5GW collaboration target, yet terminal prose added unsupported customer rank, specific GPU models, bargaining-power claims, and “no equity relationship”. Audit confirmed that the terminal had the full tool transcript. The remaining root cause was the empty `finish_research({})` control: it transferred finish authority without requiring the same Agent to identify the precise current-turn source locations it intended to use. Raw transcript availability did not create a reliable claim boundary.

The same incident review found a separate browser replay/flicker path. Shared `apiFetch` retried transient failures for all HTTP methods, including `POST /api/public/chat`; if the first request had reached the server, the retry could create a duplicate submission. A non-Abort stream exception also marked the assistant card as error before bootstrap/history recovery ran, so a recoverable disconnect could briefly flash an error and then return to thinking.

### Architecture Correction

- The eligible tool round now submits one logical finish-only structured `finish_research` handoff with `answer_scope`, `facts`, `inferences`, and `gaps`. The prompt asks for one call, while provider-duplicated finish controls in the same round are tolerated: the runtime uses the first parseable handoff. If none validate, it enters the terminal once using only the first JSON-readable nonempty scope recovered from fixed `facts[*].evidence[*]` paths; syntax-broken/truncated controls contribute no evidence and do not call the research model again. A fact contains no Agent-authored claim: it is only a stable id plus current-turn evidence references. Web references carry `tool_call_id`, 1-based result index, and an exact excerpt; the runtime injects the actual title and URL. DataFetch references carry `tool_call_id` plus one scalar JSON Pointer. If any real business call appears in the same round, all finish controls are ignored and only the business calls execute.
- Evidence handling is deliberately tolerant. The runtime resolves every reference independently by current-turn tool id, result index or JSON Pointer, exact excerpt, and bounded size. A bad/stale/fabricated reference is omitted or annotated; valid sibling references survive. A bad reference never rejects the entire handoff, forces another Agent loop, creates a public protocol error, or becomes a whole-answer refusal.
- Valid resolved facts remain intact. Fallback is limited to the Agent-selected locator: exactly one referenced Web result or the exact direct DataFetch object around a selected scalar/missing leaf. DataFetch search candidates, missing current-turn invocations, unselected same-turn NBIS/CWY results, neighboring batched entities/financial periods, root/array/null targets, and error paths cannot enter terminal evidence. Duplicate invalid handoffs never union scopes, and a gaps-only handoff imports nothing. This preserves local protocol tolerance without introducing a whole-turn scanner, semantic selector, source ranking, generated claim, or completeness judgment.
- The no-tool terminal is reconstructed from the current system contract, current user request, exact Session-time anchor, and resolved evidence/inferences/gaps only. It does not receive the raw tool transcript, prior assistant/tool rows, older user evidence, tool-round prose, discarded drafts, or provider reasoning. The same Agent owns final relevance and wording.
- This is a pre-terminal evidence transport protocol, not an output interceptor. Because facts contain no free-text claim, Hone only locates source bytes/fields; it never semantically approves a proposition, parses the final answer for completeness, retries based on content, rewrites a conclusion, appends a warning/refusal, or rejects an otherwise successful Interactive body. Complete provider `Stop + Done` DirectFinal remains an unchanged no-veto compatibility fallback.
- The Web API helper now retries only idempotent GET/HEAD requests. Public-chat POST and every other non-idempotent method are one-shot on transient status and transport failure. A non-Abort SSE interruption keeps the same assistant card pending and attempts authoritative bootstrap/history recovery first; an error appears only after recovery is exhausted. Explicit abort/stop still closes immediately. Refresh/recovery preserves the server-owned original run start and never replays POST. A restore captures sync/send generations; a newer local send advances both, aborts the old restore, and clears its retry before creating the optimistic pair, so stale bootstrap reconciliation cannot delete the next assistant card or its first deltas.

### Verification And Remaining Acceptance

- The updated finance static contract passes `39/39`. Cases 28, 31, 34, and 38 lock finish-only provider duplication, fixed-path malformed recovery without another research-model loop, mixed business/finish isolation, current-turn-only terminal input, selected-result/object fallback, and the structured relationship deletion constraint.
- Focused regression combines valid and invalid Web/JSON-Pointer references in one handoff, proves valid evidence survives, proves no bad-reference completion loop or public error occurs, and proves the schema contains no free-text fact claim or model-supplied URL. Adversarial cases exclude DataFetch search candidates and results without matching invocations; gaps-only and syntax-broken controls scan nothing; invalid Web excerpts recover only the selected result; batched quote/financial fallback cannot cross the selected object/period; array, null, root, and error pointers cannot widen to a parent; duplicated invalid handoffs never union scopes; and a same-turn unselected NBIS result cannot contaminate CRWV/NVDA. A realistic global-cap test combines CRWV/CWY and NBIS/NBIX search candidates, batched quotes, duplicate profiles, four FY periods for both companies, and two Web result sets: search candidates are excluded while both explicitly selected companies' market cap/PE and latest date/period/revenue/EBITDA survive the 64-item bound. Existing DirectFinal no-veto, one-output, exact visible/persisted, prefix recovery, and runtime-failure boundaries also pass.
- Frontend regression proves GET/HEAD retry, POST one-shot behavior for both transient response and transport failure, non-Abort recovery-first state, explicit abort immediate closure, exhausted-recovery error, original server elapsed time after refresh, late-frame rejection after the unique terminal, and rejection of a previous-turn bootstrap after a newer local send.
- Pre-deployment gates pass: `hone-agent` 96/96, `hone-channels` 642/642, focused `hone-tools web_search` 18/18, Web 274/274 plus typecheck and public production build, workspace check/test with the documented platform exclusions, and the complete CI-safe regression runner. Rust formatting, shell syntax, prompt-size (`11,914 / 12,000` characters), and diff checks pass. Two independent read-only reviews found and then verified closure of the DataFetch fallback-scope and stale-bootstrap/next-send race blockers. The only emitted compiler warning is the pre-existing unused `feishu_direct_actor_contact_targets_from_records`; the optional matplotlib smoke test remains skipped when that local dependency is absent.
- Full repository gates have passed. An exact-commit build, zero-active-chat graceful supervisor restart, PG/OSS/API/channel/process health checks, and fresh `crwv和英伟达有什么关系`, `crwv和nbis的估值怎么看`, and non-finance canaries remain required. Acceptance requires current source support, no unsupported ownership/rank/model/contract conclusions, one run/answer/terminal, no error/reset/refusal/flicker, exact history, POST one-shot behavior, and active chats back to zero.
- This handoff and the umbrella plan remain `in_progress`; do not archive them until exact deployment and production acceptance are recorded and the separately tracked scheduler entity P2 is resolved.

## Phase 13 — Bind Finish Locators To Real Current-Turn Source IDs

### `ae4d9a8f` Deployment: Runtime Healthy, Direct Content Canary Failed

Commit `ae4d9a8f` passed the complete repository gates, was pushed to `main`, built into an immutable deploy directory with exact prompt and 27-skill snapshots, and gracefully restarted only after the active-chat count remained zero. The previous process had been launched from a temporary worktree without the repository `.env`, which explained its local-mode metadata and missing public assets; the replacement started from the repository root with explicit public dist and runtime snapshot paths. `/api/meta` and the cloud doctor then reported cloud mode, authoritative Postgres, and object storage healthy. Ports 8077/8088, public routes, auth boundaries, Web, Discord, and Feishu were healthy, with no duplicate channel process.

The first internal probe incorrectly sent `channel_scope:"direct"`. In this API that value creates `Session_web__group__direct` and restores group history, so its stale-session result is not product evidence. A true direct request omits `channel_scope`; actor/session ownership then becomes `Actor_web__direct__<actor>`. Fresh actor `codex-canary-ae4d9a8f-relationship-direct-1784440318` used that correct shape for `crwv和英伟达有什么关系`. SSE contained one `run_started`, one assistant delta, one successful `run_finished`, no reset/error/legacy done/second output, and active chats returned to zero. Persisted history contained exactly one user and one byte-identical assistant row. First and final visible output arrived at about 101.748 seconds.

Transport passed but content failed. Audit showed healthy current-turn CRWV and NVIDIA DataFetch search/quote/profile calls plus one successful relationship Web search. FMP and Tavily were not down. The model's `finish_research` handoff instead cited generic strings—`web_search`, `quote`, and `profile`—as `tool_call_id`. Although the submitted Web result number and exact excerpt were sufficient to identify one real source uniquely, the ae4 runtime had no such generic-ID recovery; its Web reference and the malformed DataFetch references were all dropped. The resolved terminal handoff therefore contained no facts. An unrestricted empty-evidence terminal then relied on model memory: it reversed the available source's disclosed direction (NVIDIA purchasing unused CoreWeave capacity through 2032) and added unsupported customer/supplier rank, strategic investment, GPU models, allocation priority, and purchase claims. The answer was rejected as a canary even though deployment, providers, stream uniqueness, history equality, and active-run cleanup were healthy.

### D07 Architecture Correction

- Immediately before `finish_research` is exposed, the runtime walks only the current business turn and joins each tool result to its matching invocation. A bounded citable source catalog admits successful non-search DataFetch results with usable scalar data and successful Web invocations with at least one result containing an original URL plus excerpt-capable title/content/snippet. DataFetch search, failures, empty data, results without invocations, unsupported tools, and non-citable Web rows are excluded.
- A nonempty catalog becomes the exact opaque-ID enum for every evidence `tool_call_id` in the otherwise flat provider-compatible finish schema. The active prompt repeats the exact IDs with structural descriptions such as data type/target or citable Web result numbers, but does not inject URLs, excerpts, errors, or the raw transcript. An empty catalog emits no empty enum.
- Exact current-turn IDs remain authoritative. When an OpenAI-compatible provider substitutes a generic Web tool name, result number plus verbatim excerpt may recover the actual ID only if exactly one current-turn Web invocation matches; zero or multiple matches resolve nothing. DataFetch generic names such as `quote` or `profile` are never guessed.
- When the catalog contains citable sources, either an unresolved locator or a handoff with no mechanically resolved related fact/scoped fallback causes the same Agent to receive one hidden pre-terminal instruction to correct source addresses only by copying exact catalog IDs and keeping Web/DataFetch locator fields separate. This is bounded by one correction, remains inside the same Agent/context, emits no visible draft, reruns no business tool, and does not inspect, rewrite, approve, or reject final prose.
- Valid sibling facts remain available. After the one correction, unresolved siblings may be omitted only when at least one valid related fact or scoped fallback survives; otherwise the run fails as a genuine finish-protocol error and never enters an unrestricted empty-evidence terminal. A direct gaps-only handoff is legal only when the current-turn citable catalog is empty because the real attempted sources supplied no citable result; it imports no evidence and must disclose the concrete gaps. Catalog-nonempty gaps-only never authorizes terminal. Syntax-broken finish controls likewise have no implicit whole-turn authority; with citable sources they receive the same single correction, otherwise they fail. Complete DirectFinal remains an unchanged no-veto fallback, and no post-answer semantic gate, repair, rewrite, fixed refusal, or second generation is introduced.

### Verification And Remaining Work

- Focused verification passes `hone-agent` 101/101, `hone-channels` 642/642, and finance static contracts 39/39. Coverage proves the source catalog includes only citable matching current-turn sources, the schema enumerates exact IDs without emitting an empty enum for an empty catalog, generic Web recovery requires a unique excerpt match, generic DataFetch IDs are corrected before one grounded terminal, malformed finish receives at most one hidden same-Agent locator correction, direct gaps-only succeeds only with an empty catalog, and nonempty-catalog gaps-only/repeated-all-invalid evidence fails without an empty terminal.
- Full proportional repository gates must still rerun; the focused counts above do not claim the full workspace, Web, or CI-safe suite.
- No D07 commit, immutable rebuild, production restart, or fresh acceptance canary is claimed yet. Remaining work is exact-commit build, zero-active-chat graceful restart, PG/OSS/API/channel/process health checks, and fresh direct `crwv和英伟达有什么关系`, `crwv和nbis的估值怎么看`, and non-finance replays. Relationship acceptance requires correct source direction, no unsupported ownership/rank/GPU/contract claims, one start/answer/successful terminal, no reset/error/refusal/flicker, byte-identical two-row history, active chats back to zero, and TTFT/final timing recorded separately. The 101.748-second ae4 baseline was dominated by model rounds rather than FMP/Tavily and remains a latency failure to optimize after correctness.
- Status remains `in_progress`. Do not archive this handoff or the umbrella plan; the independent scheduler 800G/NAND/AST/SEC entity P2 also remains open.

## Phase 14 — Remove The Interactive Finish Pipeline And Restore Natural DirectFinal

### `d4d44735` Canary: Providers Healthy, Completion Architecture Failed

The next exact production replay of `crwv和英伟达有什么关系` ran on `d4d44735` and again disproved the provider-outage diagnosis. Ten DataFetch/Web calls succeeded; FMP, DataFetch, and Tavily supplied current identity, quote/profile, and relationship evidence. The full turn still made seven model calls and took 92.435 seconds. Business-tool time was only about 8.9 seconds. After evidence was already in context, the finish-locator round used about 13.993 seconds, the hidden locator correction about 19.925 seconds, and the separate tool-free terminal about 19.363 seconds.

That extra synthesis was not merely slow. It treated `market_date_new_york` as proof of a New York Stock Exchange close even though structured entity evidence identified NASDAQ, and it expanded an empty validated-inference set into unsupported “核心 GPU 供应商 / 大客户 / 高度依赖 / 锁定 / 多重绑定” conclusions. The brief intermediate error was part of the service-imposed finish/correction/terminal path. The incident was current deployment plus healthy tools followed by an unsafe second-generation protocol—not stale binaries, FMP/Tavily failure, missing credentials, or a configured ban on answering.

### Superseding Architecture

- Production Interactive finance returns to the ordinary Agent loop requested by the product: the same Agent reads the complete query, identifies all named securities, loads DataFetch/Web/business-tool results into the same context, and then either calls another real tool or writes one natural DirectFinal. No service completion role is inserted between research and answer.
- `finish_research` is no longer exposed by the production Interactive runner. Structured handoff, opaque source-id catalog, locator-only correction, tool-free terminal synthesis/recovery, terminal audit, post-answer semantic review, second generation, answer rewrite, and fixed completeness refusal are all disabled in production. The legacy state/builder is now test-only at compile time.
- The natural research-mode request flag is independent of Web terminal-stream policy and is set for Interactive `function_calling` finance on Web, Feishu, Discord, and Telegram. Requests use the composed current input plus at most four prior user utterances / 4,000 characters, clearly labelled for follow-up reference only; historical assistant/tool/reasoning claims, prices, financials, and conclusions are never replayed. Thus “它/第二个” still works while an old assistant `NBIS = 15 USD` claim cannot contaminate current `CRWV + NVIDIA` evidence.
- The Agent-declared entity/evidence ledger remains. Separate stable `entity_route` values, call-scoped exact/name matching, identity-only search, same-route quote/profile/asset routing, bounded empty attempts, and lowercase/mixed-case exact-symbol normalization continue to guide the loop. Before the structural floor, only real business tools are requested under `Required`; after the floor, the same real tools use `Auto`, allowing more evidence calls or natural DirectFinal. The floor is guidance/telemetry, never publication authority.
- Tool-capable rounds remain buffered behind the one-output boundary because a prose preamble may still be followed by a tool call. Only a complete natural `Stop + Done` answer is emitted and persisted once. This removes reset/second-answer flicker without paying for a completion handoff or second synthesis.
- The natural-final prompt now owns the evidence rules that the terminal previously mishandled. The answer time is the current Session Beijing time. Quote-source time prefers `hone_quote_time.beijing`. `market_date_new_york` or generic `new_york` values are date/timezone context only and may not become “纽交所”, “NYSE”, “收盘价”, or “常规交易收盘”. Exchange wording comes only from structured `exchange` / `exchangeShortName` fields.
- Relationship direction and strength must stay inside current evidence. When the sources do not directly establish strength, the Agent uses neutral bounded wording and removes “核心/最大/主要客户或供应商”, “高度依赖”, “锁定/排他/优先”, “多重绑定”, ownership/non-ownership, GPU model, contract value, and equivalent unsupported detail. This remains prompt-driven Agent discipline, not a service keyword filter or post-answer gate.
- Typed scheduled/heartbeat investment contracts remain separate and retain deterministic validation/fail-closed behavior. Browser chat POST one-shot, recovery-first UI, server-owned run identity/time, and unique terminal rules are unchanged.

### Focused Verification And Remaining Work

- Production-path regression proves that the eligible model request sees only real registered business tools, never `finish_research`, and returns the original natural DirectFinal with no terminal call or terminal audit. Additional regressions cover compile-time test-only quarantine of the retired schema/locator/terminal/prefix state; ordinary unknown-tool feedback for a hallucinated retired name without a public ToolStatus flash; lowercase `crwv` exact-symbol resolution to `CRWV`; bounded prior-user follow-up wording with stale assistant/tool/price exclusion; and channel-independent mode selection. The CRWV/NVIDIA behavioral fixture now supplies exact quote time, NASDAQ fields, explicit NVIDIA-to-CoreWeave capacity-purchase direction, NVIDIA investor wording, and MFN wording in its tool evidence before the natural final may use them. Prompt regressions cover exact Session/quote/market-date/exchange semantics and neutral relationship strength without repeatedly priming the model with internal refusal/rewrite terms.
- The finance static contract rejects production use of the legacy finish-terminal mode, verifies each retired entry point is `cfg(test)`, and requires the natural-loop runner switch, bounded history boundary, registered-tool observer boundary, and grounded relationship fixture. Tool-enabled deltas remain hidden, while one final body remains visible and byte-identical in persistence.
- Full proportional gates, exact commit/build, zero-active-chat graceful supervisor restart, PG/object-storage/API/channel/process health checks, and fresh production canaries are still required. Replays must cover `crwv和英伟达有什么关系`, `crwv和nbis的估值怎么看`, a single-ticker/no-coverage case, and a non-finance query. Acceptance requires no finish/terminal/audit operation, ideally no more than five model calls, under-60-second first-visible target, correct Session/quote/exchange language, no unsupported relationship-strength expansion, one output/terminal, byte-identical two-row history, and active chats back to zero.
- This handoff and the umbrella ticker plan remain `in_progress` and are not archived. Even after Interactive production acceptance, the independent scheduler `800G` / `NAND` / `AST` / `SEC` entity P2 must still be resolved before the umbrella plan can close.
