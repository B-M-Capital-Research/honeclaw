# v0.15.0 Formal Release

- title: v0.15.0 Formal Release
- status: done
- created_at: 2026-07-26
- updated_at: 2026-07-26
- owner: Codex
- related_files:
  - `Cargo.toml`
  - `Cargo.lock`
  - `bins/hone-desktop/tauri.conf.json`
  - `bins/hone-user-app/tauri.conf.json`
  - `apps/hone-ios/HONE.xcodeproj/project.pbxproj`
  - `docs/releases/v0.15.0.md`
  - `resources/architecture.svg`
  - `resources/architecture.html`
  - `.github/workflows/release.yml`
- related_docs:
  - `docs/releases/README.md`
  - `docs/runbooks/desktop-release-app-runtime.md`
  - `docs/current-plan.md`
  - `docs/archive/index.md`

## Goal

Publish the current `main` as `v0.15.0`, including synchronized version metadata and bilingual release notes, an annotated Git tag, a GitHub Release, a Universal macOS DMG, and the repository's supported iOS Simulator/Xcode artifacts.

## Scope

- Audit `v0.14.1..HEAD`, the clean worktree, remote parity, Apple client build contracts, and release automation.
- Bump all workspace, Tauri, and Xcode marketing versions to `0.15.0`.
- Refresh the architecture poster for the new release because the Agent runner architecture changed after `v0.14.1`.
- Run the release baseline plus targeted Agent/channel/Web/Apple client validation.
- Push the release commit to `main`, create and push annotated tag `v0.15.0`, and monitor the tag-triggered Release workflow until the GitHub Release and expected assets are complete.
- The repository intentionally publishes an unsigned/ad-hoc Universal macOS DMG and an unsigned iOS Simulator app plus Xcode source archive. A device IPA/App Store archive is outside this release because this host and CI do not have an Apple Developer signing identity or provisioning profile.

## Validation

- `bash scripts/ci/check_fmt_changed.sh`
- `cargo check --workspace --all-targets --exclude hone-desktop --exclude hone-user-app`
- `cargo test --workspace --all-targets --exclude hone-desktop --exclude hone-user-app`
- `bun run test:web`
- `cd workers/public-community-edge && bun run typecheck && bun run test`
- `bash tests/regression/run_ci.sh`
- `cargo test -p hone-user-app`
- `cargo check -p hone-user-app`
- `bash -n scripts/build_user_app.sh`
- `bash tests/regression/ci/test_hone_ios_contract.sh`
- macOS user-app release build through `scripts/build_user_app.sh`
- iOS Release Simulator build through `xcodebuild` locally when full Xcode is available, otherwise require the macOS GitHub Actions job to pass and publish `HONE-iOS-Simulator-v0.15.0.zip`
- `bash scripts/prepare_release_notes.sh v0.15.0 /tmp/release-notes-v0.15.0.md`
- Render and visually inspect `resources/architecture.svg`
- Verify the final GitHub Release asset names and SHA-256 manifest

## Result

- Release commit `a96425bad98ffc08c64c35718bd8b85245d43e51` was pushed to `main`.
- Annotated tag `v0.15.0` was pushed and published as a non-draft, non-prerelease GitHub Release.
- GitHub Actions run `30207643379` passed every job, including Universal macOS App, iOS Simulator App, Linux/macOS CLI assets, checksums, and Homebrew formula publication.
- The Release contains all eight expected assets. Downloaded Apple assets passed `HONE-Apple-SHASUMS256.txt`; the DMG passed `hdiutil verify`, contained a strictly valid ad-hoc signed Universal `0.15.0` App, and the iOS Simulator archive contained a Universal `0.15.0` App.
- The final handoff is `docs/handoffs/2026-07-26-v0.15.0-formal-release.md`.

## Documentation Sync

- Added this task to `docs/current-plan.md` while release work was active.
- Added bilingual `docs/releases/v0.15.0.md`.
- Refreshed `resources/architecture.svg` and `resources/architecture.html`.
- Moved this plan to `docs/archive/plans/`, added a release handoff and archive entry, and removed the active index entry.

## Risks / Open Questions

- This Mac currently exposes Command Line Tools rather than full Xcode and has no valid Apple code-signing identity. The tag-triggered `macos-14` workflow is therefore the authoritative Apple build environment.
- The Universal macOS DMG remains ad-hoc signed unless repository secrets are expanded to Developer ID/notarization credentials.
- The iOS deliverable is a Simulator `.app` and Xcode source archive, not a device-installable IPA; producing an IPA requires Apple Developer signing and provisioning that are not configured.
- The Release workflow also updates the Homebrew tap and must be monitored through completion rather than treating a pushed tag as completion.

All listed risks were handled within the intended release contract. The Apple assets remain ad-hoc/unsigned and are not device/App Store artifacts; no unresolved release blocker remains.
