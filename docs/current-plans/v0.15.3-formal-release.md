# v0.15.3 飞书原生表格修复正式发布

- title: v0.15.3 飞书原生表格修复正式发布
- status: in_progress
- created_at: 2026-07-27
- updated_at: 2026-07-27
- owner: Codex
- related_files:
  - `Cargo.toml`
  - `Cargo.lock`
  - `bins/hone-desktop/tauri.conf.json`
  - `bins/hone-user-app/tauri.conf.json`
  - `apps/hone-ios/HONE.xcodeproj/project.pbxproj`
  - `bins/hone-feishu/src/markdown.rs`
  - `bins/hone-feishu/src/outbound.rs`
  - `crates/hone-channels/src/prompt.rs`
  - `docs/releases/v0.15.3.md`
- related_docs:
  - `docs/archive/plans/feishu-native-table-rendering.md`
  - `docs/handoffs/2026-07-27-feishu-native-table-rendering.md`
  - `docs/handoffs/2026-07-27-v0.15.3-formal-release.md`
  - `docs/archive/index.md`
- related_prs: pending

## Goal

把飞书表格源码外泄的根因修复作为 `v0.15.3` 补丁版本发布：同步所有正式版本位置，补齐双语用户向 release notes，通过必要门禁，把 release commit 推到 `main`，再创建并推送 annotated tag。

## Scope

- 只吸收从当前 `origin/main` 到飞书原生表格修复所需的代码、回归与上下文文档。
- 不合并原工作区正在进行的 merge、无关暂存改动或冲突文件。
- 不执行生产部署、运行时重启或真实用户消息实发。
- 不更新 `resources/architecture.svg`：本次只修复 Feishu channel 内部序列化与降级行为，没有新增/删除 channel binary、route、runner、tool、crate 或 storage 布局。

## Validation

- 通过：`bash scripts/ci/check_fmt_changed.sh`
- 通过：`cargo check --workspace --all-targets --exclude hone-desktop --exclude hone-user-app`
- 通过：`cargo test -p hone-feishu -- --nocapture`（69 个测试）
- 通过：`cargo test -p hone-channels prompt --lib -- --nocapture`（55 个相关测试）
- 通过：`cargo test -p hone-channels raw_table_component_copy --lib -- --nocapture`（两条既有 shared sanitizer / scheduler 回归）
- 通过：本次 3 个 Rust 文件 scoped `rustfmt --check` 与全变更 `git diff --check`
- 通过：`bash scripts/prepare_release_notes.sh v0.15.3 /tmp/release-notes-v0.15.3.md`
- 待完成：release commit、push、tag 与远端 workflow 核对

## Documentation Sync

- 创建 `docs/releases/v0.15.3.md`。
- 继续保留并引用表格修复 plan、handoff、bug ledger 与 repo map。
- 完成后把本计划移入 `docs/archive/plans/`，新增发布 handoff，更新 `docs/archive/index.md` 并从 `docs/current-plan.md` 移除。
- 无需更新 `docs/decisions.md` / ADR：协议纠错不改变架构取舍。

## Risks / Open Questions

- 原工作区处于未完成 merge 且有无关冲突；发布必须始终在基于最新 `origin/main` 的独立 clone 中完成。
- tag 推送只代表 Release workflow 已触发；远端资产需要等待工作流完成后再确认。
- 本次不部署生产，线上 Feishu 仍需在后续正常部署后复核原生表格显示。
