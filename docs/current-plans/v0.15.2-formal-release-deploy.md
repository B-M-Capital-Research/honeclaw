# v0.15.2 Formal Release And Production Deployment

- title: v0.15.2 Formal Release And Production Deployment
- status: in_progress
- created_at: 2026-07-27
- updated_at: 2026-07-27
- owner: Codex
- related_files:
  - `Cargo.toml`
  - `Cargo.lock`
  - `bins/hone-desktop/tauri.conf.json`
  - `bins/hone-user-app/tauri.conf.json`
  - `apps/hone-ios/HONE.xcodeproj/project.pbxproj`
  - `docs/releases/v0.15.2.md`
  - `docs/runbooks/backend-deployment.md`
  - `.github/workflows/release.yml`
- related_docs:
  - `docs/releases/v0.15.1.md`
  - `docs/handoffs/2026-07-27-security-audit-remediation-release.md`
  - `docs/current-plan.md`
  - `docs/archive/index.md`

## Goal

Synchronize the latest remote `main`, publish it as `v0.15.2`, replace the old production `0.14.1` runtime with an exact immutable `0.15.2` build through the established supervisor, and publish newly verified macOS and iOS release assets.

## Scope

- Preserve all current answer-format, interaction, cloud-authority, and channel contracts.
- Carry the already released but not yet deployed `v0.15.1` security baseline into production.
- Synchronize workspace, Tauri, and Xcode versions to `0.15.2`.
- Build a new immutable runtime directory and retain `target/deploy-620391d1` as the immediate rollback package.
- Require two zero-active-chat checks before replacement, then verify process paths, ports, cloud authority, authentication boundaries, public assets, history, and Feishu.
- Build and locally verify the Universal macOS App. Require the tag-triggered Apple job for the iOS Simulator/Xcode artifacts because this host exposes Command Line Tools rather than full Xcode.
- Push the release commit to `main`, create and push annotated tag `v0.15.2`, monitor the GitHub Release, and verify all published checksums.

## Validation

- `bash scripts/ci/check_fmt_changed.sh`
- `cargo check --workspace --all-targets --exclude hone-desktop --exclude hone-user-app`
- `cargo test --workspace --all-targets --exclude hone-desktop --exclude hone-user-app`
- `bun run test:web`
- `cd workers/public-community-edge && bun run typecheck && bun run test`
- `bash tests/regression/run_ci.sh`
- `cargo test -p hone-user-app`
- `cargo check -p hone-user-app`
- `bash -n scripts/build_user_app.sh`
- `bash tests/regression/ci/test_hone_ios_contract.sh`
- `env CARGO_INCREMENTAL=0 bash scripts/build_user_app.sh`
- `bash scripts/prepare_release_notes.sh v0.15.2 /tmp/release-notes-v0.15.2.md`
- Exact immutable deployment manifest verification
- Local/origin/public production health and auth-boundary probes
- Final GitHub Release inventory plus Apple SHA-256, DMG, and iOS bundle inspection

## Documentation Sync

- Track the active release/deployment in `docs/current-plan.md` and this plan.
- Add bilingual `docs/releases/v0.15.2.md`.
- Do not update `resources/architecture.svg`: the release contains no architecture change, and the reason is recorded in the release notes.
- On completion, archive this plan, add a release/deployment handoff, update `docs/archive/index.md`, and remove the active plan entry.

## Risks / Open Questions

- The production runtime is currently `0.14.1`, so the rollout activates cumulative security and dependency changes. Keep the exact prior immutable package intact for immediate rollback.
- Discord is configured but already offline because its gateway credential is rejected; do not treat that pre-existing condition as a regression in Web/Feishu.
- The macOS package remains ad-hoc signed/not notarized, and the iOS deliverable remains Simulator/Xcode-only without Apple Developer signing and provisioning.
- Frontend Pages deployment is Git-connected and may finish independently of the backend restart. Compare local, backend-served, and public asset hashes before declaring the public rollout complete.
