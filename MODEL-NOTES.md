# MODEL-NOTES

外包执行池（codex / grok / opencode）在**本仓库**的后验观测。路由时先看这里，再看先验；
收活时按下面的规则记一笔。只写可核对的事实（返工几轮、验证是否一次过、有没有偏离 spec、改了哪些不该改的文件），
不写「表现不错」这类评价。

## 复杂度分档

| 档 | 含义 | 判据 |
|---|---|---|
| L1 机械 | 规则明确，做错能被工具发现 | 重命名、格式转换、按模板扩展、补样板测试 |
| L2 有界实现 | 边界清晰，有现成范式可模仿，验收标准写得出来 | 一个新组件、一个 DAL 方法、一条协议解析 |
| L3 需要判断 | 要在方案间取舍，或要理解没写出来的约束 | 接线、跨模块改动、根因不明的 bug、改核心 IO 路径 |
| L4 开放式 | 目标本身要先定义 | 架构设计、方案取舍 |

## 逐单流水

`日期 | 模型+版本 | 任务类型 | 复杂度 | 结果 | 失败模式`

- 2026-09-06 | codex gpt-6-astra（xhigh，本机 ChatGPT.app codex-cli 0.153.4）| Rust：hone-core 结构体/枚举/幂等/日期解析/派生日期 + 工具 action + web-api 字段，含测试 | L2（三文件、spec 给全锚点与基准数）| 一次过：18/8/11 全绿、workspace check 过、rustfmt 过；未碰范围外文件；报告按要求列了三条关键决策与未做项 | 无。细节：`content_as_of` 用 `(date, day_precision)` 元组取 max，比 spec 描述更干净；`set_watch` 工具层合并、核心层整条替换，与 spec 一致
- 2026-09-06 | grok grok-4.6-build（grok 1.0.5，cost $0.31，20 turns）| 底稿 JSON：折回线上 4 条改动、AVGO 三处改到 FY26Q3、103 条关注点/变量按来源日期标 `as_of` | L2（有校验脚本作硬闸门）| 一次过：校验脚本 OK（96/103 标注）、hone-core 测试过、只改了那一个文件；报告给了逐条判定表与 6 条不确定项，判断口径合理（`what`/`where` 里的数字不计、多源取最新、对照数字不算引用） | 无。注意：它把「对照/不要倒推」的数字排除在引用之外，这是自行定的口径，事后看是对的但 spec 没写

## 模型画像

codex:gpt-6-astra
  行   Rust 有界实现（结构体 + serde + 测试，spec 带行号与基准数）— 1 单一次过
  待观测 跨模块接线、前端

grok:grok-4.6-build
  行   带硬校验脚本的 JSON 内容标注（长文本逐条判断 + 只允许白名单改动）— 1 单一次过
  待观测 Rust / 前端 / 无校验脚本的内容改写

opencode:o48 / o50 / 0812
  待观测 本仓库尚未派过
